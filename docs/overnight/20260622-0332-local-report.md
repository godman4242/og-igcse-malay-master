# Local build-loop report — 2026-06-22 03:32 UTC (T13 Grammar deep-dive guide)

**Time check (step 1):** KL `202606221132` — no finite CUTOFF set (forever loop). Proceed.

## Item shipped
Directed epic axis-3 (Full Page Guide), plan `docs/superpowers/plans/2026-06-21-guide-full-page-rollout-plan.md`
Phase 3c **T13 — the Grammar drills deep dive (`/grammar`)**. The header ▶ "Tour this page" now works on `/grammar`.

**Why this item:** queue all `[x]` through T12; no fresh axis-1/axis-2 gap cleared the GOAL bar (converged
content ledger trusted per prior cycles); `/grammar` is the explicit `▶ NEXT` thread from T12 + the next page
in the plan's priority order.

## What shipped
- `src/lib/guide/pageGuides.js` — `/grammar` entry (5 steps: centered intro + 4 anchored).
- `src/lib/guide/pageGuideRoutes.js` — `/grammar` added to the eager `PAGE_GUIDE_ROUTES` seam (~12 bytes).
- `src/pages/Grammar.jsx` — 4 `data-guide` anchors: `grammar-mode` (SRS/Cram pill), `grammar-lang`
  (language toggle row), `grammar-tabs` (tab row), `grammar-drill` on BOTH mutually-exclusive drill branches
  (Malay imbuhan card div + a new plain wrapper div around the English `McqDrillCard`).
- `tests/e2e/guide-grammar.spec.js` — new (2 tests).
- Fixture re-point: `guideController.test.js` + `guide-full-page.spec.js` "route with NO page guide" moved
  `/grammar` → `/settings` (last in rollout, T25 → no near-term churn).
- `README.md`, `RESUME_HERE.md`, plan doc — updated same commit.

## Decisions (decide-and-flag)
- **Non-theater page → header ▶ is the entry** (no floating pill, unlike `/study`).
- **All anchors on always-mounted elements**; `grammar-drill` on both language branches (only one in DOM at a
  time → arrow always resolves to one node, no hang-then-skip).
- **Step order = visual top-to-bottom** (mode → lang → tabs → drill) for calm arrow flow. Veto: swap 2↔4 to
  lead with language — zero rework.
- **Content:** no new Malay authored. Single example `meN- + tulis → menulis (the 't' drops)` = exact in-app
  vetted drill `prefix-meN-tulis` (`src/data/grammar.js`). All control claims grounded by reading `Grammar.jsx`.

## TDD red→green evidence
RED first (`npx vitest run pageGuides.test.js -t "grammar deep dive"`):
```
FAIL > exists with a centered intro + several anchored steps
  expect(Array.isArray(steps)).toBe(true)  → + false
FAIL > covers each control with a real grammar anchor
  TypeError: Cannot read properties of undefined (reading 'map')
```
GREEN after implementation: pageGuides + guideController = 52 passed.

## Gate result (authoritative)
- `npm run build` — exit 0 (eager index 479.37 kB / 151.78 kB gz; lazy `pageGuides` chunk 15.15 kB / 5.71 kB gz).
- `npm run test:run` — **1870 passed** (+2, was 1868).
- `npm run lint` — 0 errors (3 known exhaustive-deps warnings).
- e2e (UI-affecting): `guide-grammar` 2/2 + `guide-full-page` 4/4 (re-pointed) + `guide-roleplay`/`guide-practice`/`guide-smart-study` 2/2 each = **12/12**; `imbuhan-interleave` **7/7** (no Grammar regression from the wrapper + attributes; dark+light render tests green).

## Self-review (hostile)
No new Malay; example is a shipped vetted drill. No cross-language class leak (English wrapper is a plain
block div, no className). No theme change. No `STORE_VERSION`/schema/free-path/`instruct.js` touch. Anchors
verified present (e2e count 1). No double-ship (origin/main not ahead; T13 absent on origin).

## Deploy
Committed to `main` (= prod deploy). Re-checked origin before push: not ahead, T13 not already shipped.

## ▶ NEXT
Phase 3c **T14 — Writing (`/writing`)**, folding in its "Try a sample" (Phase 5 T7 — Writing already has a
sample-text button to mirror). A fresh axis-1/axis-2 gap still preempts if one clears the bar.
