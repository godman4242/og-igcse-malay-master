# Local build-loop cycle — UTC 2026-06-14 19:15 (KL 2026-06-15 03:15)

**Mode:** GOAL-driven Self-source (autonomous build queue empty — all items `[x]`).
**Item shipped:** Content-truth fix — word-family explorer taught the non-word `berdidik` (axis-1, content truth).

## Assessment against `docs/loop/GOAL.md` axes
- **Axis 1 (content truth):** Flagged Cikgu/grammar threads no longer clear the bar — paper-numbering
  inversion = product decision (NOT solo); `exam-paper2` Writing format entangled with it; `imbuhan-se`
  `semua`/`seluruh` murky (could hurt totality-vocab pedagogy). Audited a **still-unaudited** student-facing
  content surface: `src/data/wordFamilies.js` (word-family explorer) → found a fabricated word.
- **Axis 5 (critical-risk coverage):** already strong — store migrations v26–v34 have dedicated tests,
  cloud-sync covered by the Q-1 two-device integration suite. No clear gap.
- Axes 2/3/4/6: no clearly-evidenced, measurable gap surfaced above the axis-1 find.

## The gap (Real + Measurable + Verified)
`src/data/wordFamilies.js:190` — the `didik` (educate) family listed
`{ word:'berdidik', type:'ber-', meaning:'educated (adj)' }`. `WordFamilies.jsx` renders every
`forms[].word` to the student as a legitimate derivation. **`berdidik` is not a Malay word** — same bug
class as the prior `penjadi` non-word fix.

## Web verification (DBP — authoritative)
- `berdidik` → **no entry**: "Tiada maklumat tesaurus untuk kata berdidik"
  (https://prpm.dbp.gov.my/Cari1?keyword=berdidik)
- `terdidik` → **found**: "mendapat latihan (pengajaran dll), terlatih" = educated/well-trained
  (https://prpm.dbp.gov.my/Cari1?keyword=terdidik). Also in the Education Ministry motto "Insan Terdidik".
- Corroborated internally: the `latih` family already carries `terlatih` (ter-, "well-trained").

## Fix (surgical, 1 data line)
`{ word:'berdidik', type:'ber-', meaning:'educated (adj)' }` →
`{ word:'terdidik', type:'ter-', meaning:'educated/well-trained', pos:'adj' }`.
Scope kept to ONE item: the orphan `berdidik.webp` icon-manifest/script entries are build tooling (not
student-facing; WordFamilies uses no per-word icons) — left untouched, flagged in ▶ NEXT.

## TDD (red → green)
New `src/data/__tests__/wordFamilies.test.js` (+3). Watched **2 of 3 FAIL first** (berdidik present,
terdidik absent) with the non-vacuity test passing; all 3 green after the fix.

## Gate
- `npm run build` — green (data file; `index` chunk unchanged)
- `npm run test:run` — **1622 passed** (155 files; +3 vs 1619)
- `npm run lint` — 0 errors (3 pre-existing exhaustive-deps warnings only)
- e2e skipped by design (pure data swap, no layout/flow change; CI runs e2e on push)

## Invariants
No STORE_VERSION bump · no Supabase-schema/free-path break · no feature deleted · `instruct.js` API
untouched · no secrets · content web-verified against DBP.

## Deploy
One commit to `main` (= prod deploy). Pre-push re-sync: origin/main unchanged (0 ahead / 0 behind) —
no concurrent cloud-builder collision.
