# Local build-loop cycle — 2026-06-23 03:15 UTC (11:15 KL)

## Item
**AXIS-1 content-truth — fix the WRONG "Listening (Paper 4)" exam-paper labels.**
First unchecked `[ ]` at the top of the `RESUME_HERE.md` 🤖 queue (axis-1 content-truth
preempts the directed axis-3 epic per `GOAL.md`). Pre-vetted + Kheshav-cleared 2026-06-22.

## Anti-hallucination gate — re-web-verified this cycle
- **Cambridge IGCSE Malay – Foreign Language 0546** (2025–27 syllabus): Paper 1 = **Listening** ·
  Paper 2 = **Reading** · Paper 3 = **Speaking** · Paper 4 = **Writing**.
- **Cambridge IGCSE English – Second Language 0510** (2024–26 syllabus): Paper 1 = Reading &
  Writing · Paper 2 = **Listening** · Component 3 = Speaking.
- ⇒ "Listening = **Paper 4**" is confident-wrong for *both* (Paper 4 is the Malay **Writing**
  paper; listening is Paper 1 Malay / Paper 2 English). The app's listening + comprehension
  surfaces are **bilingual** (lead-by-`studyLang`), so no single number is correct → **drop the
  number, label by skill** (Kheshav's 2026-06-22 decision). `Paper 4 = Writing` stays where the
  copy is single-syllabus Malay writing (verified-correct).

## What shipped (9 source edits + 1 new test + README)
User-facing labels (bilingual surfaces → drop number):
- `src/pages/ExamRehearsal.jsx` — `label="Listening (Paper 4)"` → `"Listening"` ×2 (stage chip
  :356, in-stage `StageHeader` :523); intro prose "a Paper-4 audio clip" → "an audio clip".
- `src/pages/Listening.jsx:62` — heading "Paper 4 Listening" → "Listening".
- `src/pages/Comprehension.jsx` — `<h2>` "Paper 1 Comprehension" → "Comprehension"; `<Meta>`
  desc "IGCSE Paper 1 reading skills" → "IGCSE reading skills".
- `src/lib/guide/tourSteps.js:166` — FULL_TOUR `title:'Listening (Paper 4)'` → `'Listening'`.
- `src/lib/guide/pageGuides.js` — `/listening` body "Paper 4 listening skill" → "listening
  skill"; `/comprehension` body "Paper 1 reading skill" → "reading skill".

Comments (drop the listening/reading paper tag, for the grep-Done):
- `src/pages/ExamRehearsal.jsx:27,104`, `src/pages/Listening.jsx:9`,
  `src/data/listeningPassages.js:1`, `src/lib/passageOrder.js:4-5`,
  `tests/e2e/study-lang.spec.js:119`.

Docs:
- `README.md:42` — "Paper 4 listening practice" → "Listening practice".

KEEP (verified-correct `Paper 4 = Writing`, NOT stripped): `TemplatesView.jsx:11`
"Paper 4 Q3 karangan"; `pageGuides.js:301-302` "Malay Paper 2 or Paper 4" (Writing-analyzer
toggle copy); `connectors.js:5` comment. A KEEP-boundary test guards against over-stripping.

**Scope note (flagged):** the queue's enumerated FIX list was narrower than its own measurable
Done ("no bilingual surface shows a listening/reading paper number"). Done governs → I also
fixed the two user-facing headings the list omitted (`Listening.jsx`, `Comprehension.jsx`).

## TDD red → green
New `src/lib/__tests__/examPaperLabels.test.js` (source-scan style, mirrors
`contentLint.test.js` / `studyFeedbackA11y.test.js`).
- **RED first:** 7 of 8 failed for the right reason (the wrong labels were still present); the
  KEEP-boundary test passed (TemplatesView still names Paper 4).
- **GREEN after edits:** 8/8.
Pins: FULL_TOUR listening title === 'Listening'; `/listening` + `/comprehension` page-guide copy
carry no `Paper[\s-]*\d`; `ExamRehearsal`/`Listening`/`Comprehension` source carry no paper
number; `passageOrder.js` + `listeningPassages.js` comments clean; TemplatesView KEEP-boundary.

## Gate
- `npm run build` — exit 0.
- `npm run test:run` — **1892 passed**, 183 files, 0 fail (+8 new).
- `npm run lint` — 0 errors, 3 known exhaustive-deps warnings (no new ones).
- e2e (UI-affecting): `guide-comprehension` 2/2 + `guide-listening` 2/2 + `study-lang` 5/5 = **9/9**.
- Theme: text-only swaps, no color/class/layout change → dark+light identical.
- Invariants: no `STORE_VERSION`/schema/free-path/`instruct.js` touch; no feature deleted.

## Decision log
- **Drop the number on bilingual surfaces** (not pick one syllabus's number) — the verified
  numbers diverge by syllabus, so any single number is wrong for half the users. *Veto:* if a
  surface were ever single-syllabus, name the verified paper there.
- **Defer the Malay-only "reading = Paper 1" mislabels** (Cikgu/system-prompt content) to a new
  queued `[ ]` item — that's a number-CORRECTION (→ Paper 2), a different fix-type from this
  number-DROP, and touches user-facing Cikgu content; folding it in would balloon a surgical
  diff. *Veto:* if Kheshav wants it bundled, it's one more cycle.
- **Flag the Writing-analyzer Malay "Paper 2 or Paper 4" sub-choice** as possibly-wrong (Malay
  0546 Paper 2 = Reading) but NOT silently flipped — it's a live feature toggle → SPEC it.

## Ship
Re-synced (`git fetch` + origin/main = 0/0 divergence, no rebase needed). One commit for the one
item, with the `RESUME_HERE.md` `[x]` checkoff + shipped section + the new follow-up `[ ]` item +
this report, all in the same commit. Pushed to `main` (= prod deploy).

## Step-1 time-check
No finite `CUTOFF` set (forever loop) → no cutoff gate. KL 11:15, well within bounds.
