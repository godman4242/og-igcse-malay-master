# Local build-loop cycle report — 2026-06-15 02:12 UTC

## Item shipped
**A11y fix (axis-3 / WCAG 2.1 SC 4.1.3 Status Messages):** the Comprehension (Paper-1 reading)
and Listening (Paper-4 listening) MCQ drills announced correct/incorrect feedback **visually only**
— no live region — so a screen-reader / switch learner heard nothing. Mounted the shared
`<FeedbackLive>` polite live region on both, bound to the verdict + corrective explanation.

Self-sourced (queue empty → GOAL-driven assessment). Followed the prior TypeMode cycle's `▶ NEXT`
lead: *"re-assess axes 2/3 (pedagogy / a11y) for the next evidenced gap."*

## Assessment (how this gap was found)
- Queue: 0 unchecked items → Self-source mode.
- Swept the study-mode graders (axis-1 follow-up): confirmed every other grader uses exact `===`
  (ClozeMode/ListenMode/FlashcardMode variants/QuizMode/ProduceMode) — the substring false-credit
  bug class is genuinely closed. No new axis-1 code gap.
- Swept axis-3 (a11y): `grep` for `FeedbackLive` vs. feedback/correct state across `src/components`
  + `src/pages`. Found **two interactive drill pages with feedback state but NO `FeedbackLive`**:
  `Comprehension.jsx` and `Listening.jsx`.
- Verified the gap is real: `Comprehension.jsx:438-441` + `Listening.jsx:292-295` render the verdict
  as a plain `<p>`; grep confirmed **zero `aria-live`/`role="status"`** in either file. CLAUDE.md's
  convention ("every drill announces via a polite live region... or SRs hear nothing") is violated —
  these two were missed in the original FeedbackLive rollout.

## Anti-hallucination gate
- **Real:** concrete `file:line` evidence + grep-confirmed absence of any live region. ✔
- **Measurable Done:** a `role="status"` region exists and carries the verdict text after an answer
  (RED→GREEN behavioural test). ✔
- **Verified:** no new content — reuses the EXACT existing visible verdict strings; WCAG citation is
  standard. Nothing to web-verify. ✔

## Red → green evidence
- Wrote `studyFeedbackA11y.test.js` +2 (behavioural Comprehension + structural both pages) FIRST.
- RED: behavioural failed `expected null to be truthy` (no `role="status"` in the question view);
  structural failed (no `FeedbackLive` import). Both for the right reason.
- Implemented: import + unconditional `<FeedbackLive text={feedbackText}>` mount on both pages.
- GREEN: `studyFeedbackA11y.test.js` 8/8 pass.

## Decision log
- **Decision:** reuse shared `FeedbackLive`; announce verdict **+ explanation**.
  - *Why:* exact established convention (no new component/content); explanation gives SR users parity
    with the visible feedback box (verdict + explanation shown together), mirroring ClozeMode's
    "the answer is X" corrective announcement.
  - *Veto:* verdict-alone rejected (less helpful for a comprehension drill where the reasoning is the
    teaching moment; explanation already on-screen → announcing it is parity, not new content).
- **Decision:** behavioural test for Comprehension, structural for Listening.
  - *Why:* Listening's questions only unlock after `playPassage`, `disabled` when `hasSpeechSynthesis()`
    is false (jsdom) → its question view is unreachable in a unit test. Structural pin matches the
    repo's own precedent for hard-to-drive heavy pages (the Grammar page in this same test file). The
    code change is identical in shape to Comprehension's (behaviourally proven).

## Gate result
- `npm run build` — OK. Comprehension chunk 13.6 KB / Listening 10 KB raw (both ≪ 70 KB budget).
- `npm run test:run` — **1664 passed** (159 files, +2).
- `npm run lint` — **0 errors** (3 pre-existing exhaustive-deps warnings, unchanged: RoleplayScorecard,
  Comprehension [pre-existing `userInterests`/useMemo], Roleplay).
- e2e not required: invisible non-interactive `sr-only` div — no visible screen/control/layout/flow;
  the mounted unit test drives the real component render + answer flow.

## Invariants
No paywall · no `STORE_VERSION` bump · no schema / free-path break · `instruct.js` API untouched ·
no feature deleted · no content authored · no secrets. ✔

## Files
- `src/pages/Comprehension.jsx` — import + `feedbackText` + `<FeedbackLive>` mount.
- `src/pages/Listening.jsx` — same.
- `src/components/__tests__/studyFeedbackA11y.test.js` — +2 tests (1 behavioural, 1 structural).
- `RESUME_HERE.md` — queue `[x]` + shipped section + `▶ NEXT`.

## Deploy
Commit to `main` (= prod deploy). Vercel READY to be confirmed after push.

## Step-1 time-check
No finite `CUTOFF` set (forever mode) — proceeded.
