# Local build-loop cycle report — 20260621-0609 UTC (commit 34f79e1)

## Item shipped
**Directed epic axis-3 (Full Page Guide, plan Phase 3b★ — T2★ + T6, shipped together):** the docked/minimized
in-app guide box is now a **persistent icon pill** (controls stay icon-only on hover/focus — no more
jitter-back-to-full-size under the pointer) and a **double-click on the box restores it** to the centre with full
labels (R5d).

## Why this item (assessment)
- Queue in `RESUME_HERE.md` was fully `[x]` (no in-flight item) → GOAL-driven Self-source mode.
- `docs/loop/GOAL.md`'s **ATTENDED RE-STEER (2026-06-21, commit 4db820f — latest on main)** LIFTED the overnight
  clamp and set Phase 3b★ as the top priority. The older shipped items' "NO-OP every cycle / Phase 4/5/3c
  attended-only" notes predate that commit and are superseded.
- Axis-1/axis-2 screen: the content-truth ledger (top of queue) is fully converged across every named + prose
  surface; no fresh evidenced content/pedagogy gap. So the directed axis-3 task = next unchecked plan item =
  Phase 3b★ T2★ (+ T6, which the plan mandates shipping together — without a restore path a no-hover minimized box
  is unrecoverable by pointer).

## Red → green evidence
- **TDD:** +6 unit tests. RED first (`npx vitest run popoverDecorations + guideController`): 4 failed for the right
  reason — decorator `dblclick→onRestore` + idempotent-no-stack (no listener yet); controller `restoreDefault`
  undefined + `onRestore` not passed to decorator. The 2 guard tests (nav-button→no-restore, no-onRestore→no-throw)
  passed before+after by design.
- GREEN after implement: 56/56 in the two guide unit suites (50 baseline + 6).

## Decision log
- **T2★ CSS:** remove ALL three `.guide-docked …:hover/:focus-within` re-expands (labels `display:inline`, box
  `max-width:340px`, description `display:block`); keep `display:none` so docked = icon-only always. Tighten the
  docked footer into a no-gap pill (`justify-content:flex-end`, nav-btns `gap:0` + `button+button{margin-left:0}`).
  *Veto:* layout-only props, no color tokens → dark+light identical, no theme risk.
- **T6 controller:** `restoreDefault()` = `if (dockedZone) undock()` then clear inline `left/top/right/bottom` +
  remove `data-guide-dragged` (labels return via the `.guide-docked` class drop). Exposed on the handle + passed to
  the decorator as `onRestore`.
- **T6 decorator:** ONE idempotent `dblclick` on the wrapper (guarded by `dataset.guideRestoreWired` — driver reuses
  the wrapper across renders) that ignores action controls (`RESTORE_IGNORE`). *Veto:* the grip ⠿ + box body stay
  valid restore surfaces; a double-tap on Next/Back never also restores.
- **a11y:** SR users keep names via the icon buttons' `aria-label` (unchanged); keyboard undock via grip arrow keys
  is untouched — double-click is a pointer convenience, not the only path.

## Gate result
- `npm run build` → exit 0. **Eager index byte-identical: 478.05 kB stash-verified** (all JS in the lazy guide
  chunk, CSS in the stylesheet bundle).
- `npm run test:run` → **1816 passed** (+6).
- `npm run lint` → 0 errors (3 known exhaustive-deps warnings).
- e2e `guide-drag-dock + guide-pause-skip + guide-full-page` → **9/9** (updated T2★ hover-keeps-labels-hidden + new
  T6 double-click-restore + all regressions).

## Deploy
- Commit `34f79e1` on `main`; `origin/main == local` confirmed (auto-push hook). Prod deploy triggered (Vercel).
- README guide bullet + plan doc (T2★/T6 checked) + RESUME_HERE shipped section updated in the same commit.

## Step-1 time check
No `CUTOFF` set (forever loop) → time check skipped; one cycle complete.

## ▶ NEXT
Phase 3b★ continues (clamp lifted, build top-down one commit/cycle): **T2b★** (e2e asserting the N-of-M jumper stays
visible+tappable in the minimized strip — already visible after this CSS, needs the test), **T2c★** (step explanation
in a SEPARATE box when minimized — today the description is hidden when docked), **Tpause★** (Pause HIDES the whole
guide chrome). Then **Phase 3c** per-page CONTENT, PDF reader FIRST (Kheshav spot-checks each live). A fresh
axis-1/axis-2 gap still preempts the epic if one clears the bar.
