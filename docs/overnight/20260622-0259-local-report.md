# Local build-loop cycle — 2026-06-22 02:59 UTC (10:59 KL)

## Item shipped
**Directed epic axis-3 (Full Page Guide) — Phase 3c, T11 part 1 of 2: the Smart Study (`/smart-study`) deep-dive guide.**
The header ▶ "Tour this page" now works on `/smart-study`.

- Plan: `docs/superpowers/plans/2026-06-21-guide-full-page-rollout-plan.md` Phase 3c T11.
- Queue was all `[x]`; no fresh axis-1/axis-2 gap cleared the bar (converged content ledger trusted per prior cycles). Picked the explicit `▶ NEXT` thread from T10 (the next page in the plan's priority order).

## What changed (surgical, 7 modified + 1 new)
- `src/lib/guide/pageGuides.js` — `/smart-study` entry: intro + 3 anchored steps (speaking toggle · Begin Session · Manual Study Mode).
- `src/lib/guide/pageGuideRoutes.js` — added `/smart-study` to the eager seam (lights the header ▶).
- `src/pages/SmartStudy.jsx` — 3 inert `data-guide="smartstudy-…"` anchors on the config-screen controls.
- `src/lib/guide/__tests__/pageGuides.test.js` — +2 unit tests (TDD).
- `tests/e2e/guide-smart-study.spec.js` — NEW, 2 tests.
- `README.md` — "Tour this page" line now lists Smart Study + its walk-through.
- `docs/superpowers/plans/…-rollout-plan.md` + `RESUME_HERE.md` — progress + shipped section.

## Key decision (flagged)
Guide targets the **config / landing screen**, not the active session. `SmartSession.jsx:36` enters theater mode only when `status === 'active'`, so the landing screen keeps the header visible → the **normal header ▶ is the entry** and all 3 anchors are mounted there. **No floating pill** (unlike /study) — one would be wrong, since in-session the config controls unmount (a config-screen guide launched mid-session would hang-then-skip on missing anchors). Veto: an in-session SmartSession deep dive can be added later keyed to its own controls, zero rework.

## Content grounding (axis-1, no confident-wrong)
No new Malay authored — English UI copy. Every pedagogy claim grounded against `src/lib/study/interleavedQueue.js`:
- "~20 min / ~5 cycles" → `buildSession`: `maxCycles = round(targetMinutes/4)` = 5.
- "recognition (flashcard) → recall (quiz/fill-the-blank) → production (write a sentence)" → `buildCycle` Steps 1–3.
- "spoken task at the end of some cycles" → `microSpeakTask` when `includeSpeaking && cycleIdx % 3 === 2`.
- "leads with words due today and recently got wrong" → `selectFocalCards` priority order.
- Deliberately did NOT repeat the Dashboard guide's looser "vocab, grammar and speaking" — the cycle has no grammar drills.

## Red → green evidence
- RED: added the 2 `/smart-study` unit tests first → `npx vitest run pageGuides.test.js` → 2 failed (`Array.isArray(steps)` false; `steps.map` on undefined) — `PAGE_GUIDES['/smart-study']` did not exist.
- GREEN: after adding the guide entry + route seam → 12/12 in that file.
- New e2e RED-then-GREEN by construction → `guide-smart-study` 2/2.

## Gate result
- `npm run build` — exit 0. `pageGuides` lazy chunk 9.42 kB (loaded only on ▶); eager `index` 479.33 kB / 153.28 kB gz; no per-route page chunk over the 70 kB budget.
- `npm run test:run` — **1864 passed** (+2, was 1862).
- `npm run lint` — 0 errors (3 known exhaustive-deps warnings).
- e2e (UI-affecting): `guide-smart-study` 2/2 + regressions `guide-full-page` 4/4 · `guide-pdf-reader` 2/2 · `guide-study` 2/2 · `guide-pause-skip` 1/1.

## ⚠️ Pre-existing e2e flake found (NOT this diff)
`guide-drag-dock.spec.js:246` (Tslide★, "holds across Next/Back") failed 3/3 this session (`afterBack - rightPos` ≈ 58 px, expected < 12). Proven pre-existing: fails identically on clean `origin/main` with my changes `git stash`-ed, on byte-identical code that the **T10 cycle recorded as drag-dock 11/11 green**. Signature = a geometry/rAF-timing assertion racing driver.js's synchronous re-position under headless load = a flaky test, not a code regression, not caused by this diff (it touches no dock geometry). Not in the pre-commit gate → does not block. **Recommended follow-up:** a small "improve the loop's tests" cycle to await the dock-restick rAF settle (or `expect.poll` / widen the 12 px tolerance).

## Invariants
No `STORE_VERSION` / schema / free-path / `instruct.js` / Malay-content touch. Theme-safe (no color/layout changes). Surgical diff.

## Deploy
Committed to `main` (= prod deploy). Step-1 time check: no finite CUTOFF set → continue (forever mode); KL 202606221059.

## ▶ NEXT
Phase 3c **T11 part 2 — the Practice hub (`/practice`)**: mirror this shape (`pageGuides.js` entry + `/practice` in `PAGE_GUIDE_ROUTES` + `data-guide="practice-…"` anchors on the grouped tile grid + `pageGuides.test.js` + a `guide-*` e2e). Completes T11; then T12 Roleplay, T13 Grammar … one page per commit. A fresh axis-1/axis-2 gap still preempts if one clears the bar.
