# Local build-loop cycle report — 2026-06-22 03:56 UTC

## Item shipped
**Directed epic axis-3 (Full Page Guide, plan Phase 3c → T15): the Comprehension picker deep-dive guide.**
The header ▶ "Tour this page" now works on `/comprehension`.

## Why this item (assessment)
- Queue (`RESUME_HERE.md` 🤖 Autonomous build queue) was **all `[x]`** — 0 unchecked items.
- `GOAL.md` directed epic = the Full Page Guide rollout; the explicit `▶ NEXT` thread from the just-shipped
  T14 (`/writing`) is **Phase 3c T15 — Comprehension (`/comprehension`)**, the next page in the plan's
  priority order.
- No fresh axis-1 (correctness/content) or axis-2 (pedagogy) gap cleared the bar — the converged content
  ledger is trusted per prior cycles (no new web-verified wrong content surfaced; the change authors NO new
  Malay). So the top eligible gap = the next directed-epic task = T15.

## What shipped
- `/comprehension` entry in `src/lib/guide/pageGuides.js` — 4 steps: centered intro + 2 anchored
  (`comprehension-passages`, `comprehension-badges`) + a centered "Inside a passage" summary (no selector).
- `/comprehension` added to the eager `PAGE_GUIDE_ROUTES` seam (lights the header ▶).
- 2 new `data-guide="comprehension-…"` anchors in `Comprehension.jsx` on the FIRST passage card + its badge
  row, both via an `idx === 0` guard in the existing `.map`.
- README "Tour this page" line + the rollout plan doc updated (T14 + T15 expanded to ✅ lines).

## Key decisions (decide-and-flag)
- **Picker landing state is the guide target.** Comprehension is a normal (non-theater) page (no
  `theaterMode` call) → the normal header ▶ is the entry, like `/roleplay`/`/grammar`/`/practice`.
- **Reading-screen mechanics taught in a centered summary step, NOT arrows.** The passage text,
  tap-to-look-up, Read-along TTS, the MCQ + instant explanation, and the score only mount after a passage
  opens (`setPassage`); a picker-anchored arrow at them would hit a missing node and hang-then-skip 3 s
  (`guideController.js:101`). The centered `arrow:'none'` summary never misses. *Veto note:* a separate
  in-reading deep dive keyed to those controls can be added later with zero rework.
- **Omitted the "Get fresh AI questions" button** — gated on `isGeminiAvailable()` (BYOK Gemini key), so
  teaching it would confuse the majority who never see it.

## Red → green evidence (TDD)
- RED (before implementing): `npx vitest run pageGuides.test.js` → 2 failed —
  `Array.isArray(steps)` got `false` (PAGE_GUIDES['/comprehension'] undefined) and
  `steps.map` threw `Cannot read properties of undefined (reading 'map')`.
- GREEN (after implementing): **23 passed (23)** in `pageGuides.test.js` (incl. the `PAGE_GUIDE_ROUTES`
  sync test, which only passes because `/comprehension` was added to both lists).

## Gate result
- `npm run build` — exit 0 (✓ built in 4.26s).
- `npm run test:run` — **1875 passed (1875)** (+2 vs 1873).
- `npm run lint` — 0 errors, 3 known pre-existing warnings (Comprehension.jsx:47, Roleplay.jsx:30,
  RoleplayScorecard.jsx) — none new.
- e2e (UI-affecting → ran locally): `guide-comprehension` 2/2 + `guide-full-page` 4/4 = **6/6**.

## Perf (axis-4)
- `Comprehension` page chunk 14.02 kB / 4.25 kB gz (well under the 70 KB per-route budget — 2 inert
  attributes + one `idx` map param).
- `pageGuides` lazy on-demand chunk 19.88 kB / 7.21 kB gz (loaded only when ▶ tapped — exempt).
- eager `index` 479.39 kB / 153.30 kB gz (the `/comprehension` seam string ≈ 12 bytes).

## Invariants
No `STORE_VERSION` / Supabase-schema / free-path / `instruct.js` touch. No new Malay authored. No feature
deleted. No secrets.

## Deploy
Committed to `main` (= prod deploy). Confirm Vercel READY on the `upg-` project.

## Step-1 time check
Forever loop (no finite `CUTOFF`); KL local at cycle start `202606221146` — well before any backstop.

## ▶ NEXT
Phase 3c **T16 — Listening (`/listening`)**: mirror this entry's shape in `pageGuides.js`, add
`/listening` to `PAGE_GUIDE_ROUTES`, add `data-guide="listening-…"` anchors to the live controls (the
clip/passage picker that leads with `studyLang`, the audio-play controls, the question/answer flow), pin
with `pageGuides.test.js` + a `guide-*`-style e2e. Then T17 Speaking, T18 Import, … one page per commit.
A fresh axis-1/axis-2 gap still preempts if one clears the bar.
