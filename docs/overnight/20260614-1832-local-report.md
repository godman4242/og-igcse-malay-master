# Local build-loop report — 2026-06-14 18:32 UTC (KL 2026-06-15 02:32)

**Cycle outcome:** SHIPPED one axis-1 content-truth fix (self-sourced; queue was empty).

## Item
Cikgu Maya `imbuhan-an` entry filed three `peR-...-an` words under a "Combined with **peN-**" header.

- **GOAL axis:** 1 (Correctness & content truth) — a confident-WRONG grammar lesson rendered verbatim to the student.
- **Source:** GOAL-driven Self-source mode (§3A–3D). The pre-thought `▶ NEXT` of the imbuhan-pen ship said to audit
  the remaining unaudited Cikgu imbuhan/tense entries.

## Assess (grounded)
Audited the still-unaudited suffix/tense Cikgu entries: `imbuhan-kan`, `imbuhan-i`, `imbuhan-ke-an`, `imbuhan-se`,
`tense-markers`. All correct **except**:
- `imbuhan-an` (`cikguKnowledge.js:297–300`): header "Combined with peN-" listed `perjalanan`/`permainan`/
  `pertandingan`, which are actually `per-...-an` nouns of ber- verbs (berjalan/bermain/bertanding) — **picked**.
- `imbuhan-se` (`:370`): `sekolah` listed as an se- word, but it's a Portuguese loanword (*escola*) — **flagged for
  next cycle** (kept this cycle to ONE item; the pre-commit `git add -A` ships the whole tree).

## Web-verification (not memory)
- `perjalanan`/`permainan`/`pertandingan` = **per-...-an** circumfix (noun of a ber- verb), NOT peN-...-an —
  Malay tatabahasa references (BM Tatabahasa imbuhan pe-; per-...-an konfiks). Corroborated internally by
  `grammar.js`'s `GRAMMAR_RULES['ber-']`.

## Decision log
- **Decision:** regroup — keep `pendidikan`/`penerbangan`/`pembelajaran` under peN-, add a correctly-labelled
  `Combined with peR- (per-...-an)` section for the three per- words (with their ber- roots + a "don't confuse with
  peN-" note). **Why:** more surgical than deleting words AND better pedagogy (teaches the very distinction that
  caused the bug). **Veto note:** rejected swapping the per- words out for more peN- words (hides useful IGCSE words,
  teaches nothing about peR-); deferred the separate `sekolah` false-etymology to the next cycle.
- **Pick over alternatives:** `imbuhan-an` peN-/peR- chosen over the `sekolah` false-affix because it's the bigger
  axis-1 gap — an explicit false affix header on 3 exam-tested words vs a single false-etymology parenthetical.

## Red → green evidence (TDD)
- New `src/data/__tests__/cikguKnowledge.test.js` block (+4). Against **pre-fix** data: **3 failed / 1 passed** —
  no peR- section; per- words under the peN- header; peR- block empty; the peN-words-present check passed
  (non-vacuity). After the fix: **30/30 green** in the file (incl. the 10 confidence-gate calibration tests, proving
  scoring is unaffected).

## Gate
- `npm run build` — green (data-only; `index` chunk unchanged).
- `npm run test:run` — **1604 passed** (+4 vs 1600).
- `npm run lint` — **0 errors** (3 known pre-existing exhaustive-deps warnings).
- e2e skipped by design (string edit in existing answer data — no layout/flow change; CI runs e2e on push).

## Invariants
No STORE_VERSION bump · no Supabase-schema/free-path break · `instruct.js` API untouched · no feature deleted (all
six words retained, correctly classified) · no secrets · content web-verified.

## Deploy
One commit to `main` (= prod deploy). `RESUME_HERE.md` updated in the same commit (queue item `[x]` + shipped-✅
section). Vercel READY to confirm post-push.

## Step-1 time check
KL `202606150224` at cycle start; `CUTOFF` far-future (forever) → continue.
