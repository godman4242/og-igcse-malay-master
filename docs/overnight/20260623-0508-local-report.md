# Local build-loop cycle — 2026-06-23 05:08 UTC

## Item shipped
**Directed epic axis-3 (Full Page Guide, Phase 3c → T21): the `/for-you` deep dive.** The header
▶ "Tour this page" now works on the For You home — a 6-card, entirely-centered Full Page Guide.

## Why this item (assessment against GOAL.md axes)
- Queue (`RESUME_HERE.md` 🤖 build queue) was fully `[x]`; T20's entry named **▶ NEXT (T21): the For
  You deep dive (`/for-you`)**, and GOAL.md's **Current DIRECTED epic** (Phase 3c page-tour deep dives,
  one page per commit) is still active.
- Precedence screen: no fresh **axis-1 (correctness/content-truth)** or **axis-2 (pedagogy)** gap cleared
  the bar this cycle — the recent cycles (T19, T20 + four axis-1 paper-number fixes) swept the content
  ledger to convergence, and the loop's own rule favours continuing the in-flight directed increment over
  re-surveying every cycle. So the top axis-3 gap = the next unchecked task in the rollout plan = **T21**.

## Design decision (flagged) + veto note
For You has TWO mutually-exclusive render states with NO shared control — a `GetStarted` empty card
(`ForYou.jsx:109` `visible.length === 0`, where a brand-new guide-explorer lands) vs the populated
shelves. **Mirrors T19 (`/mistakes`):** an anchored step would skip-then-hang on the empty state
(GOAL-backlog-#5 / Bug-A class), so the guide is ENTIRELY `arrow:'none'` centered cards — renders
identically in both states, ZERO JSX anchors, `ForYou.jsx` untouched (lowest regression risk).
*Veto note:* arrows-on-shelves would force gating the header ▶ on a populated state + adding anchors —
but that hides the guide from new users (worse for ADD-first onboarding).

## Content grounding (no confident-wrong)
Every card grounded line-by-line against the live source:
- `ForYou.jsx` + `forYouShelves.js` — the 5 shelves (`SHELF_ICON`), the `tasks`/`session`/`cards`/`link`
  kinds, the `Rail` swipe wrapper, the isProbe "Show meaning" reveal + the "this probe NEVER touches FSRS"
  code comment, `Start session`→`/smart-study`, `Practise saved words`→`/saved-cloze`, `buildGoal` driven
  by `identity.goalPreset`/`idealSelf`.
- `Settings.jsx:646,668-673` — confirmed the goal preset + ideal-self ("Goal saved!") ARE set in Settings,
  so "the goal you set in Settings" is accurate.
- `MakeDeckPanel.jsx` — `deckAiAvailable` gate + the no-key "Add an AI key in Settings (OpenRouter, Gemini
  or Ollama)" prompt.
- `GetStarted` — "Your home fills up as you learn" + `/word-families` + `/import` quick-starts.
No new Malay authored (English UI copy only). `/for-you` already in `APP_ROUTES` + `FULL_TOUR` (no route
reconcile needed).

## TDD red → green
- Added 2 unit tests to `pageGuides.test.js` (mirror the T19 `/mistakes` block): "exists with a centered
  intro + several steps" + "is ENTIRELY centered cards (no anchor)".
- **RED first:** `PAGE_GUIDES['/for-you']` undefined → `steps` not iterable → both new tests fail for the
  right reason; the existing 34 (incl. the `PAGE_GUIDE_ROUTES`-sync pin) pass.
- **GREEN after** adding the `/for-you` entry to `pageGuides.js` + `/for-you` to `pageGuideRoutes.js`:
  36/36 in the file.

## Files changed
- `src/lib/guide/pageGuides.js` — new `/for-you` entry (6 centered cards).
- `src/lib/guide/pageGuideRoutes.js` — `/for-you` added to the eager `PAGE_GUIDE_ROUTES` seam.
- `src/lib/guide/__tests__/pageGuides.test.js` — +2 red-proofed unit tests.
- `tests/e2e/guide-for-you.spec.js` — NEW (2 tests, state-agnostic).
- `README.md` — page list + For You walk-through summary (ship-contract).
- `docs/superpowers/plans/2026-06-21-guide-full-page-rollout-plan.md` — T21 marked ✅.
- `RESUME_HERE.md` — shipped `[x]` queue entry (this commit).

## Gate
- `npm run build` — exit 0 (134 precache entries; `pageGuides` lazy chunk ~34.1 kB — on-demand → exempt
  from the 70 KB per-route rule; eager index seam +1 string only).
- `npm run test:run` — **1910 unit tests pass** (183 files, +2 new, 0 fail).
- `npm run lint` — 0 errors, 3 known exhaustive-deps warnings (RoleplayScorecard, Comprehension, Roleplay).
- e2e (UI-affecting) — `guide-for-you` 2/2 + `guide-mistakes` 2/2 + `guide-full-page` 4/4 = **8/8 green**.

## Self-review (hostile pass)
- Malay regression? None — no Malay authored.
- Cross-language class leak / broken study mode? None — content-only; `ForYou.jsx` untouched.
- Dark/light theme? No CSS/color/class change → identical.
- Unverified claim? The only location claim ("goal you set in Settings") was verified against
  `Settings.jsx`. The "chips only once weak spots are known" caveat matches the `buildPicked` subtitle
  fallback. No confident-wrong.
- HARD invariants: no `STORE_VERSION`/schema/free-path/`instruct.js` touch; no feature deleted; no secrets.

## Step-1 time check
No finite `CUTOFF` set (forever loop) → no cutoff gate. Proceeded normally.

## Deploy
Committed to `main` (= prod deploy). One commit for the one item, queue checked `[x]` + shipped section in
the same commit.

## ▶ NEXT
T22 — the Word Families deep dive (`/word-families`). A fresh axis-1/axis-2 gap still preempts if one
clears the bar.
