# Local build-loop cycle report — 2026-06-22 03:09 UTC (11:09 KL)

## Item shipped
**Directed epic axis-3 — Full Page Guide, Phase 3c T11 part 2: the Practice hub (`/practice`) deep dive → T11 COMPLETE.**
The header ▶ "Tour this page" now works on `/practice`, walking a student through the grouped-by-exam-skill
layout, that each tile is a one-tap launcher, and the live status cues ("due"/"to fix"/"% ready"/"saved").

## Why this item (assessment against GOAL.md axes)
- Queue in `RESUME_HERE.md` was all `[x]` (75 checked, 0 unchecked) → GOAL-driven Self-source mode.
- No fresh axis-1 (correctness/content) or axis-2 (pedagogy) gap cleared the bar — the content ledger is
  converged per prior cycles (trusted, not re-audited from scratch).
- The active **DIRECTED epic** (GOAL.md) is the Full Page Guide Phase 3c. The explicit `▶ NEXT` thread from
  the T11-part-1 (`/smart-study`) cycle, and the next item in the plan's priority order, is `/practice` —
  the open part 2 of T11. So this is the deterministic next task, not an invented one.

## Red → green evidence
- TDD: added 2 unit tests to `src/lib/guide/__tests__/pageGuides.test.js` (`/practice deep dive` block).
- RED proof (before implementing): `npx vitest run …/pageGuides.test.js` →
  `2 failed | 12 passed` — `Array.isArray(steps)` false and `steps.map` threw `Cannot read properties of
  undefined (reading 'map')` because `PAGE_GUIDES['/practice']` was undefined. Correct failure reason.
- GREEN after implementing: `14 passed (14)`.

## What was built (surgical diffs)
- `src/lib/guide/pageGuides.js` — `/practice` entry: 1 centered intro + 3 anchored steps
  (`practice-groups` / `practice-tile` / `practice-cue`). LAZY chunk (loaded only when ▶ tapped).
- `src/lib/guide/pageGuideRoutes.js` — added `/practice` to the eager `PAGE_GUIDE_ROUTES` seam (~12 bytes;
  lights the header ▶).
- `src/pages/Practice.jsx` — 3 inert `data-guide` anchors: `practice-groups` on the first `<section>`,
  `practice-tile` on the Study tile, `practice-cue` on the Mistakes tile. All on always-mounted elements;
  `data-guide={… : undefined}` renders nothing on non-target nodes (byte-identical behaviour elsewhere).
- `tests/e2e/guide-practice.spec.js` — new 2-test spec (mirror of `guide-smart-study.spec.js`).
- `README.md`, `RESUME_HERE.md`, plan doc — ship-contract + handoff updates.

## Decision log
- **Anchor the TILES, not the status badge** (which is conditional on a count > 0). An arrow at a missing
  badge hangs-then-skips (`guideController.js:101`); the cue concept is taught in the step body with all four
  examples instead. Veto: if a dedicated cue arrow is wanted later, add an always-rendered placeholder + step.
- **No floating ▶ pill** (unlike `/study`): `/practice` is a non-theater page, so the normal header ▶ is the
  entry — same as `/smart-study`. Veto: none; verified Practice uses the standard `Layout` header.
- **3 anchored steps is the floor** for a simple index page — fewer skips a real mechanic, more is padding.

## Content verification (axis-1, no confident-wrong)
No new Malay authored — the hub guide is English UI copy. Every claim grounded against
`src/lib/practiceSurfaces.js` + `Practice.jsx` `statusText`: the six group names, the Speaking group's
members, and the exact cue strings (`N due`, `N to fix`, `N% ready`, `N saved`).

## Gate result
- `npm run build` — exit 0.
- `npm run test:run` — **1866 passed** (+2, was 1864).
- `npm run lint` — 0 errors (3 known exhaustive-deps warnings).
- e2e (UI-affecting → ran locally): `guide-practice` 2/2; full guide suite **24/24** green.
- Perf: `pageGuides` lazy chunk 11.04 kB; eager `index` 479.34 kB / 153.28 kB gz; `Practice` page chunk
  4.83 kB (≪ 70 kB budget).

## Invariants screen
No `STORE_VERSION` bump · no Supabase-schema change · no free-path break · `instruct.js` API untouched ·
no secrets · no feature deleted · no paywall. Clean.

## Deploy
Pushed to `main` (= prod deploy). Confirm Vercel READY on `upg-igcse-malay-master`.

## Time-check (step 1)
No finite `CUTOFF` set (single-cycle invocation) — loop is forever; this is one cycle, then exit.
