# Local build-loop cycle report — 2026-06-21 07:02 UTC (15:02 KL)

**Commit:** `9a38089` — `feat(guide): pause HIDES the chrome — box tucks away + Resume pill; minimized keeps the icon strip (Phase 3b★ Tpause★)`
**Deploy:** pushed to `origin/main` (auto-push) → prod (Vercel upg- + og-).

## Item
Directed epic axis-3 — Full Page Guide, plan Phase 3b★ **Tpause★** (the next unchecked top-down task;
queue empty, content ledger converged → no fresh axis-1/axis-2 gap cleared the bar, clamp lifted → next plan task).

**What shipped:** Pause now HIDES the guide chrome, split by minimized state:
- Paused & un-minimized → whole popover box + page-guide arrows hide; eager `GuideHud` shows ONE floating
  "▶ Resume tour" pill (the only way back; box's own Resume is hidden). Page un-dimmed + interactive.
- Paused & minimized → only the step explanation hides; the docked icon strip + N/M jumper stay on the margin
  (its own ⏸→▶ covers recovery — no pill).

## Design / decision log
- **New `guide-paused` root class** (separate from Tdim★'s `guide-explore` dim switch, which is ALSO on for a
  docked-not-paused box) = the CSS chrome-hide switch. `syncPausedClass()` toggled by `pause/resume` + re-synced
  every `onPopoverRender` (a step change while paused-docked keeps the explanation hidden).
- **Stranding guard (axis-1):** un-docked pause hides the box's own Resume, so the controller publishes `paused`
  to in-memory `guideState`; the eager HUD renders the lone pill which lazy-imports the already-loaded controller
  chunk and calls a new `resumeActiveTour()` seam (zero eager-bundle cost; controller stays lazy).
- **Veto note:** a dedicated `guide-paused` class is required — the dim axis and the chrome axis diverge
  (docked-not-paused must stay un-hidden), so one class can't encode both. FAB only un-docked (docked strip already
  has a Resume button → second control redundant).
- Page-guide arrows: `emitPointer()` early-returns `pointer:null` in explore mode → arrows hide whenever paused.

## Files (4 src + 5 test/doc)
`guideState.js` (`paused:false`), `guideController.js` (`resumeActiveTour`/`syncPausedClass`/pause+resume publish/
emitPointer guard/teardown clear/render re-sync), `GuideHud.jsx` (pill), `index.css` (2 hide rules + `.guide-resume-fab`);
`guideState.test.js` + `guideController.test.js` + `GuideHud.test.js` (+3 unit); `guide-pause-skip.spec.js` (updated) +
`guide-drag-dock.spec.js` (+1 e2e); README + plan + RESUME.

## Red → green evidence
- 6 assertions RED first for the right reasons: `resumeActiveTour is not a function`; `expected undefined to be false`
  (guideState.paused absent); `expected null to be truthy` (no `.guide-resume-fab`); INITIAL-shape mismatches.
- After implement: all green.

## Gate
- `npm run build` exit 0 — eager index **478.34 kB / 153.08 kB gz** (+0.29 kB raw vs the 478.05 kB Tdim★ baseline =
  the GuideHud FAB JSX + lazy-import helper; controller logic stays in the lazy guide chunk).
- `npm run test:run` — **1821 passed** (+3, was 1818).
- `npm run lint` — 0 errors (3 known exhaustive-deps warnings).
- e2e (UI-affecting) — guide suite **14/14** (drag-dock 9/9 incl. new Tpause★, full-page 4/4, pause-skip 1/1 updated),
  run against the production preview server.
- Pre-commit quality gate: build ✓ tests ✓ lint ✓ content ✓.

## Self-review (hostile pass)
No Malay/English content, study modes, store/STORE_VERSION, schema, free-path, or `instruct.js` touched (UI chrome only,
nothing to web-verify; guideState is in-memory, no migration). Theme-safe (`--color-accent`/`--color-on-bright`, FAB inside
the `.light` subtree). No stranding: both recovery paths (pill / docked strip ▶) e2e-proven. a11y: FAB aria-label + ≥44px.

## Step-1 time check
No `CUTOFF` set (forever). KL `202606211447` at cycle start → continued. Single cycle complete; stopping per the
shell-driven loop contract.

## ▶ NEXT
Phase 3b★ continues top-down: **Tslide★** (slide the minimized box ALONG the margin — pure along-edge model in
`dragDock.js`) → **Tresize★** (PowerPoint-style resizable box, in-session, no STORE_VERSION). Then Phase 3c per-page
CONTENT, PDF reader FIRST (Kheshav spot-checks each live). A fresh axis-1/axis-2 gap still preempts if one clears the bar.
