# Local build-loop cycle report — 2026-06-14 18:55 UTC (2026-06-15 02:55 KL)

**Mode:** GOAL-driven Self-source (autonomous build queue empty — all items `[x]`).
**Axis:** 1 — Correctness & content truth (highest priority).
**Item shipped:** Content-truth fix — Cikgu Maya `lisan-paper3` + `exam-paper3` taught a **fabricated** IGCSE
Paper 3 Speaking exam format.

## 1. Time check
`TZ=Asia/Kuala_Lumpur date` = `202606150245` at cycle start. `CUTOFF` = far-future (forever loop) → continue.

## 2/3. Assess (Self-source 3A/3B)
Synced `main` (already up to date). Queue empty → assessed the app against `docs/loop/GOAL.md` axes. The recent
cycles have been axis-1 content-truth fixes in `cikguKnowledge.js`; the latest `▶ NEXT` flagged the unaudited
`lisan-paper3`/`exam-*` exam-tip entries. Audited them grounded against the official syllabus:

- `common-mistakes`, `peribahasa` bank (spot-checked several proverb meanings), `lisan-roleplay-tips` — **clean**.
- **GAP FOUND:** `lisan-paper3` + `exam-paper3` describe Paper 3 Speaking as 3 parts — *Role play (2-3 min) /
  Topic presentation (3-4 min) / General conversation (5-6 min)*.

**Evidence (web-verified, not memory):** downloaded the official **Cambridge IGCSE Malay – Foreign Language (0546)
2025–2027 syllabus** PDF and `pdftotext`-extracted Paper 3 – Speaking (syllabus lines 756–764):
> ~10 minutes (+10 min preparation), 40 marks. **one role play** — respond to **five transactional questions**
> (~2 min); **two topic conversations** — share views/opinions/experiences (4 min per conversation).

So there is **no "topic presentation"** and **no separate "general conversation"** — the app invented a structure
that mis-prepares students for the real test. Highest-priority (axis-1) confident-wrong content. Passed the
anti-hallucination gate: **Real** (exact syllabus wording), **Measurable Done** (failing test → green), **Verified**
(official 0546 syllabus PDF).

## 4. Build (TDD)
- **RED first:** added a `+12` parametrised block (both entries) to `src/data/__tests__/cikguKnowledge.test.js`.
  Ran it → **10 failed / 2 passed** (the two `role play` non-vacuity checks passed → not vacuous).
- **Fix (surgical, data-only):** `src/data/cikguKnowledge.js` — rewrote both `Format` blocks to the real 0546
  structure; relabelled the two contradicting sub-headings in `exam-paper3` ("topic presentation"→"each topic
  conversation"; "general conversation"→"going deeper in the conversations"). All strategy advice preserved; no
  content deleted. Paper 3 = Speaking is uncontested in both app and syllabus, so the fix is self-contained.
- **GREEN:** full `cikguKnowledge.test.js` = 45/45.

## 5. Gate
- `npm run build` — green (PWA generated; `index` unchanged — data-only).
- `npm run test:run` — **1619 passed** (+12).
- `npm run lint` — **0 errors** (3 pre-existing exhaustive-deps warnings only).
- e2e skipped by design (string edits in existing answer data → no layout/flow change; CI runs e2e on push).

## 6. Self-review (hostile)
Content matches the official syllabus verbatim; no phantom components remain; advice preserved; "Prepare 3 topics …
4 minutes each" prep line now *consistent* with the corrected timing; Malay phrases intact (no MS/EN leak); markdown
balanced; scoring-neutral (`paper3-tips` gold query keys on paper/speaking/score — all kept; removed/added tokens not
in any gold question). No STORE_VERSION/schema/free-path/API change.

## 7. Ship
One commit: the fix + `cikguKnowledge.test.js` block + `RESUME_HERE.md` (queue `[x]` + shipped section) + this report.

## Decision log
- **Decision:** fix the Paper 3 Speaking FORMAT in both entries; do NOT renumber papers app-wide.
  **Why:** the speaking-format error is the single biggest, cleanest, web-verified wrong item and is self-contained
  (Paper 3 = Speaking is correct in both). **Veto note:** the broader paper-NUMBERING inversion (real 0546: Paper
  1=Listening, 2=Reading, 4=Writing vs the app's 1=Reading/2=Writing/4=Listening) + the Writing-paper task structure
  are also wrong, but renumbering is an app-wide user-facing relabel that needs Kheshav's product call — flagged in
  `▶ NEXT`, not solo-done.
- **Decision:** relabel (not delete) the two phantom-component sub-headings. **Why:** preserves transferable advice
  while removing the wrong-structure claim. **Veto note:** considered deleting the sections — rejected (would lose
  useful speaking advice; never delete content).

## ▶ NEXT
- **Bigger flagged thread (needs Kheshav):** app-wide paper-numbering inversion + Writing-paper (0546 Paper 4) task
  structure ("form-filling + directed + extended", not "1 topic, 200-300 words").
- Cleaner axis-1 threads: `peribahasa` bank totality, murkier `imbuhan-se` `semua`/`seluruh`.
