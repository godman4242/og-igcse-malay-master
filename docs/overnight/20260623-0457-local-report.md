# Local build-loop cycle — 2026-06-23 04:57 UTC (`241e13c`)

## Item shipped
**Directed epic axis-3 (Full Page Guide, Phase 3c → T20): the Exam Rehearsal deep dive.**
The header ▶ "Tour this page" now works on `/exam-rehearsal`.

## Why this item (assessment against GOAL.md axes)
- Autonomous build queue (`RESUME_HERE.md`): **empty** — no unchecked `[ ]` items → GOAL-driven Self-source mode.
- Precedence: (1) no fresh axis-1/axis-2 gap cleared the bar — the content-truth ledger converged across the prior paper-number cycles (all `[x]`); the lone red `guide-drag-dock` Tslide★ is a pre-existing pointer-drag flake already triaged to the GOAL backlog (#7, test-reliability, which sits *below* the directed epic). (2) The **Current DIRECTED epic** (Full Page Guide, Phase 3c) is therefore the pick; its next unchecked task in the rollout plan is **T20 — Exam Rehearsal** (T19 Mistakes shipped in `e61f511`).

## What shipped
- `src/lib/guide/pageGuides.js` — `/exam-rehearsal` entry: centered intro + 3 anchored steps (`exam-stages` four-skill overview · `exam-lang` Bahasa Melayu/English toggle · `exam-start` Start button) + a centered "readiness" summary.
- `src/lib/guide/pageGuideRoutes.js` — `/exam-rehearsal` added to the eager `PAGE_GUIDE_ROUTES` seam (lights the header ▶; ~14-byte eager delta).
- `src/pages/ExamRehearsal.jsx` — 3 `data-guide="exam-…"` anchors on always-mounted INTRO elements (Stages card, language toggle row, Start button). No logic change.
- `tests/e2e/guide-exam-rehearsal.spec.js` — new (2 tests).
- `README.md`, `RESUME_HERE.md`, rollout plan — updated in the same commit.

## Design decisions (decide-and-flag)
- **Header ▶ is the entry, not a floating pill:** `ExamRehearsal.jsx` never calls `useTheaterMode`, so the header stays visible on the INTRO landing (unlike `/study`, which hides it mid-session). *Veto:* if the page later enters theater mode at landing, add the floating ▶ as `/study` does.
- **Timed stages taught arrow-free:** the COMP/LISTEN/WRITE/SPEAK/RESULTS stages mount only after Start, so anchoring an arrow there would skip-then-hang on the landing state. They're taught in a centered `arrow:'none'` summary (renders in any state), mirroring picker pages T15–T17. *Veto:* arrows-on-stages would force the guide to drive the rehearsal forward (complex + burns a real attempt) — the centered summary is the calmer ADD-first choice.

## Content verification (anti-hallucination gate)
Every claim grounded line-by-line against `ExamRehearsal.jsx`: `BUDGET` 8/6/12/10-min stages; `hasSpeechSynthesis()`-gated listening; `StageHeader` "(over)" soft timer; `examRehearsalLang` MS/EN toggle (labels "Bahasa Melayu"/"English"); `composeReadiness` + RESULTS (% for comprehension/listening, band /6 for writing/speaking, composite "Exam Readiness %"); `getNextExamDue` ("next due in N days"); the listening re-normalisation when TTS is absent. No new Malay authored — only the verified Cambridge syllabus codes 0546 (Malay – Foreign Language) / 0500 (English – First Language) / 0510 (English – Second Language). No confident-wrong; no paper numbers on this bilingual surface (per the 2026-06-22 label-by-skill decision).

## TDD red→green evidence
- RED: added the `/exam-rehearsal` describe block to `pageGuides.test.js`, ran it → 2 failed for the right reason (`PAGE_GUIDES['/exam-rehearsal']` undefined → `Array.isArray` false, `steps.map` throws). 32 passed.
- GREEN after implementing: `pageGuides.test.js` 34/34.
- e2e `guide-exam-rehearsal.spec.js` 2/2 (header-▶ launch + first arrow draws; every anchor exists, count 1).

## Gate result (pre-commit, full suite)
- build: **0 errors** (PWA generated; eager `index` 479.46 kB / 153.33 kB gz — accumulated page-guide-rollout figure, this commit's eager delta ≈14 bytes).
- tests: **1908 passed** (183 files, +2 new).
- lint: **0 errors** (3 known exhaustive-deps warnings only).
- content-lint: pass.
- UI-affecting e2e run locally: `guide-exam-rehearsal` 2/2.

## Self-review (hostile pass)
No Malay regression (no Malay authored). No cross-language class leak (inert `data-guide` attributes only). Study flow untouched (3 attributes added to existing elements). Dark/light identical (no color/style change). No anchors on the conditional readiness/attempts cards (no missing-anchor skip). All factual claims re-checked against the component.

## Deploy
Committed `241e13c` → auto-pushed to `origin/main` (verified `origin/main == HEAD`) → Vercel prod deploy from `main`.

## Time check (step 1)
KL local `202606231257` — no finite `CUTOFF` set (forever loop). Continue.

## ▶ NEXT
Plan Phase 3c **T21 — For You** (`/for-you`) is the next page in the rollout priority order.
