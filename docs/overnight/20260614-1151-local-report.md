# Local build-loop cycle report — 2026-06-14 11:51 UTC (19:51 KL) — commit `992718d`

**One cycle, one item. Test-only coverage, no app behaviour change.**

## Item
Queue item #1 (RESUME_HERE.md): **Pure-lib test coverage (`interleave`)** — red-proofed unit tests for
`src/lib/interleave.js` (`getMixedSessionSummary` + `buildMixedSession` ratio/target math). Self-sourced
2026-06-14, vetted safe-to-solo. The repeatable behaviour-preserving pattern from the shipped `diff.js` pin.

## What shipped
- **`src/lib/__tests__/interleave.test.js` (+15)** — the module had no dedicated test file before
  (`interleaveByPrefix.test.js` / `interleavedQueue.test.js` cover different `src/lib/study/` modules).
  - `buildMixedSession` (9): default 8/5/2 (total 15); custom ratios 6/2/2; comp floor=1 (total 11 >
    sessionSize); pool-limited vocab; `ex.length > 15` comp filter; not-due exclusion; grammar `due`-filter;
    gTarget cap (119 drills → 5); type-tag invariant. Counts/targets only — never shuffle order.
  - `getMixedSessionSummary` (6, pure): empty; all-correct (no weakest, `worstAcc < 100` guard); accuracy
    rounding (1/3 → 33); `byType` accumulation + lowest-accuracy weakest; tie → first-seen (strict `<`);
    zero-correct weakest.
- **`src/lib/interleave.js` byte-identical** (`git diff --stat` empty after the red-proof restore).

## Red→green evidence
- Initial run of the new file: **15/15 green** (expectations grounded against the function's real output +
  the live 119-drill pool count, not guessed).
- **Non-vacuity red-proof:** mutated three SUT behaviours at once — `vTarget` `Math.round`→`Math.floor`,
  comp filter `> 15`→`> 1`, accuracy `Math.round` removed → **5 targeted tests failed** for the right
  reasons (default-counts, comp-filter, not-due-count, grammar-count, accuracy-rounding); the other 10
  (which don't touch those paths) stayed green. Restored `interleave.js` byte-identical → **15/15 green**.

## Decision log
- **Hand-calculated literal expectations, not formula-derived** — so a rounding/clamp regression actually
  fails (a test that re-runs the SUT's own formula would be vacuous). *Veto: re-deriving counts in the test
  defeats the purpose of pinning them.*
- **Imported the live drill arrays** (`IMBUHAN/TENSE/ERROR/TRANSFORM_DRILLS`) for fixture construction +
  the gTarget-cap pool size, guarded by an `> 5` assertion — robust if the drill data ever shrinks.
- **Asserted counts/types, never shuffled order** — `buildMixedSession` uses `Math.random`; order is
  non-deterministic, counts/targets are not. Matches the queue item's explicit scope.

## Gate result
- `npm run build` — green; eager `index` chunk unchanged (test-only).
- `npm run test:run` — **1441 passed** (+15; was 1426).
- `npm run lint` — **0 errors** (same 3 pre-existing exhaustive-deps warnings).
- Pre-commit quality gate (build → tests → lint → content) — ✅ all green.
- e2e skipped — pure data/logic change, no rendered UI/flow (per LOCAL_BUILD_LOOP step 5).

## Deploy
- Committed + auto-pushed to `main` (`HEAD == origin/main`, `992718d`) = prod deploy triggered. Test-only,
  no bundle change → low-risk deploy.
- **No STORE_VERSION bump** (stays 34); no Supabase-schema / free-path / frozen-API change; no feature
  removed.

## Known nit (flagged, not fixed)
- The commit **subject** reads `… (v35)` — a misleading tag; there is **no** `STORE_VERSION` bump (the body
  and code correctly say so; `STORE_VERSION` stays 34). Left as-is: the commit is already on `origin/main`
  (prod), so fixing a cosmetic subject would require a force-push to the production branch — riskier than
  the tag. No code impact.

## Time check
KL `202606141951` < cutoff `202606142300` — well within the window. Per instructions: exactly one cycle,
no loop, no wakeup scheduled.

## ▶ NEXT (repeatable)
~18 untested pure `src/lib/` helpers remain — next strongest targets: `pronunciation` (scoring), `feedback`,
`patterns`. Re-add a `[ ] Pure-lib test coverage` queue item to continue the pattern.
