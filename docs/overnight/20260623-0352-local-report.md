# Local build-loop cycle report — 2026-06-23 03:52 UTC (11:52 KL)

## Item shipped
**AXIS-1 content-truth (comments-only) — `dictionary.js` Malay vocab section comments used the old wrong writing paper number.** First unchecked `[ ]` item at the top of the `RESUME_HERE.md` 🤖 queue (finish-in-flight-work-first), and axis-1 content-truth, so it preempts the directed Full-Page-Guide epic per `GOAL.md`.

## Evidence (the gap was real)
- `src/data/dictionary.js:830` — `// Formal Register (Paper 2 essay)`
- `src/data/dictionary.js:854` — `// Abstract Nouns (Paper 2 & 3)`
- Malay 0546 **Paper 2 = Reading**, so tying productive (essay/speaking) vocab to "Paper 2" is the same confident-wrong paper-numbering scheme the recent commits have been clearing.

## Content verification (anti-hallucination gate — PRIMARY source)
Fetched the official **Cambridge IGCSE Malay – Foreign Language 0546 (2025–27) syllabus PDF**
(`cambridgeinternational.org/Images/664637-2025-2027-syllabus.pdf`), p.9 **Assessment overview**:

| Paper | Skill | Detail |
|---|---|---|
| Paper 1 | Listening | ~50 min, 25%, 40 marks, externally assessed |
| Paper 2 | **Reading** | 1 hr, 25%, 45 marks, externally assessed |
| Paper 3 | **Speaking** | ~10 min, 25%, 40 marks, internally assessed + externally moderated |
| Paper 4 | **Writing** | 1 hr, 25%, 45 marks — "one form-filling task, one directed writing task and one task in the format of an email/letter or article/blog" |

⚠️ **Caught a hallucination:** a WebSearch AI-summary swapped Paper 3/4 (claimed "Paper 4 = Speaking"). Discarded in favour of the primary syllabus doc. So essay/formal-register = Writing = **Paper 4**; abstract nouns serve speaking + writing = **Paper 3 & 4**.

## Decision log
- **Decision:** correct `:830` → "(Paper 4 essay)", `:854` → "(Paper 3 & 4)"; add the source-scan guard to the existing `examPaperLabels.test.js` rather than a new file (it already holds the web-verified 0546 paper facts + source-scan helpers — DRY).
  **Why:** matches the queue Done exactly; co-locates with the related paper-label guards.
  **Veto note:** a separate `dictionary.test.js` block would have worked but duplicated the helper + facts.
- **Decision:** the general guard bans **all** `/Paper\s*2/i` in `dictionary.js` (not just essay-tied).
  **Why:** Paper 2 = Reading is a comprehension paper, not a productive-vocab category — there is no legitimate "Paper 2 vocab" tag in this Malay vocab list, so any future one is the same bug class; the guard forces a deliberate check.
  **Veto note:** if a real Reading-vocab category is ever added it can adjust the guard intentionally.

## TDD red→green evidence
- Wrote 3 tests FIRST, ran them RED: all 3 failed for the right reason — the offenders scan printed exactly:
  `["  // Formal Register (Paper 2 essay)", "  // Abstract Nouns (Paper 2 & 3)"]`, and the two pin assertions couldn't find the corrected strings.
- Applied the 2 comment edits → `examPaperLabels.test.js` **18/18 green**.

## Gate result
- `npm run build` — exit 0 (PWA generated, 134 precache entries).
- `npm run test:run` — **1903 passed** (+3, was 1900), 183 files, 0 fail.
- `npm run lint` — 0 errors, 3 known exhaustive-deps warnings (Comprehension, Roleplay, RoleplayScorecard).
- e2e: skipped — comments-only + pure-test change, no UI render (per loop step 5; CI covers on push).

## Self-review (hostile)
No gloss/entry changed (comments only) · no Malay regression · no cross-language leak · no theme/layout change · no STORE_VERSION/schema/free-path/`instruct.js` touch · content verified vs primary source · test red-proofed. Not a user-facing feature → README/guide ship-contract N/A.

## Deploy
One commit to `main` (= prod deploy). Queue item checked `[x]` + shipped note added to `RESUME_HERE.md` in the same commit.

## Step-1 time check
KL local at cycle start = `202606231146` — no finite `CUTOFF` set (forever mode), so continue/ship.

## ▶ NEXT
Two page-guide robustness items remain in the queue, both AXIS-3 and UI-affecting (need an e2e):
1. `/pdf-reader` page-guide evaporates on an empty reader (line 26).
2. `/writing` page-guide first anchored step skips for returning users with persisted text/results (line 27).
A fresh axis-1/axis-2 gap still preempts if one clears the bar.
