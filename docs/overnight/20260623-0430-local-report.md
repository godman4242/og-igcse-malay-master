# Local build-loop report — 2026-06-23 04:30 UTC

## Item shipped
**AXIS-3 — `/writing` page-guide's first anchored step is missing for returning users** (the sole
unchecked `[ ]` in the `RESUME_HERE.md` 🤖 Autonomous build queue; finishing in-flight work first per
the loop contract).

## Gap (evidence)
- `src/pages/Writing.jsx:108` — `showSampleCta = lang !== 'templates' && !text && !results`; the
  `data-guide="writing-sample"` CTA button (`Writing.jsx:123`) renders only when that is true.
- `src/lib/guide/pageGuides.js` — `PAGE_GUIDES['/writing']` step 1 was anchored to
  `[data-guide="writing-sample"]`.
- `src/lib/guide/guideController.js:113` — a step whose selector resolves to no node is silently
  skipped (`if (!node) { i += dir; continue }`).
- Net: a returning user with a draft/results launches the ▶ tour → the sample step's anchor is absent →
  the step silently skips. The existing unit + e2e cleared `localStorage` so only the first-visit
  (empty-composer) path was tested.

## Decision / why / veto
- **Decision:** convert the sample step to a centered `arrow:'none'` card (no selector), reworded to read
  true whether or not the CTA is visible.
- **Why:** established precedent — `/pdf-reader`, `/comprehension`, `/listening` all teach
  state-dependent controls as centered cards so the tour skips nothing in any state. Fully satisfies the
  Done bar ("first step resolves whether or not the composer has content").
- **Veto note:** keeping an arrow-at-the-button for brand-new users specifically would need a
  stateful/function-form page guide — a bigger change to the static guide config, not worth it for one
  step.
- The `data-guide="writing-sample"` attribute stays on the button (empty-landing CTA + a future arrow
  upgrade), mirroring how the `/pdf-reader` fix kept its attributes.

## Red → green evidence
- **Unit (red-proof):** added `pageGuides.test.js` › `/writing` › "teaches the sample CTA as a centered
  card so a draft-in-progress never skips it" → ran first: `FAIL` — `expected '[data-guide="writing-sample"]'
  to be undefined` (sample was anchored). After the `pageGuides.js` fix → `30 passed`.
- **E2E (Done-criteria, red-proof):** added `guide-writing.spec.js` › "the sample step shows even with a
  draft in the composer (no skip)". Confirmed RED by temporarily re-anchoring the step (selector restored):
  the step skipped and the `toContainText(/Try a sample/i)` assertion failed at line 123. Reverted to the
  centered card → green.
- Also updated the existing "first arrow draws" e2e: after the fix the first post-intro step is the
  centered sample card (no arrow), and the FIRST ARROW now draws on the always-present language toggle.

## Gate result
- `npm run build` — 0 errors.
- `npm run test:run` — **1904 passed** (183 files).
- `npm run lint` — 0 errors (3 pre-existing exhaustive-deps warnings only).
- `npx playwright test guide-writing` — **3 passed** (UI-affecting change → ran the relevant e2e against
  the production preview build).

## Files changed
- `src/lib/guide/pageGuides.js` — sample step → centered `arrow:'none'` card + explanatory comment.
- `src/lib/guide/__tests__/pageGuides.test.js` — `/writing` deep-dive block: dropped `writing-sample`
  from the anchored list; new test pinning the sample is a centered card + first anchored step is
  `writing-lang`.
- `tests/e2e/guide-writing.spec.js` — updated first-arrow test to the new structure; added the
  no-skip-with-a-draft test; refreshed the header comment.
- `RESUME_HERE.md` — queue item `[x]` + shipped note.

## Deploy
Committed to `main` → prod deploy (per the standing auto-push/deploy pipeline).

## ▶ NEXT (already GOAL.md backlog #5)
Every other `PAGE_GUIDE_ROUTES` page still skip-runs its loaded-state-only anchors on its empty/initial
state. A parametrized empty-state chaos e2e (mirror `tests/e2e/guide-pdf-chaos.spec.js`) would pin that
each route's ▶ tour completes with no skip/hang and never dead-ends on its empty state.

## Step-1 time-check
`CUTOFF` not set (loop runs forever) → no cutoff gate; proceeded normally.
