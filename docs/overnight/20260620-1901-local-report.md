# Local build-loop cycle — 2026-06-20 19:01 UTC (2026-06-21 03:01 KL)

## Item shipped
**Axis-1 content-truth fix:** the Malay "find the error" grammar drill `error-mencomel`
(`src/data/grammar.js:115`) carried a **doubly confident-WRONG** explanation. Self-sourced
(queue empty + Phase-3b mechanical clamp T1–T3 fully resolved → GOAL-driven axis assessment;
the ber-kerja item's `▶ NEXT` explicitly flagged the meN-/peN- allomorph + adjective surfaces
as the same bug class worth a grounded spot-check — this cycle ran that check and found a hit).

## Why it cleared the overnight clamp
GOAL.md's overnight scope-clamp NO-OPs every cycle UNLESS a fresh **axis-1 (content-truth)** or
**axis-2 (pedagogy)** gap clears the anti-hallucination bar. This is a web-verified axis-1
confident-wrong content gap — the highest-priority axis — so it overrides the clamp (Phase 4/5/3c
stay attended-only and untouched).

## The gap (evidence)
`grammar.js:115` explanation literal (single occurrence, grep-confirmed; no `feedbackRules.js`
keyed entry to sync, unlike the ber-kerja fix):
> "Comel" is an adjective and does not take the meN- prefix. "Mencomel" is not a valid word.

BOTH claims are false:
1. **"adjectives don't take meN-"** — false. Adjectives DO take meN- to mean "become X".
2. **"mencomel is not a valid word"** — false. `mencomel` is a real DBP entry (a homonym from a
   different sense of `comel`).

The drill's ANSWER (`answer: 'mencomel'` → `correction: 'comel'`) is **correct in context** —
"a cute cat" = `comel`, and `mencomel`="grumble" doesn't fit — only the taught REASON was wrong.
`error.explanation` renders to the student at `Grammar.jsx:748` (`errorFb.explanation`), so a
wrong reason mis-teaches. Same confident-wrong-REASON class as the `ber-+kerja` fix the cycle before.

## Web-verification (authority, not memory)
- `membesar` = "menjadi besar; bertambah besar", derived from adjective `besar` + meN-
  — prpm.dbp.gov.my, Kamus Pelajar Edisi Kedua → adjectives CAN take meN-.
- `mencomel` = "merungut, mengomel" (to mutter/grumble)
  — prpm.dbp.gov.my, Kamus Dewan Edisi Keempat → it IS a valid word.
- Internal corroboration: the app's own `src/data/malayValidityList.js:14393` lists `mencomel`.
- **Loanword-t check audited the SAME pass, NO gap:** `mentadbir` / `menterjemahkan`
  (`grammar.js:108,113`, both "No error") are web-confirmed CORRECT — Malaysian DBP retains the
  `t` on loanwords (tadbir→mentadbir). Left untouched.

## Decision log (decide-and-flag)
- **Decision:** rewrite ONLY the explanation string; keep `answer`/`correction`/`options`
  byte-identical (the drill is pedagogically sound in context).
  - **Why:** the answer key is right; the only defect is the taught reason. A surgical
    string change carries zero regression risk to the drill mechanics.
  - **Veto note:** a single grounded counter-example (`besar → membesar`) beats a sweeping
    false rule.
- New explanation:
  > Here "comel" simply describes the cat, so it stays bare: "kucing yang comel" (a cute cat).
  > "Mencomel" does not mean "cute" — it is a separate word meaning to mutter/grumble.
  > (Some adjectives DO take meN- to mean "become X", e.g. besar → membesar "to grow bigger",
  > but comel is not used that way.)

## TDD (red → green)
+3 unit tests in `src/data/__tests__/grammar.test.js` (new describe block, web-verified header):
1. answer key unchanged (`mencomel` → `comel`)
2. explanation drops BOTH false claims (`not /does not take the meN-/i`, `not /not a valid word/i`)
3. teaches the correct reason (cites `membesar`, keeps `comel`)

RED-proofed first: ran before the fix → 2 of 3 failed for the right reason (current explanation
contains both false literals + lacks `membesar`); test 1 trivially passed. After the fix → all green.

## Gate result
- `npm run build` — OK
- `npm run test:run` — **1778 passed** (+3; 174 files)
- `npm run lint` — 0 errors (3 known pre-existing exhaustive-deps warnings)
- Content-only data change → README + tour NOT required (no new feature); not UI-flow-affecting
  (existing `<p>` text only) → e2e NOT required (consistent with the ber-kerja fix).

## Files changed (3)
- `src/data/grammar.js` — `error-mencomel` explanation only (line 115)
- `src/data/__tests__/grammar.test.js` — +3 content-truth tests
- `RESUME_HERE.md` — shipped `[x]` queue item

## HARD-invariant screen
No answer key / `STORE_VERSION` / Supabase-schema / free-path / `instruct.js` / paywall touch.
No feature deleted. No secrets. Content web-verified.

## Step-1 time check
KL `202606210300` (≈03:01) — no finite CUTOFF set (runs forever); well within bounds.

## ▶ NEXT
Clamp resumes — Phase 3b (T1–T3) stays resolved; NO-OP every cycle unless another fresh
axis-1/axis-2 gap surfaces (Phase 4/5/3c remain attended-only). The meN-/peN-/ber- allomorph
TABLES + the menge-/loanword-t/transform-root drills are now all spot-audited clean. A future
grounded pass could check the ENGLISH grammar drills (`grammarEng.js`) for an analogous
over-stated rule, but no concrete evidence of a gap there today (don't invent it).
