# Local build-loop report — 2026-06-15 06:09 UTC

**Item:** A11y / axis-3 — `.animate-fadeUp` (the app's primary entrance animation, the translate-based
`fadeUp` keyframe used 82× across 40 files) now respects `prefers-reduced-motion: reduce`.

**Mode:** self-sourced (autonomous queue empty → GOAL-driven assessment). Picked the freshest pre-thought
`▶ NEXT` reduced-motion lead.

## Gap (Real — concrete evidence)

- `src/index.css:81` — `.animate-fadeUp { animation: fadeUp 0.25s ease; }`; the `fadeUp` keyframe
  (`:77-80`) translates `translateY(8px)→0` (real motion). `grep -rn animate-fadeUp src/` → 82 hits / 40
  files, incl. `Dashboard.jsx:246` + `Study.jsx:66` page wrappers.
- `src/index.css:92-94` — `@media (prefers-reduced-motion: reduce) { .page-transition { animation: none; } }`
  zeroed the SAME keyframe for `.page-transition` ONLY. `.animate-fadeUp` un-suppressed → 82 sliding
  entrances for a motion-sensitive learner. Internal inconsistency (app respects reduced-motion everywhere
  else: framer-motion `useReducedMotion`, 3 toasts, guide controller, Settings scroll).

## Anti-hallucination gate

- **Real:** ✅ concrete `file:line` + grep counts above.
- **Measurable Done:** ✅ computed `animationName === 'none'` under `reduced` (e2e) + `.animate-fadeUp` present
  in the reduced-motion CSS block (unit) — both observable pass/fail, both red-proofed.
- **Verified:** ✅ pure CSS, no Malay/English content authored — nothing to web-verify.

## Red → green evidence

- Unit `src/lib/__tests__/reducedMotionCss.test.js` (+2): RED before —
  `expected '\n  .page-transition { animation: none; }\n' to match /\.animate-fadeUp/`. GREEN after.
- e2e `tests/e2e/page-transitions.spec.js` (+2): under `reducedMotion:'reduce'` → `.animate-fadeUp` computed
  `animationName === 'none'` (reduce case logically RED pre-fix: would compute `'fadeUp'`); default control
  still `'fadeUp'`. All 5 page-transitions e2e pass.

## Fix

`src/index.css` — added `.animate-fadeUp` to the existing reduced-motion media block (shared rule with
`.page-transition`). No FOUC: grep-confirmed no `.animate-fadeUp` element also sets `opacity-0`, so resting
state is opacity 1 / no transform.

## Decision log

- Shared rule with `.page-transition` (not a new `@media`) — same keyframe + intent, avoids future drift.
- Scope-vetoed `animate-spin` (loading affordance / essential), `animate-pulse` (separate decorative
  judgment), `shimmer` (dead CSS).

## Gate

- `npm run build` ✅ · `npm run test:run` ✅ 1676 tests (164 files, +2) · `npm run lint` ✅ 0 errors
  (3 known exhaustive-deps warnings, unchanged) · `npm run test:e2e -- page-transitions.spec.js` ✅ 5 passed.
- No `STORE_VERSION` / schema / free-path / `instruct.js` / content touch. HARD invariants intact.

## Hostile self-review

Malay regression? none (no content). Cross-language leak? none. Broken study mode? no (default animation
unchanged — e2e control proves `fadeUp` still fires). Dark/light? unaffected (animation, not colour). FOUC?
ruled out by the opacity-0 grep. Unverified claim? none (all grep-grounded).

## Deploy

Committed to `main` (= prod deploy). Confirm Vercel READY on the public `upg-` project.
