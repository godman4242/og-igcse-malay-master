# Local build-loop cycle report — 2026-06-20 22:12 UTC (KL 2026-06-21 06:12)

## Item shipped
**Content-truth fix (axis-1, HIGHEST)** — corrected a confident-WRONG exclusivity claim in the
Malay `prefix-ber-ajar` imbuhan drill feedback (`feedbackRules.js` entry `'bel- + ajar (irregular)'`).

This **completed an in-flight TDD cycle**: a prior cycle had red-proofed the test in
`grammar.test.js` but left the implementation unfinished + uncommitted in the working tree. This
cycle web-verified the linguistic claim and finished the fix.

## Why this beat the directed guide epic
GOAL.md's overnight clamp NO-OPs unless a fresh **axis-1 content-truth** gap clears the bar.
A confident-wrong grammar rule in a learning tool is exactly that, and axis-1 outranks the
directed axis-3 Full-Page-Guide work. The Phase-3b mechanical clamp tasks (T1 deferred, T2, T3)
are all already resolved, so absent an axis-1/axis-2 hit this cycle would have NO-OP'd.

## The gap (evidence)
- `feedbackRules.js:107` mnemonic: *"belajar is the ONE exception where ber- loses its R before a vowel-initial root."*
- `feedbackRules.js:113` relatedRule: *"Only ajar has this irregular form. All other vowel-initial roots keep ber-."*
- Single occurrence (grep-confirmed `one exception` / `only ajar` across `src/` non-test).
- These strings render to the student via `drill.rule` in `Grammar.jsx` → a false "only ajar" mis-teaches the rule as more restricted than it is.

## Web verification (authority, not memory)
- `ber- → bel-` applies to **two** roots, not one: `ajar → belajar` AND `unjur → belunjur`.
- Direct teaching-source quote: *"Awalan beR- yang bergabung dengan perkataan **ajar dan unjur**, awalan beR- akan menjadi bel-"* — azam09.blogspot.com/2009/09/imbuhan-ber.html.
- `belunjur` = "(duduk) meluruskan kaki ke depan / berselonjor", root `unjur`, irregular `bel-` — educalingo / kamusbm / KBBI, corroborated across 3+ independent references.
- Most vowel-initial roots still keep `ber-` (e.g. `ber- + usaha → berusaha`), so the corrected text names the precise two-root restriction, not a sweeping "all vowel-initial → bel-".

## Decision (flagged)
Rewrite mnemonic + relatedRule to teach the complete two-root rule; ADD the verified
`unjur → belunjur` example beside the unchanged `ajar → belajar`; align `generativePrompt` to
"two pure memory cases" (was a singular "the most common word" superlative).
**Veto note:** a complete, web-grounded two-example rule beats a tidy-but-false "only ajar". The
join key + anchor + `belajar` example stay byte-identical, so the drill still resolves.

## Fix (surgical — 1 data file)
`src/data/feedbackRules.js`, entry `'bel- + ajar (irregular)'` only: mnemonic / relatedRule /
generativePrompt strings + one added example. No answer key, drill `rule` key, `STORE_VERSION`,
schema, free-path, or `instruct.js` touch.

## TDD (red → green)
+3 unit tests in `src/data/__tests__/grammar.test.js`:
1. join key + `belajar` example intact (passed before and after — structural guard).
2. mnemonic+relatedRule drop `/one exception/i` & `/only ajar/i` — **RED first** (matched old strings).
3. text names `/unjur/i` — **RED first** (old text had no `unjur`).
Both RED tests failed for the right reason, then went green after the edit.

## Gate
- `npm run build` — OK (PWA generated, 0 errors).
- `npm run test:run` — **1781 passed** / 174 files (+3).
- `npm run lint` — 0 errors, 3 known exhaustive-deps warnings.
- e2e not run: pure data/content change, no rendered layout/flow change (consistent with the ber-kerja / error-mencomel content fixes). CI covers e2e on push.

## Self-review (hostile)
No Malay regression (strictly more correct); no cross-language class leak (data only); entry shape
unchanged so Grammar.jsx feedback rendering is unaffected; no theme/UI impact; every new claim
web-verified. Working tree held to the one item (3 files) before commit.

## Deploy
Ship to `main` (= prod). Vercel READY to be confirmed after push.
