# Local build-loop cycle report — 2026-06-20 23:25 UTC (KL 2026-06-21 07:25)

**Commit:** `9b4b61d` — `fix(writing): correct the confident-wrong Surat Kiriman Rasmi (formal letter) format template`

## Item
Axis-1 content-truth fix (overrides the overnight Full-Page-Guide clamp — GOAL.md:
"a real axis-1 gap STILL wins if one clears the bar — do that first"). Queue was empty +
Phase-3b clamp T1–T3 resolved → GOAL-driven Self-source assessment.

## Assessment that found the gap
Cross-referenced `src/data/*.js` against the top-of-queue **converged content ledger** and
audited the learner-facing files NOT on it:
- `connectors.js` — 28 penanda-wacana → English glosses, all correct → **CLEAN**.
- `writingSamples.js` — intentionally Band-3 error-laden drafts (by design, the analyzer is
  meant to catch them) → **not a bug**.
- `writing.js` `KARANGAN_TEMPLATES` (4 format templates) — `rencana`/`cerita`/`ucapan` clean;
  **`surat` (Formal Letter) was WRONG**.

## The gap
`writing.js`'s `surat` template (rendered to students at `TemplatesView.jsx:37`) taught the
**INFORMAL** letter layout under the "Surat Kiriman Rasmi / Formal Letter" label:
- sender's address `hint: 'Your address (top right)'`
- date `hint: 'Date below address'`, placed BEFORE the recipient's address

A student copying this loses format marks on IGCSE 0546 Paper 2/4 directed writing.

## Web verification (authority, not memory)
WebSearch was down this cycle; used DuckDuckGo HTML, aggregating UPND / MOE / JPA curriculum
sources:
- sender's address = `"Ditulis di bahagian atas kiri surat"` (top **LEFT**)
- order = Alamat Pengirim (kiri) → **garisan melintang** → Alamat Penerima → Tarikh
- date = `"di sebelah kanan surat, sebaris dengan"` the recipient's address (**RIGHT**)

## Fix (surgical, 1 data file)
`writing.js` `surat.structure` only — 3 coordinated lines:
- sender hint `top right` → `top left`
- **reorder** so `Tarikh` follows `Alamat Penerima`
- date hint `Date below address` → `Date on the right, level with the recipient address`
- note the garisan in the recipient hint (`under a line below your address`)

Malay section labels + examples left byte-identical (already correct — only the English
placement hints + order were wrong). No answer key / `STORE_VERSION` / schema / free-path /
`instruct.js` touch; templates are read-only display data.

## TDD (red → green)
New `src/data/__tests__/writing.test.js`, +4 tests:
1. sender hint contains "left", not "right" — **RED** first ("top right"), green after
2. `Alamat Penerima` index < `Tarikh` index — **RED** first (date before recipient), green after
3. date hint contains "right" — **RED** first ("below address"), green after
4. structural "is the formal letter" guard — passes before and after

Red proof: `3 failed | 1 passed` before the edit; `4 passed` after.

## Gate
- `npm run build` — exit 0
- `npm run test:run` — **1790 passed** (+4)
- `npm run lint` — 0 errors (3 known exhaustive-deps warnings)
- pre-commit quality gate (build → tests → lint → content) — ✅ passed

UI-affecting? Content text + reorder inside an EXISTING generically-rendered view (step count
unchanged at 9) — not a new screen/control/layout/flow → e2e not required (consistent with the
exemplars / dictionary / grammar content fixes). Content correction, not a new feature →
README + tour not required.

## Decision log
- **Decision:** correct the `surat` structure to the web-verified formal-letter format.
  **Why:** confident-wrong taught content = the worst failure for a learning tool; this is a
  clear axis-1 hit that overrides the clamp. **Veto note:** a web-verified standard format beats
  a tidy-but-wrong informal layout mislabeled as formal.
- **HARD-invariant screen:** no paywall / individual-revision / native-app / STORE_VERSION /
  schema / free-path / `instruct.js` / feature-deletion / secrets impact. All clear.

## Deploy
Pushed to `origin/main` (`9b4b61d`) → Vercel prod auto-deploy in flight (upg- + og-).

## Time check (step 1)
KL `202606210725`. `CUTOFF` is far-future (forever) → continue; no time-box hit.

## ▶ NEXT
Clamp resumes — Phase 3b (T1–T3) stays resolved; NO-OP every cycle unless another fresh
axis-1/axis-2 gap surfaces (Phases 4/5/3c remain attended-only). **Ledger now ALSO includes**
`writing.js` (KARANGAN_TEMPLATES) ✓, `connectors.js` ✓, `writingSamples.js` ✓. Remaining
un-audited `src/data` files are non-learner-facing infra (`malayValidityList.js`,
`englishFrequency.js`, `wikidataMalayEn.js`, `dictionaryIcons.js`, `aiMocks.js`,
`systemPrompts.js`, `topics.js`, `microPrompts.js`, `dictionaryEn.js`=generated) — a future
grounded pass could spot-check `topics.js`/`microPrompts.js`, but no concrete evidence of a gap
there today (don't invent it).
