# Local build-loop cycle report — 2026-06-14 14:05 UTC

**Mode:** GOAL-driven Self-source (autonomous build queue empty — all items `[x]`).
**Axis:** 1 — Correctness & content truth (highest priority).
**Item shipped:** `peN-` reference table listed `penulis` under the wrong allomorph rule ("No change").

## Gap (Real · evidenced)

`src/data/grammar.js:164` — `GRAMMAR_RULES['peN-']` rule #0 (`pe- + l, m, n, r, w, y`, note **"No change"**)
listed `penulis` as an example. But `penulis` (writer) is built from root **`tulis`** (t-initial) where the
**t drops** (`pen-` + (t)ulis → `penulis`) — the t-drop allomorph, already correctly listed in the same table's
rule #2 (`pen- + c, d, j, t`, "T drops!"). So one derived word sat under **two contradictory rules** — the same
bug class as the prior cycle's `kejar → mengejar` fix. This was the pre-thought `▶ NEXT` of that fix.

Internal corroboration that `penulis` = t-drop (and rule #0 was the outlier): drill `prefix-peN-tulis`
(`root:'tulis'`, `rule:'pen- + t → t drops'`), error drill `error-pentulis` (correction → `penulis`),
`transform-noun-menulis` (`hint:'… (T drops)'`), and `cikguKnowledge.js:183` ("pen- (t drops) before t →
penulis"). Every other reference in the codebase already treats it as t-drop.

## Measurable Done

- Removed `penulis` from the "No change" group; added `peramal` (a genuine no-change form).
- New `grammar.test.js` invariant: no derived word appears under two rules of the same prefix's allomorph
  table (run over `meN-` AND `peN-`), plus `penulis`-only-in-t-drop + `peramal`-present pins.

## Verified (content web-checked, not memory)

- peN- stays `pe-` (no consonant change) only before l/m/n/r/w/y (pelari, peramal, pelukis); t-initial native
  roots drop the t. Source: [BM Tatabahasa · imbuhan pe-](https://sites.google.com/site/bmalaysiatatabahasa/imbuhan/pe)
  ("Pe– tidak berubah jika bertemu huruf n, l, m"; t "luluh"). Web search confirmed `peramal` = pe- + ramal.

## Decision log

- **Replacement word = `peramal`** (pe- + ramal = forecaster). *Why:* verified clean no-change form that adds
  the `r` consonant, mirroring the meN- table's `merangkak` (r); `pelukis` already covers `l`.
  *Veto note:* `pelari` is also correct but duplicates the `l` example — `peramal` gives better consonant
  spread at equal correctness. The answer key (`penulis`, now only under t-drop) and all drills are untouched.

## Red → green evidence

- Pre-fix: 3 of 4 new tests FAIL — `peN-` duplication invariant fails with
  `"penulis" appears under two peN- rules (#0 and #2)`; `penulis`-only-in-t-drop and `peramal`-present both
  fail. The `meN-` branch of the invariant PASSES (non-vacuity — the invariant is not always-failing).
- Post-fix: `grammar.test.js` 7/7 green.

## Gate

- `npm run build` — green (eager `index` unchanged; data-file edit).
- `npm run test:run` — **1581 passed** (+4).
- `npm run lint` — 0 errors (same 3 pre-existing exhaustive-deps warnings).
- e2e skipped by design: a single text token in an existing reference list is no new layout/flow (covered by
  the content test + unit gate).

## Invariants

No STORE_VERSION bump · no Supabase-schema or free-path break · no feature deleted · `instruct.js` API frozen ·
no secrets · content web-verified. Surgical diff: 1 word in `grammar.js`, +4 tests, RESUME_HERE bookkeeping.

## Deploy

Committed to `main` (= prod deploy via auto-push). Step-1 time check: KL 202606142205, no cutoff set (forever
mode) → continue.

## ▶ NEXT

The meN-/peN- allomorph tables are now internally consistent and guarded by the cross-rule invariant.
Strongest remaining axis-1 threads: grounded content audits of `scenarios.js`, `exemplars.js`,
`comprehensionPassages.js`, and a grounded ruling on the still-muddled `ber- + asa → berasa` notation
(line 37 + `GRAMMAR_RULES['ber-']`'s `'berasa → berasa'` — root `rasa` vs `asa` ambiguity).
