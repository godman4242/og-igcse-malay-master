# Local build-loop cycle report — 20260614-1926 UTC

**Item:** Content-truth fix — word-family explorer taught the non-word `penyihat` (not in DBP).
**Mode:** GOAL-driven Self-source (autonomous build queue empty). Axis-1 (correctness & content truth).
**Time check (step 1):** KL `202606150318` — no `CUTOFF` set (forever loop); continue.

## Gap (evidenced)

`src/data/wordFamilies.js:446`, the `sihat` (healthy) word-family — rendered verbatim to students by
`WordFamilies.jsx` — listed `{ word:'penyihat', type:'peN-', meaning:'healer', pos:'noun' }`. **`penyihat`
is not a Malay word.** DBP (prpm.dbp.gov.my) returns no entry for it, and DBP's official "Kata Terbitan"
derived-words list for `sihat` is `menyihatkan` / `kesihatan` / `penyihatan` — the bare peN- agent noun is
absent. The genuine word for "healer" is `penyembuh` (root `sembuh`). Same fabricated-word bug class as the
`berdidik` / `penjadi` non-word fixes.

This was the pre-thought `▶ NEXT` of the `berdidik` cycle (audit `wordFamilies.js`'s remaining suspects
`penyihat` / `bertinggal` / `pengaman`). All three were web-verified against DBP this cycle:
- `penyihat` — **non-word** (no DBP entry) → SHIPPED this cycle.
- `bertinggal` — real word, but the app's meaning "to reside (formal)" is fabricated (DBP: *"bertinggal kata
  = berpesan sebelum berangkat"*) → flagged ▶ NEXT.
- `pengaman` — real word, gloss "security guard" is loose (DBP: *"orang yg mengamankan"* = peacekeeper/securer)
  → flagged ▶ NEXT.
Picked `penyihat` as the single biggest gap (a non-word is the highest-severity content error) — and it had
the cleanest fix (a DBP-attested replacement that enriches the family).

## Web verification (before shipping, not from memory)

- `penyihat`: https://prpm.dbp.gov.my/Cari1?keyword=penyihat → no entry.
- `sihat` derived-words list: https://prpm.dbp.gov.my/Cari1?keyword=sihat → menyihatkan / kesihatan / penyihatan.
- `penyihatan` definition: https://prpm.dbp.gov.my/Cari1?keyword=penyihatan → *"perbuatan atau perihal
  menyihatkan"* (the act of making healthy), example *"Vitamin membantu penyihatan badan"*.
- `penyembuh` = healer (root `sembuh`): Glosbe / indifferentlanguages corroboration.

## Fix (surgical, 1 data line)

`{ word:'penyihat', type:'peN-', meaning:'healer', pos:'noun' }` →
`{ word:'penyihatan', type:'peN-...-an', meaning:'the act of making healthy', pos:'noun' }`

**Decision/why:** biggest severity (non-word) + cleanest fix — `penyihatan` is in DBP's Kata Terbitan, has a
correct meaning, and adds a peN-...-an process-noun affix the family lacked (it had meN-...-kan + ke-...-an).
Affix type `peN-...-an` matches the file's convention (`peninggalan` / `penangkapan`). **Veto note:** could
not keep the "healer" slot — `penyembuh` is root `sembuh`, so filing it under `sihat` would create a new
misfiling.

**Scope:** kept to ONE item. Orphan `penyihat.webp` icon-manifest/script entries are build tooling, not
student-facing (`WordFamilies.jsx` uses no per-word icons) — left untouched, flagged ▶ NEXT (same as the
`berdidik.webp` orphan).

## TDD (red → green)

New `src/data/__tests__/wordFamilies.test.js` block (+3) over `WORD_FAMILIES['sihat']`: NOT-contains
`penyihat`; contains `penyihatan` typed `peN-...-an`/`pos:'noun'`; keeps `menyihatkan`/`kesihatan`
(non-vacuity). Red-proof: **2 of 3 FAILED first** for the right reasons (`penyihat` present, `penyihatan`
absent); non-vacuity passed. All 3 green after the fix.

## Gate result

- `npm run build` → green (PWA generated; data file → `index` chunk unchanged).
- `npm run test:run` → **1625 passed** (155 files), +3.
- `npm run lint` → 0 errors, the 3 known pre-existing exhaustive-deps warnings only.
- e2e skipped by design (single string swap in existing list data — no layout/flow change; CI runs e2e on push).

## Self-review (hostile)

No Malay regression (non-word → real DBP word, correct affix + meaning); no cross-language leak (data only);
WordFamilies render path unaffected; no theme/study-mode impact; every fact web-verified against DBP. HARD
invariants intact: no STORE_VERSION bump, no schema/free-path break, no feature deletion, instruct.js
untouched, no secrets.

## Deploy

One commit to `main` (= prod deploy) with the shipped-✅ section + `[x]` queue line in the same commit.
Re-synced before push.

## ▶ NEXT

`bertinggal` (swap for `meninggal` — to die/pass away) and `pengaman` (gloss → peacekeeper/securer) in
`wordFamilies.js` — both web-verified-wrong this cycle; pick the bigger or NO-OP if a grounded re-check
clears them. Lower-priority threads: orphan `*.webp`/manifest GC; paper-numbering inversion (Kheshav's
product call); `imbuhan-se` `semua`/`seluruh`.
