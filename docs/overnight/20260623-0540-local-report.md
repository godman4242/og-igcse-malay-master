# Local build-loop cycle report — 2026-06-23 05:40 UTC (commit b494250)

## Item shipped
**Phase 3c · T23 — the `/cikgu` (Cikgu Maya) page-tour deep dive** (directed axis-3 epic,
Full Page Guide rollout plan).

The header ▶ "Tour this page" now works on `/cikgu`: a centered intro + 2 anchored steps
(the Expert/AI mode toggle · the question input row) + a centered "Suggested questions,
topics & answers" summary (the empty-chat helpers + the per-answer Expert/AI tag).

## Why this pick (assessment against GOAL.md axes)
- Autonomous build queue in `RESUME_HERE.md` was **empty** (all `[x]`) → GOAL-driven Self-source.
- **An axis-1 candidate was ASSESSED then REJECTED by the anti-hallucination gate** (the gate
  working as intended): the bilingual SPEAKING surfaces (`Roleplay.jsx:168` scenario chip ·
  `RoleplayScorecard.jsx:171` "IGCSE Paper 3 Band" · `Dashboard.jsx:888` onboarding ·
  `dailyPlan.js:133` plan reason) carry "Paper 3" un-gated. I weighed dropping it like the
  prior listening + feedback cycles — but **web-verified vs Cambridge**
  (`help.cambridgeinternational.org`: *"Speaking Test (Paper 3) for … 0510 and 0511"* +
  `0510/03 Paper 3 Speaking`): **"Paper 3" is the CORRECT speaking-paper label for Malay 0546
  AND English 0510/0511** (the app's primary ESL audiences); it is only imprecise for English
  0500 First Language (speaking = Component 4). Dropping it would remove correct info for the
  majority → NOT a content-truth win. The 0500 nuance = the same "which English syllabus"
  product call already parked as **needs-Kheshav** (GOAL-backlog #4). Deferred, not solo-built.
- No fresh axis-1/axis-2 gap cleared the bar → fall to the **directed epic** → next plan task =
  **T23 — Cikgu**.

## Decision log
- **Decision:** build T23 to the established T16/T17 hybrid shape (intro + 2 anchored on
  always-mounted default-view controls + a centered `arrow:'none'` summary for state-/device-
  dependent surfaces). **Why:** CikguBot's empty-chat helpers (suggested prompts + Browse
  Topics) render only with no history, Voice/mic is capability-gated, and the Expert/AI tag
  only appears after a reply — anchoring any of those would skip-then-hang (GOAL-backlog-#5 /
  Bug-A). The mode toggle + input row ARE always mounted, so they get real arrows.
  **Veto note:** a zero-anchor guide (T19/T21 style) would also be safe but teaches less — the
  two always-mounted controls are worth real arrows.
- **Decision:** rejected the speaking "Paper 3" drop (see above). **Veto note:** if Kheshav
  wants the 0500 case handled, gate by `studyLang`/syllabus — not a blanket drop.

## Red → green evidence (TDD)
- Added the `/cikgu` describe block to `pageGuides.test.js` (+3 tests) and ran it FIRST:
  **3/3 failed for the right reason** — `PAGE_GUIDES['/cikgu']` undefined + the 2 anchors absent
  from `CikguBot.jsx` source.
- Implemented the entry + route seam + anchors → re-ran: **42/42 pass** in the file.

## Files changed
- `src/lib/guide/pageGuides.js` — `/cikgu` entry (4 steps).
- `src/lib/guide/pageGuideRoutes.js` — `/cikgu` added to the eager seam.
- `src/pages/CikguBot.jsx` — 2 inert `data-guide` anchors (`cikgu-mode`, `cikgu-input`).
- `src/lib/guide/__tests__/pageGuides.test.js` — +3 red-proofed tests (incl. a non-tautological
  source-scan of `CikguBot.jsx`).
- `tests/e2e/guide-cikgu.spec.js` — new (2 tests).
- `README.md`, plan doc, `RESUME_HERE.md` — ship-contract updates.

## Gate result
- `npm run build` → **0 errors**.
- `npm run test:run` → **1916 passed / 183 files** (was 1913 + 3 new).
- `npm run lint` → **0 errors**, 3 known pre-existing warnings.
- e2e (UI-affecting) → `guide-cikgu` 2/2 + `guide-full-page` 4/4 = **6/6**.
- Pre-commit quality gate: build ✓ tests ✓ lint ✓ content ✓.

## Content grounding (axis-1)
Every claim hand-verified against live `CikguBot.jsx`: Expert "Instant answers, always free" ·
the Gemini→OpenRouter→quota AI chain + `getRemainingCalls()` count · `topicLabels` Browse-Topics
groups verbatim · `getSuggestedPrompts(mistakes)` · the per-message Expert/AI badge ·
`clearHistory` 🗑 · the Voice FSM + `parseStopKeyword` "say stop". No new Malay authored (only
existing app terms imbuhan/peribahasa/etc.). No confident-wrong.

## Invariants
No `STORE_VERSION` bump · no schema/free-path change · `instruct.js` API untouched · no feature
deleted · no secrets. CSS/theme: no color/class change → dark+light identical.

## Deploy
Committed `b494250`, pushed to `main` (auto-push; `0 0` in sync with `origin/main`) → prod deploy.

## ▶ NEXT
- **T24** — Dictation / Cloze-Listening / Saved-cloze deep dives (plan Phase 3c line 162).
- Low-value follow-up (NOT an axis-1 learner-content gap): residual confident-wrong COMMENTS
  calling listening "Paper-4" (`clozeListening.js:6`, `dictation.js:4`, `examPassages.js:24`,
  `useStore.js:328` "Paper 1+2+3") — non-learner-facing hygiene.
- **needs-Kheshav:** the bilingual speaking "Paper 3" 0500-nuance (gate-by-syllabus vs keep).

## Step-1 time check
No `CUTOFF` set (forever loop) → no time-box stop. One cycle complete; exiting (the shell loops).
