# Local build-loop report — 2026-06-14 18:21 UTC (KL 2026-06-15 02:21)

**Cycle outcome:** SHIPPED one axis-1 content-truth fix.

## Item
Cikgu Maya `imbuhan-pen` entry taught the peN- + j-initial rule with a **fabricated word**:
`- pen- before d, c, j → pendapat, pencari, penjadi`. `penjadi` is not a Malay word (no DBP /
dictionary entry); the canonical j-initial peN- nouns are `penjual` / `penjaga`. The line is
rendered **verbatim** to the student via `formatKnowledgeResponse` (`cikguKnowledge.js:1462`),
so this is a confident-wrong grammar lesson — the highest-priority (axis-1) failure class.

Self-sourced (queue empty → GOAL-driven Self-source mode). It was the pre-thought `▶ NEXT` of the
prior imbuhan-ber ship. Audited the 4 flagged tatabahasa Cikgu entries first — `kata-sendi`,
`penjodoh-bilangan`, `kata-ganda`, `golongan-kata` all checked out correct; only `imbuhan-pen` had
the wrong example.

## Evidence (Real · Measurable · Verified)
- **Real:** `src/data/cikguKnowledge.js:182`. `penjadi` appears nowhere else in `src/`/`scripts/`
  (sole occurrence = the bug site); `penjual` is already a validated headword in the app's own
  `malayValidityList.js` / `wordFamilies.js` / `wikidataMalayEn.js`.
- **Measurable Done:** new `cikguKnowledge.test.js` block (+3) — j-line must contain `penjual`, must
  NOT contain `penjadi`, d/c examples preserved. Red-proofed: 2 failed / 1 passed pre-fix → 3 green
  post-fix.
- **Verified (web):** SlideShare "Imbuhan PEN~"; sites.google.com/site/bmalaysiatatabahasa/imbuhan/pe —
  peN- + jual → penjual, + jaga → penjaga. DBP/dictionary lookup for "penjadi" → no entry.

## Decision log
- **Decision:** replace `penjadi` → `penjual (seller, from jual)`. **Why:** most common j-initial peN-
  noun, already a validated word in the app's own data; the parenthetical matches the sibling-line style
  (`pemukul (hitter/bat)`, `penulis (writer, from tulis)`). **Veto note:** `penjaga` (guard) equally
  valid, but `penjual` is more frequent + self-consistent with existing data → chosen.
- Scoring-neutral: neither token is a `goldCikgu.mjs` keyword; confidence-gate calibration unchanged.

## Gate
- `npm run build` → green (data file; `index` chunk unchanged).
- `npm run test:run` → **1600 passed** (+3 vs 1597).
- `npm run lint` → 0 errors, 3 pre-existing exhaustive-deps warnings.
- e2e skipped by design (single data-string edit, no layout/flow change; CI runs e2e on push).
- No STORE_VERSION bump; no schema/free-path break; `instruct.js` API untouched; no feature deleted.

## Files
- `src/data/cikguKnowledge.js` (1 data line)
- `src/data/__tests__/cikguKnowledge.test.js` (+3 tests)
- `RESUME_HERE.md` (queue `[x]` + shipped section)

## Step-1 time check
KL `202606150216` at cycle start. No finite CUTOFF (forever loop) → continue.

## ▶ NEXT
imbuhan tables + 4 tatabahasa Cikgu entries now audited + clean. Remaining axis-1 threads:
`imbuhan-an`/`imbuhan-kan`/`tense-markers` Cikgu entries, the `peribahasa`/`common-mistakes` banks,
and `grammarEng.js` — grounded web-verified audit each, or NO-OP if clean.
