# Local build-loop cycle — 2026-06-15 10:19 UTC

**Mode:** GOAL-driven Self-source (queue empty — all items `[x]`).
**Axis:** 1 — Correctness & content truth (HIGHEST).
**Item shipped:** Exam countdown now counts LOCAL calendar days (kills the UTC off-by-one for the
UTC+8 primary audience).

## Step-1 time check
`TZ=Asia/Kuala_Lumpur date +%Y%m%d%H%M` → `202606151810`. No finite CUTOFF set (forever mode) → continue.

## Assessment (3A/3B)
Queue empty; ~20 recent cycles swept content-truth + a11y micro-fixes + substring-grading clean. Directed a
fresh read-only Explore bug hunt at the UNDER-examined **computational/state-logic** surfaces (date math,
FSRS, streak/freeze, daily-challenge, readiness). Three date candidates surfaced; verified each against code:

- **REJECTED** `reviewedToday` (`reviewCardAction`) and **REJECTED** streak (`updateStreak`/`getStreak`) — both
  rested on the false claim that `toDateString()` is "locale-dependent" (it is a fixed English format). Each
  writes+reads the same `toDateString()` value → self-consistent + correct. `localDay.js`'s own header comment
  confirms streaks deliberately use local `toDateString()`. No reproducible defect → fail the gate.
- **PICKED** the exam-countdown UTC off-by-one — Real (node-proven), Measurable-Done (failing test → green),
  Verified (ECMAScript date-only parse rule). Highest axis, affects the whole UTC+8 audience, a core feature.

## Bug (proven)
`examDate` = `YYYY-MM-DD` from `<input type="date">`. `new Date('2026-06-20')` = UTC midnight, subtracted from
local `new Date()` + `Math.ceil` → for UTC+8, +1 too high during local 00:00–08:00 daily.
`TZ=Asia/Kuala_Lumpur node`: KL June-20 04:00 / exam June-20 → buggy **1** (should be 0); KL June-15 03:00 /
exam June-20 → buggy **6** (should be 5). Same P2-C3 UTC-vs-local class, missed for the countdown.

## Fix
One pure helper `daysUntilLocalDate(dateStr, now)` in `src/lib/localDay.js` (local-date parse, `Math.round` of
two local midnights, accepts full-ISO too, signed return). 4 call sites swapped: `useStore.js:589` & `:1810`,
`feedback.js:58`, `Settings.jsx:547`. No buggy `new Date(examDate)` day-math remains in `src/` (grep-confirmed).

## Red → green
+7 unit tests in `src/lib/__tests__/localDay.test.js` (TZ=Asia/Kuala_Lumpur). RED first:
`daysUntilLocalDate is not a function` (7 fail, 6 pre-existing pass). After implementing: 13/13 green. Each new
test contrasts the OLD buggy value with the fix.

## Gate
- `npm run build` → OK (no new chunk-budget regressions; pure logic).
- `npm run test:run` → **165 files, 1685 tests pass** (+7).
- `npm run lint` → **0 errors**, 3 known exhaustive-deps warnings (unchanged).
- e2e: skipped — pure numeric/logic change to existing rendered text, no new screen/control/layout/flow; a
  date-dependent countdown e2e would be flaky. Unit coverage on the helper is the right level.

## Self-review (hostile)
No Malay/EN content authored (nothing to web-verify beyond the proven parse rule); no CSS/theme/class change; no
study-mode impact; null/malformed → null then `Math.max(0,…)`=0; past-exam `<0 → null` guard preserved; full-ISO
path keeps `feedback.test.js` green; `Math.round` of local midnights is DST-robust (KL has no DST anyway).

## Invariants
No STORE_VERSION bump (stored format unchanged → no migration); no schema/free-path/`instruct.js`/content/feature
touch. Surgical diff (1 helper + 4 one-line swaps + tests + docs).

## Deploy
Commit `fix(exam): …` to `main` (= prod). Vercel READY to confirm post-push.

## Decision log
- One shared helper (not 4 inline fixes) — single source of truth stops the copies drifting again.
- Accept full-ISO timestamps — keeps the synthetic-input feedback tests green; harmless for real data.
- `Math.round` of local midnights, not `ceil` of fractional UTC ms — exact + DST-robust.
