# Local build-loop cycle report — 2026-06-14 19:34 UTC (KL 2026-06-15 03:34)

**Mode:** GOAL-driven Self-source (autonomous build queue all `[x]`).
**Axis:** 1 — Correctness & content truth (highest priority).
**Item shipped:** Content-truth fix — word-family explorer taught a FABRICATED meaning for `bertinggal` ("to reside").

## Assess → gap pick

Queue empty → assessed the app against `docs/loop/GOAL.md`'s 6 axes. The recent `▶ NEXT` of the `penyihat` ship
(`RESUME_HERE.md`) carried two web-flagged axis-1 suspects in the still-only-partly-audited `src/data/wordFamilies.js`
(rendered verbatim to students by `WordFamilies.jsx`):

- `bertinggal` (`tinggal` family) — claimed ber- "to reside (formal)".
- `pengaman` (`aman` family) — claimed peN- "security guard".

Both **independently web-verified against DBP this cycle** (not trusting the prior note):

- `bertinggal` IS a real DBP word, but means *"~ kata = berpesan (sebelum berangkat)"* (bid farewell before
  departing), **not** "to reside" — a **fully fabricated meaning**. Also absent from DBP's official Kata Terbitan for
  `tinggal`.
- `pengaman` is real; "security guard" is a **loose-but-ballpark** gloss (real sense: peacekeeper / one who secures).

**Pick:** `bertinggal` — a fully-wrong meaning outranks `pengaman`'s mild gloss-slip. Passes the anti-hallucination
gate: **Real** (DBP evidence), **Measurable Done** (failing content tests → green), **Verified** (DBP-grounded).

## Red → green

New `src/data/__tests__/wordFamilies.test.js` block (+3) over `WORD_FAMILIES['tinggal']`:
- does NOT contain `bertinggal` · contains `meninggal` typed `meN-`/`pos:'verb'` · keeps genuine derivations
  (`meninggalkan`/`ditinggalkan`/`peninggalan`).

Watched **2 of 3 FAIL first** on pre-fix data (`bertinggal` present, `meninggal` absent), non-vacuity PASSED. After the
1-line fix in `src/data/wordFamilies.js:415`
(`bertinggal`/ber-/"to reside (formal)" → `meninggal`/meN-/"to pass away/die"), all 3 green.

## Decision log

- **Decision:** replace `bertinggal` → `meninggal` (meN-). **Why:** `meninggal` is in DBP's official Kata Terbitan for
  `tinggal`, is a genuine meN- form (t-drop, mirrors the family's own `meninggalkan`), is high-frequency
  (*meninggal dunia*), and adds the bare meN- verb the family lacked. **Veto note:** no "reside" slot is honest under
  `tinggal` (that is `menetap`/`bermastautin`, different roots) — replacing the meaning would just create a new
  misfiling.
- **Scope:** left the orphan `bertinggal.webp` entry in `dictionaryIconsManifest.json` (build tooling, not
  student-facing; `WordFamilies.jsx` renders no per-word icons) to keep the diff to ONE item.

## Gate result

- `npm run build` — green (data file; `index` chunk unchanged).
- `npm run test:run` — **1628 passed** (155 files; +3 new).
- `npm run lint` — 0 errors (3 known pre-existing exhaustive-deps warnings).
- e2e — skipped by design (single string swap in existing list data, no layout/flow change; content test + unit gate
  cover it; CI runs e2e on push).
- HARD invariants — no STORE_VERSION bump, no schema/free-path break, no feature deleted, `instruct.js` API untouched,
  no secrets. Pure content fix.

## Deploy

One commit to `main` (= prod deploy) with the RESUME_HERE shipped section + queue `[x]` in the same commit.

## ▶ NEXT

- `pengaman` (`aman` family) gloss "security guard" → tighten to "peacekeeper/one who secures" or NO-OP after a fresh
  DBP check.
- Orphan `*.webp`/manifest entries for `berdidik`/`penyihat`/`bertinggal` (tooling GC, low priority).
- Paper-numbering inversion (needs Kheshav's product call); `imbuhan-se` `semua`/`seluruh` (murky).
