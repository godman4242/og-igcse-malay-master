# Local build-loop cycle report — 2026-06-23 05:53 UTC

**Item shipped:** Directed epic axis-3 (Full Page Guide, plan Phase 3c → **T24·1: the
Dictation deep dive**). The header ▶ "Tour this page" now works on `/dictation`.

## Why this item
- Queue (`RESUME_HERE.md` 🤖) had **no unchecked `[ ]`** → GOAL-driven mode.
- GOAL.md directs the **Full Page Guide Phase 3c** epic (axis-3), one page per cycle; a fresh
  axis-1/axis-2 gap would preempt but the last five cycles (T19–T23) all converged on "no fresh
  axis-1/axis-2 gap; content ledger trusted", and the GOAL backlog holds only axis-3 robustness
  follow-ups. Trusted that convergence.
- T23's `▶ NEXT` = **T24 (Dictation / Cloze-Listening / Saved-cloze)**; plan order leads with
  Dictation. Picked `/dictation`.

## What shipped
- `src/lib/guide/pageGuides.js` — new `/dictation` entry: centered intro + 2 anchored
  (`dictation-lang`, `dictation-start`) + a centered `arrow:'none'` "Inside a set" summary for the
  listen-and-type loop (which only mounts after Start).
- `src/lib/guide/pageGuideRoutes.js` — `/dictation` added to the eager seam (lights the header ▶).
- `src/lib/guide/tourSteps.js` — **route reconcile (forced by the gate):** `/dictation` added to
  `APP_ROUTES` (19→20) + a `full-dictation` `FULL_TOUR` step (22→23 steps). `pageGuides.test.js:27`
  requires every `PAGE_GUIDES` key ∈ `APP_ROUTES`; `tourSteps.test.js:78` requires every
  `APP_ROUTES` entry ∈ `FULL_TOUR` — so building the guide mandates the reconcile the plan (line
  166) had deferred. Partially closes that deferral; `/cloze-listening` finishes it next increment.
- `src/pages/Dictation.jsx` — 2 inert `data-guide` anchors (`dictation-lang` on the setup language
  toggle div, `dictation-start` on the Start button). No behavioral change.
- `README.md` — "Tour this page" route list + walk-through clause extended with Dictation.
- `tests/e2e/guide-dictation.spec.js` — new (2 tests).

## Content grounding (axis-1, no confident-wrong)
Every guide claim verified line-by-line against `Dictation.jsx`: `SET_SIZE=5`;
`pickDictationItems(LISTENING_PASSAGES, …)` (sentences drawn from the listening passages);
`disabled={!ttsSupported}` + the no-TTS warning banner; `MAX_PLAYS=2` with `rate 0.95→0.85`
(slower second play); `canType = playsUsed >= 1` (typing unlocks after one play); the Check
word-by-word diff (green ✓ / red ✗) + the Reference sentence reveal; the done-stage average %;
`missedDictationWords(r)` → `addMistake` (missed words → Mistake Journal). **No new Malay
authored** — only the toggle's own "Bahasa Melayu" label (correct).

## TDD red→green evidence
- Added the `/dictation` describe block to `pageGuides.test.js` (3 tests) BEFORE implementing.
- **RED (confirmed for the right reason):**
  - `exists with a centered intro + several steps` → `AssertionError: expected false to be true`
    (`Array.isArray(undefined)`).
  - `covers each setup control with a real dictation anchor` → `TypeError: Cannot read properties
    of undefined (reading 'map')`.
  - `every dictation anchor it teaches exists in Dictation.jsx source` → `AssertionError:
    dictation-lang: expected '…' to contain 'data-guide="dictation-lang"'`.
- **GREEN after implementation:** `pageGuides.test.js` + `tourSteps.test.js` = 54/54.

## Decision log
- **Decision:** anchor only the 2 always-mounted setup controls; teach the active/done loop in a
  centered no-arrow summary. **Why:** the player/typing-box/diff/score mount only after Start, so an
  arrow there would hit a missing node and hang-then-skip on the landing state (GOAL-backlog-#5 /
  Bug-A class). Mirrors the shipped Listening/Speaking pattern. *Veto note:* an in-session deep dive
  keyed to the active controls can be added later with zero rework.
- **Decision:** do the `/dictation` route reconcile now rather than defer to epic-end. **Why:** the
  test gate makes it mandatory; doing it per-page is cleaner than a big deferred batch and leaves
  `APP_ROUTES` at 20 (→21 after `/cloze-listening`). *Veto note:* `/saved-cloze` already reconciled,
  so only `/cloze-listening` remains.

## Gate result
- `npm run build` — exit 0 (built in 3.62s; PWA precache 134 entries).
- `npm run test:run` — **1919 passed** (183 files, +3 new), 0 fail.
- `npm run lint` — 0 errors, 3 known exhaustive-deps warnings (RoleplayScorecard / Comprehension /
  Roleplay — pre-existing).
- e2e (UI-affecting) — `guide-dictation` 2/2 + `guide-cikgu` 2/2 + `guide-full-page` 4/4 = **8/8**.
- Perf: `Dictation` page chunk 8.95 KiB (≪ 70 KB per-route budget); `pageGuides` on-demand
  chunk ~40 KiB (exempt); eager `index` ~479.5 kB (≈flat).

## Invariants screen
No paywall · individual-revision only · no native dep · no `STORE_VERSION` bump · no Supabase-schema
or free-path change · `instruct.js` API untouched · no feature deleted · no secrets · content
web-/code-verified. All clear.

## Deploy
Committed to `main` (= prod deploy) after a re-sync. Confirm Vercel READY on the public project.

## ▶ NEXT
T24·2 — the Cloze-Listening deep dive (`/cloze-listening`); same shape, needs the same route
reconcile (`APP_ROUTES` 20→21 + a FULL_TOUR step), which then fully satisfies the plan-line-166
reconcile. Then T24·3 Saved-cloze (`/saved-cloze`, already reconciled) and T25 Settings.

## Step-1 time check
No `CUTOFF` set (forever loop) — no time-box gate this cycle.
