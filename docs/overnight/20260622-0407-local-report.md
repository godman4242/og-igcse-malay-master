# Local build-loop cycle report — 2026-06-22 04:07 UTC (12:07 KL) · commit `396686e`

## Item shipped
**Directed epic axis-3 (Full Page Guide) — Phase 3c T16: the Listening picker deep dive.**
The header ▶ "Tour this page" now works on `/listening`.

Plan: `docs/superpowers/plans/2026-06-21-guide-full-page-rollout-plan.md` Phase 3c, T16.
Queue: directed-epic increments all `[x]` through T15; `/listening` was the explicit
`▶ NEXT` thread from T15 and the next page in the plan's priority order. No fresh
axis-1/axis-2 gap cleared the bar (converged content ledger trusted per prior cycles).

## What shipped
- `/listening` entry in `src/lib/guide/pageGuides.js` — 4 steps: centered intro +
  `listening-passages` (anchored) + `listening-badges` (anchored) + a centered
  "Inside a passage" summary (`arrow:'none'`).
- `/listening` added to the eager `PAGE_GUIDE_ROUTES` seam (lights the header ▶).
- 2 new `data-guide="listening-…"` anchors in `src/pages/Listening.jsx`, both via an
  `idx === 0` guard added to the existing passage `.map` (the page's only structural change).

## Key decisions (decide-and-flag)
- **Normal (non-theater) page** → the normal header ▶ is the entry (like `/comprehension`,
  unlike `/study`). Guide targets the picker landing state (`if (!passage)` branch).
- **Hear-it loop taught in a centered summary, not arrows.** The Player/question/score/
  transcript only mount after a passage opens, so picker-state arrows would hit missing
  nodes (3 s hang-then-skip, `guideController.js:101`). Mirrors `/comprehension`.
  Veto note: an in-passage deep dive can be added later with zero rework.
- **Guarded against a confident-wrong copy from Comprehension:** Listening shows only
  THREE badges (EN/MY · difficulty · question-count) and orders by `leadByLang` ALONE —
  NO topic/interest-star (Comprehension has five badges + `prioritiseByInterests`). The
  copy describes exactly three badges and the audio-first reveal-gating mechanic.

## Red → green evidence
- TDD: +2 unit tests in `pageGuides.test.js`. RED-proofed by `git stash`-ing the 3 impl
  files (keeping the test): the new `/listening` describe block failed 2/2
  (`PAGE_GUIDES['/listening']` undefined → `Array.isArray(false)` + `steps.map` throws),
  while the 23 existing tests (incl. the `PAGE_GUIDE_ROUTES`-sync) stayed green because
  both seam files were stashed together. `git stash pop` → 25/25 green.

## Content verification (axis-1)
No new Malay authored — English UI copy describing controls. Every claim grounded by
reading `Listening.jsx` this cycle: Paper 4 heading; `leadByLang` ordering; `speakerHint`;
the 3 badges (difficulty values beginner/intermediate/advanced verified across all 8
passages); text-hidden + Play; `MAX_PLAYS=2` second-slower replay; `canStartQuestions =
playsUsed>=1` unlock; instant verdict+explanation; score; "Show transcript" at the end;
`addMistake` on wrong; TTS-gating note. Example "Flight Announcement" = the first EN
passage title.

## Gate result
- build: exit 0
- unit tests: **1877 passed** (+2, was 1875)
- lint: 0 errors (3 known exhaustive-deps warnings)
- content-lint: pass
- e2e (UI-affecting → ran locally): `guide-listening` 2/2 + `guide-comprehension` 2/2 +
  `guide-full-page` 4/4 = **8/8**
- Perf: `Listening` page chunk 10.40 kB / 3.06 kB gz (< 70 KB budget); `pageGuides` lazy
  chunk 22.00 kB (exempt — on-demand); eager `index` 479.41 kB (≈flat vs T15's 479.39).

## HARD-invariant screen
No `STORE_VERSION`/schema/free-path/`instruct.js` touch · no paywall · no feature deleted
· no secrets · no theme/CSS change (dark+light identical) · `/listening` already in
`APP_ROUTES`+`FULL_TOUR` (no route reconcile needed) · no fixture re-point (`/settings`
stays the guideless example).

## Ship contract
README "Tour this page" line + the rollout plan (T16 → ✅) + RESUME_HERE queue (T16 `[x]`
shipped section) all updated in the same commit. FULL_TOUR already has a `/listening` step.

## Deploy
Pushed to `main` (= prod deploy). `origin/main == local == 396686e`.

## Step-1 time-check
No finite `CUTOFF` set (forever loop) → no cutoff gate. Cycle completed normally.

## ▶ NEXT
Phase 3c **T17 — Speaking (`/speaking`)**: mirror this shape in `pageGuides.js`, add
`/speaking` to `PAGE_GUIDE_ROUTES`, add `data-guide="speaking-…"` anchors to the live
controls (topic picker leading with `studyLang`, MS/EN toggle, record/speak controls,
band scoring), pin with `pageGuides.test.js` + a `guide-*` e2e. Then T18 Import, T19
Mistakes, … one page per commit. A fresh axis-1/axis-2 gap preempts if one clears the bar.
