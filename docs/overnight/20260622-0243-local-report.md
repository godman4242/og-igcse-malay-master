# Local build-loop cycle report — 2026-06-22 02:43 UTC

**Item shipped:** Directed epic axis-3 (Full Page Guide) — Phase 3c **T10: the Study page (`/study`) deep dive**.

## Why this item
Queue was all `[x]`; no fresh axis-1/axis-2 gap cleared the bar (converged content ledger
trusted per prior cycles). The explicit `▶ NEXT` from the prior cycle (T9 increment 2) and the next
page in the plan's priority order is T10 — the Study page. Built top-down, one bounded gate-green commit.

## What shipped
- `src/lib/guide/pageGuides.js` — new `/study` entry: centered intro + 5 anchored steps
  (deck switch · the 7 practice modes incl. Produce · DUE/LEARNING/KNOWN stats · flip-and-grade
  FSRS rating · skip + keyboard shortcuts). Each step = what-it-does + a concrete example.
- `src/lib/guide/pageGuideRoutes.js` — `/study` added to the eager `PAGE_GUIDE_ROUTES` seam (lights the header ▶).
- `src/pages/Study.jsx` — 5 `data-guide="study-…"` anchors on the loaded-state controls; the active-card
  `AnimatePresence` is wrapped in an always-mounted `<div data-guide="study-card">` so the arrow resolves between cards.
- `src/components/Layout.jsx` — **theater-mode gap fix:** `/study` sets theater mode while a card is active,
  hiding the header (and its ▶). Added a floating ▶ "Tour this page" pill beside the "Lights On" exit pill,
  gated `theaterMode && hasPageGuide`, calling the same `startPage(location.pathname)`. Mutually exclusive
  with the header ▶; ≥44px; themed tokens only.
- README "Tour this page" line — Dashboard, PDF reader, **and Study** + the theater-mode floating-▶ note.

## Red → green evidence
- New `pageGuides.test.js` `/study` block: **RED** first — `PAGE_GUIDES['/study']` undefined →
  `TypeError: Cannot read properties of undefined (reading 'map')` (2 failed | 8 passed). **GREEN** after
  authoring the entry + routes (10/10).
- e2e `guide-study.spec.js` Test A: **RED** first — header ▶ hidden by theater mode
  (`getByRole('Tour this page')` not visible). **GREEN** after adding the floating theater-mode ▶ (2/2).

## Decision log
- **Guide authored for the LOADED state** (cards present — the normal /study case). Intro is centered so it
  always renders. *Why:* Study's controls only mount with cards; a missing anchor hangs 3 s then skips
  (`waitForElement` timeout). *Veto:* the rare no-card user sees the EmptyState anyway; a `study-empty`
  first step can be added later with zero rework (mirrors T9's foundation/content split).
- **Theater-mode entry via a floating ▶**, not auto-exiting theater mode. *Why:* the guide overlays the
  session and the student resumes when it ends; the arrows already work in theater mode. *Veto:* if a
  dedicated in-session affordance is later wanted, the handler is already shared.
- Two tests that used `/study` as a "no page guide" example re-pointed to `/grammar` (still guideless) —
  intent preserved, not gamed.

## Self-review (hostile)
- Malay regression? None — no new Malay authored; fixtures `rumah`/`makan` are correct + test-only.
- Cross-language leak? None — guide content is bilingual-neutral.
- Broken study mode? The `study-card` wrapper is a class-less div → spacing visually identical; AnimatePresence unchanged.
- Theme? Floating ▶ + content use themed tokens / no color → dark + light identical.
- Content accuracy? Every mode/stat/rating/shortcut claim verified against `FlashcardMode.jsx` + the mode components this cycle.

## Gate result
- `npm run build` — exit 0. `Study` page chunk **27.9 kB** (< 70 kB budget); lazy `pageGuides` **7.57 kB**;
  eager `index` +~0.98 kB (route string + Layout pill JSX — eager wrapper, not a per-route page chunk).
- `npm run test:run` — **1862 passed** (+2).
- `npm run lint` — 0 errors, 3 known warnings.
- e2e (UI-affecting) — `guide-study` 2/2 + `guide-full-page` 4/4 + `guide-pdf-reader` 2/2 +
  `guide-drag-dock` 11/11 + `guide-pause-skip` 1/1 = **20/20**.

## Deploy
Committed to `main` (= prod deploy). RESUME_HERE shipped section + queue line updated in the same commit;
plan doc T10 checked.

## Time check (step 1)
KL local `202606221043`; CUTOFF far-future (forever loop) → no time-box gate. Continue.
