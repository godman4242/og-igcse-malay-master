# Local build-loop report — 2026-06-20 22:34 UTC (KL 2026-06-21 06:34)

**Item shipped:** Content-truth fix (axis-1, HIGHEST) — corrected the confident-wrong
`ijazah` gloss in the CORE Malay→English dictionary.

## Cycle

- **Step 1 (time check):** no finite `CUTOFF` → continue (loop is forever-mode).
- **Step 2:** `git fetch` + `git pull --ff-only` → already up to date on `main` (`7834ade`).
- **Step 3:** GOAL.md read; `RESUME_HERE.md` autonomous queue is **all `[x]`** (empty) →
  Self-source mode. Directed guide epic mechanical tasks T1–T3 fully resolved (T2/T3 shipped,
  T1 deferred no-red-provable-gap) → overnight clamp says NO-OP **unless** a fresh axis-1/axis-2
  gap clears the bar.

## Assessment (3A/3B)

Prior cycle declared the audit state "terminal" but had only audited grammar / Cikgu /
word-family / academic-EN / comprehension surfaces. The CORE `dictionary.js` (825 entries,
seeds every FSRS card + the reversed English deck) had **never** been read by the loop — a
genuine unaudited high-stakes surface. Read all 879 lines.

**Gap found (axis-1, clears the bar):** `'ijazah': 'certificate/degree'` is a verifiably-wrong
vocab pair. "certificate" = `sijil` in Malay (SPM = *Sijil Pelajaran Malaysia*; *sijil
kelahiran* = birth certificate); `ijazah` = the university academic **degree**.

**Web-verified (authority, not memory):** iluminasi.com "Asal-Usul Sijil, Diploma, Ijazah";
unitar.my; ms.weblogographic.com "Perbezaan Antara Sijil dan Ijazah" — all confirm `ijazah`
(degree) ≠ `sijil` (certificate).

**Non-qualifying candidates considered:** `kelmarin` "day-before-yesterday" (dialect-ambiguous,
not a clear gap → left untouched); paper-numbering product decision (HARD invariant, awaiting
Kheshav); guide Phases 4/5/3c (attended-only, clamped out).

## Decision (flagged)

`'certificate/degree'` → `'academic degree'`. Removes the wrong "certificate" synonym, keeps the
correct "degree" core, disambiguates from temperature/angle "degree" in both study directions,
reads naturally as a reversed English headword. Veto note: a precise 2-word gloss beats both the
wrong "certificate/degree" and a bare ambiguous "degree".

## Red → green evidence (TDD)

New `src/data/__tests__/dictionary.test.js` (+2):
1. `DICTIONARY['ijazah'] === 'academic degree'` and contains no "certificate".
2. `DICTIONARY_EN['academic degree'] === 'ijazah'` (red-proofs the reversed-seed regen sync).

Both **RED first** — gloss was `'certificate/degree'`; reversed key was `undefined`. Green after
the edit + `npm run build:en-dict` regen.

## Files

- `src/data/dictionary.js` — `ijazah` value only.
- `src/data/dictionaryEn.js` — regenerated: +1 entry `"academic degree": "ijazah"`, kept
  682→683, slash-drops 95→94 (the old slash-gloss had been dropped by `en.includes('/')`).
- `CLAUDE.md` — headword count 682→683.
- `src/data/__tests__/dictionary.test.js` — new, +2 tests.

No `STORE_VERSION` / schema / free-path / `instruct.js` touch. Existing user cards copy `m`/`e`
at creation → no migration needed.

## Gate

- `npm run build` — OK.
- `npm run test:run` — **1783 passed** (+2).
- `npm run lint` — 0 errors (3 known exhaustive-deps warnings).
- e2e not required (content-only data change, no rendered layout/flow), consistent with the
  ber-kerja / error-mencomel / bel-ajar content fixes.

## Deploy

Committed to `main` (= prod deploy). Confirm Vercel READY on `upg-igcse-malay-master`.

**▶ NEXT:** clamp resumes — NO-OP every cycle unless another fresh axis-1/axis-2 gap surfaces.
The OTHER never-audited content surfaces (`scenarios.js`, `exemplars.js`, `dictionaryExamples.js`,
`speakingTopics.js`) are the only remaining grounded audit leads; no evidence of a gap there today.
