# Local build-loop cycle — 2026-06-15 10:55 UTC

**Mode:** self-sourced (queue empty → GOAL-driven assessment). **KL clock at start:** 202606151844
(no `CUTOFF` set → continue). **Outcome:** SHIPPED one item.

## Item

**Bilingual + pedagogy fix (axis-6 / axis-2): `MixedSession` (the Dashboard "Mix" quick-action) ignored the
global `studyLang`.** It read the full unscoped deck and called `buildMixedSession({cards, grammarCards})` with
no `cardsForLang` scope, so an English learner's Mixed Session mixed Malay + English vocab AND Malay-only imbuhan/
tense grammar drills — breaking the v34 invariant *"Malay & English decks never mix in one session"* — and the
vocab-variant input placeholder/feedback were hardcoded Malay even for English cards.

## Assessment trail (how the gap was found)

Followed the prior Quiz cycle's `▶ NEXT` (re-sweep the v34 interleaved-session surfaces for a parallel
"Malay-pool-for-English-card" distractor leak).

- **Distractor-leak hypothesis → RULED OUT.** `ClozeMode`/`SavedWordCloze` are type-the-answer (no distractor
  pool; `blankInExample` handles multi-word `card.m` too); `SmartSession`'s quiz reuses the now-fixed `QuizMode`.
- **Real gap → MixedSession.** Traced `Dashboard.jsx:805` (Mix button shown unconditionally) → `MixedSession.jsx:26`
  reads `s.cards` (full deck) → `:33` `buildMixedSession` with no `studyLang` scope. Reachable for any English
  learner who also has Malay cards. Grammar pool (`IMBUHAN/TENSE/ERROR/TRANSFORM`) is Malay-only.

## Red → green evidence

```
# BEFORE implementation (RED):
interleave.test.js  lang:"en" excludes grammar  → AssertionError: expected 5 to be +0
mixedSessionLang    studyLang="en" placeholder  → expected 'Type the Malay word...' to be 'Type the English word...'
mixedSessionLang    studyLang="ms" no en leak   → 'Meaning: kapal terbang' rendered (English card in a Malay session)

# AFTER implementation (GREEN):
interleave.test.js + mixedSessionLang.test.js → 20 passed (2 files)
Full suite → 166 files, 1694 tests passed (+5)
```

## Fix (surgical, 3 files + 2 test files + spec)

- `src/lib/interleave.js`: `buildMixedSession({…, lang})` gates the Malay-only grammar pool on `lang !== 'en'`
  (slots fold into vocab/comprehension; `lang` absent/`'ms'` byte-identical).
- `src/components/MixedSession.jsx`: scope cards via `cardsForLang(cards, studyLang)`, pass `lang: studyLang`,
  flip variant placeholder + feedback by `current.item.lang`.
- `src/lib/__tests__/interleave.test.js` (+3), `src/components/__tests__/mixedSessionLang.test.js` (+2, new).
- Spec: `docs/superpowers/specs/2026-06-15-mixedsession-studylang-scope-design.md`.

## Decision log

- Scope MixedSession by `studyLang` + drop Malay grammar for English. *Veto:* wiring English grammar into the
  mixer = separate feature, out of scope. *Veto:* keeping `gTarget=5` for English wastes slots.
- Variant feedback: en → "Correct!"/"Answer:" (matches the comprehension block's English style); ms unchanged.

## Gate

build OK · 1694 tests (+5) · lint 0 errors (3 known exhaustive-deps warnings). No `STORE_VERSION`/schema/
free-path/`instruct.js`/content touch — pure logic + UI labels, nothing to web-verify. MixedSession chunk ~15 KB
(well under the 70 KB per-route budget). No MixedSession e2e exists; the jsdom mount test renders the real
component (placeholder + scoping) — logic/text change, CI runs e2e on push.

## Deploy

Commit on `main` → prod deploy (Vercel). Confirm READY on upg-igcse-malay-master.
