# Local build-loop cycle report — UTC 2026-06-14 14:20 (commit 96c2d7e)

**Mode:** GOAL-driven Self-source (queue was all `[x]`). **Axis:** 1 — correctness & content truth.

## Item shipped

Content-truth fix: the `prefix-ber-asa` Malay grammar drill taught `berasa` ("to feel") from the WRONG
root — `root:'asa'` / `rule:'ber- + asa → berasa'`, treating it as `ber-` + a vowel-initial root. `berasa`
actually = `ber-` + **`rasa`** (root starts with `r`, so `ber-`→`be-`: `be- + rasa → berasa`). The drill
also **contradicted** `GRAMMAR_RULES['ber-']`, which already (correctly) files `berasa` under the
be-/r-initial rule (its example string was garbled: `'bekerja, berasa → berasa'`). Same internal-
contradiction bug class as the prior `penulis` (two rules) and `kejar` (wrong rule) ships — and the
explicitly-flagged `▶ NEXT` thread from both.

`drill.rule` is **shown to the student** (Grammar.jsx) AND keys the elaborative feedback, so this was a
confident-wrong lesson — the worst failure for a learning tool.

## Why this gap cleared the GOAL bar

- **Real:** concrete evidence — `grammar.js:37` (drill) vs `grammar.js:157` (reference table) taught the
  same word two contradictory ways.
- **Measurable Done:** 3 red-proofed `grammar.test.js` assertions that fail on the old data and pass on the
  new (drill root/rule/hint; degarbled reference example; rule resolves to a real `GRAMMAR_FEEDBACK` entry).
- **Verified:** ber- → be- before an r-initial root web-verified at two Malay-grammar sources
  (awalmulamy, malaytuitionsg) AND corroborated by the app's own `cikguKnowledge.js:943` / `aiMocks.js:12`
  / `scenarios.js` usage of `berasa` = "to feel".

## Fix (surgical — 3 data edits + tests)

1. `src/data/grammar.js` drill → `root:'rasa'`, `rule:'be- + r → r drops'`, `hint:'ber- + rasa'`; **answer
   `berasa` unchanged** (no grading change).
2. `src/data/grammar.js` reference example `'bekerja, berasa → berasa'` → `'bekerja, berasa, berenang'`.
3. `src/data/feedbackRules.js` — new `GRAMMAR_FEEDBACK['be- + r → r drops']` (rasa/rehat/renang), so the
   drill's elaborative feedback is grounded; `relatedRule` cross-links the kerja `-er-` case.
4. `src/data/__tests__/grammar.test.js` +3 (red-proofed).

## Decision log

- **Dedicated feedback key vs reuse `be- + kerja`:** dedicated. The kerja entry's text says "kerja" and
  would display wrongly on a `rasa` drill; `rasa` is the distinct r-initial-ROOT case. *Veto:* costs one
  small web-verified entry — worth correct feedback.
- **Fix the root vs swap drill to `berenang`:** fix the root. `berasa` is high-frequency and used across the
  app; preserving it with the correct root is more surgical than replacing the drill's identity.

## Gate result (red → green)

- Red-proof: new block failed 3/3 on pre-fix data (root='asa', example garbled, rule not a feedback key).
- `npm run build` ✓ (eager `index` unchanged — data file) · `npm run test:run` ✓ **1584** (+3) ·
  `npm run lint` ✓ 0 errors (same 3 pre-existing exhaustive-deps warnings).
- e2e skipped by design — string edits in an existing drill/reference list = no layout/flow change; CI runs
  e2e on push.
- Hostile self-review: no Malay grading regression (answer unchanged), no cross-language leak, no theme/
  structural change, no new contradiction (drill + table now agree).

## Deploy

Commit `96c2d7e` → `origin/main` (auto-pushed by the postinstall hook; `origin/main == HEAD == 96c2d7e`).
`main` push = prod deploy. No `STORE_VERSION` bump; no schema/free-path break.

## Step-1 time-check

KL `202606142208` at cycle start; no `CUTOFF` set (forever mode) → continued. One item shipped; cycle ends.

## ▶ NEXT (for the next cycle)

The meN-/peN-/ber- allomorph tables are now internally consistent + guarded by cross-rule invariants.
Strongest remaining axis-1 threads = grounded content audits of the OTHER answer-as-content data files:
`scenarios.js` (model answers), `exemplars.js` (band-6 writing), `comprehensionPassages.js`, and
`grammarEng.js` (English drills — not yet audited for the same bug class).
