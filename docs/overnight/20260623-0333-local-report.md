# Local build-loop cycle report — 2026-06-23 03:33 UTC

## Item shipped
**AXIS-1 content-truth — Cikgu Maya + the Malay comprehension AI prompt taught the WRONG IGCSE Malay 0546 paper numbers (reading & writing).**

Picked from the `RESUME_HERE.md` 🤖 queue: the unchecked *"Malay reading = Paper 1 mislabels (should be Paper 2)"* item. Axis-1 (content-truth) outranks the two unchecked axis-3 guide items per GOAL.md priority.

## Assessment / why the scope is what it is
The queue named only the **reading** mislabel. Grounded assessment (`grep -rn "Paper [0-9]" src/`) found Cikgu's KB uses an **outdated scheme** throughout — Reading=P1, Writing=P2, Speaking=P3 — so the **writing** entries are mislabelled "Paper 2" too (lines 699/702/774/809/842/1068/1071, plus a vocab note at 1277).

Fixing only reading (P1→P2) while leaving writing at "Paper 2" would make reading AND writing **both** read "Paper 2" — a new contradiction. So the coherent axis-1 unit = the whole Cikgu paper-numbering + the Malay comprehension prompt + the bilingual passages corpus.

## Web verification (anti-hallucination gate)
Cambridge IGCSE **Malay – Foreign Language 0546** (2025–27 syllabus PDF + Cambridge International page, re-verified 2026-06-23):
**Paper 1 = Listening · Paper 2 = Reading · Paper 3 = Speaking · Paper 4 = Writing.**
Corroborated by the repo's existing `examPaperLabels.test.js:5-7` header and the 2026-06-22 listening-label commit (6417011).

## Decision log
- **Cikgu = single-syllabus Malay → keep the verified-correct number** (per the 2026-06-22 decision: drop only on bilingual/shared surfaces). Reading→Paper 2, Writing→Paper 4, Speaking→Paper 3 (already correct, untouched). *Veto:* if Cikgu were bilingual, drop the number instead — it isn't (the KB is all Malay grammar/karangan).
- **`comprehensionPassages.js` corpus IS bilingual** (8 `lang:` markers incl. `lang:'en'`) → **drop** the paper number. *Veto:* keep a number only if it were single-syllabus.
- **Keep legacy ids** `exam-paper1`/`exam-paper2` (opaque keys; renaming would churn `related` cross-links) + add comments so the title can't be "fixed" back to match the id. *Veto:* rename only if an id were user-visible — it isn't.
- **Did NOT bundle** `feedback.js` (shared surface → DROP + coupled tests), `dictionary.js` comments, `gemini.js` English (0500-vs-0510 decision), the Writing toggle (live feature) — flagged + queued instead, to stay surgical and avoid a product call. *Veto:* bundle only if they were the same surface — they aren't.

## Red → green evidence
Extended `src/lib/__tests__/examPaperLabels.test.js` with 7 new content-truth guards. Run #1 (pre-fix): **7 failed / 8 passed** — failed for the right reason (content still said the wrong numbers). After the data edits: **15 passed**.

Guards: reading tips title=`Paper 2 (Reading Comprehension) Tips` & answer has no `Paper 1`; writing strategy title=`Paper 4 (Writing) Exam Strategy`; no `TOPICS.PENULISAN` title says `Paper 2`; the 4 named writing-format entries say `Paper 4`; the reading suggestion chip says `Paper 2`, never `Paper 1`; the whole Malay KB file has no `/Paper\s*1\b/`; `COMPREHENSION_SYSTEM` says `Paper 2` not `Paper 1`; the passages corpus carries no `/Paper[\s-]*\d/`.

## Files changed
- `src/data/cikguKnowledge.js` — reading P1→P2, writing P2→P4 (titles/bodies/keywords/patterns/chip/vocab-note/coverage-menu) + legacy-id comments.
- `src/data/systemPrompts.js` — `COMPREHENSION_SYSTEM` P1→P2.
- `src/data/comprehensionPassages.js` — dropped paper number (bilingual).
- `src/lib/__tests__/examPaperLabels.test.js` — +7 guards.
- `RESUME_HERE.md` — item `[x]`, shipped section, 2 new loop-safe follow-up `[ ]` items.
- `docs/loop/GOAL.md` — new needs-Kheshav item (English writing-paper syllabus decision).

## Gate result
`npm run build` ✓ (no chunk over budget) · `npm run test:run` ✓ **1899 passed** · `npm run lint` ✓ **0 errors** (3 known exhaustive-deps warnings). Pure data/content change → e2e not required (loop contract). No `STORE_VERSION` bump, no schema/free-path change, no `instruct.js` API change, no feature deleted, no secrets.

## Deploy
One commit to `main` (= prod deploy via auto-push).

## Step-1 time check
No finite `CUTOFF` set → loop runs to the NO-OP rule. Proceeded normally.

## ▶ NEXT
Clear the `feedback.js` shared-surface paper numbers (now the top axis-1 item in the 🤖 queue) — DROP per the bilingual decision, update the 2 coupled `feedback.test.js` lines.
