# Local build-loop cycle report — 2026-06-20 23:37 UTC (KL 2026-06-21 07:37)

**Commit:** `fix(study): Smart Session micro-prompts speak English in an English session (bilingual parity)`

## Item
Self-sourced **axis-6 (bilingual parity) + axis-2 (pedagogy)** gap. Queue's unchecked
items are empty; the overnight Phase-3b clamp (T1 deferred · T2/T3 shipped) is fully
resolved → GOAL-driven Self-source assessment. The GOAL current-epic rule preserves a
real axis-1/axis-2 win over the clamp; this gap carries a genuine axis-2 (immersion-
consistent production scaffolding) reading alongside the axis-6 parity break, and the
fix is purely functional/testable — the exact class the clamp deems safe-unattended.

## Assessment that found the gap (grounded, not from memory)
Audited the two remaining `▶ NEXT`-flagged un-audited learner-facing data files:
- **`topics.js`** (13 topic packs, ~270 Malay vocab words) — read-audited every word
  for spelling + categorisation → **CLEAN** (all valid, correctly-spelled DBP Malay).
- **`microPrompts.js`** (Smart Session production prompts) — **found the gap.** Its
  header comment asserted "the app has NO English vocabulary … a true English study
  mode is a separate epic." That premise is now **false** (True English study mode
  shipped v34). Traced the data flow:
  - `getRandomPrompt` (`microPrompts.js`) is all-Malay.
  - `interleavedQueue.js:29,39` → `getRandomPrompt('writing'/'speaking', card.m)` with no lang.
  - `useInterleavedSession.js:72` scopes cards by `studyLang` (`cardsForLang`).
  - `WritingMicroPrompt.jsx:73` / `SpeakingMicroTurn.jsx:125` render `{prompt}` directly.
  ⇒ When `studyLang==='en'`, an English vocab card produces a **Malay instruction**
  ("Tulis satu ayat lengkap menggunakan perkataan 'achieve'.") above the English focal
  word — the one production-scaffolding surface left un-converted after v34, while
  TypeMode/QuizMode/ProduceMode (F5 Increment 7) already speak English to English cards.

## The gap
Smart Session **Micro-Write** and **Micro-Speak** tasks show Malay production prompts to
an English (0510 ESL) learner. Real MS/EN parity break (axis 6) + degraded immersion-
consistent production scaffolding (axis 2). Reproducible via `studyLang='en'` + Smart Session.

## Fix (surgical, additive — Malay byte-identical)
- `src/data/microPrompts.js` — added `TEMPLATES_EN` (English mirrors) + a `lang`
  param on `getRandomPrompt(category, headword, lang='ms')`; `SETS[lang] || TEMPLATES`
  so unknown/undefined `lang` falls back to Malay. Replaced the now-false header comment
  with the v34 per-language-purity invariant.
- `src/lib/study/interleavedQueue.js` — threaded `lang` through
  `microWriteTask`/`microSpeakTask`/`buildCycle`/`buildSession` (default `'ms'`).
- `src/hooks/useInterleavedSession.js` — `buildSession({ …, lang: studyLang })`; added
  `studyLang` to `startSession`'s dep array (no new exhaustive-deps warning).

**No STORE_VERSION bump:** the in-flight session persists to a separate
`smart-session-state` localStorage key, not the Zustand store (a stale session resumes
with old prompts, self-clears after 2h). No schema / free-path / `instruct.js` impact.

## Content verification (English text)
English templates are trivially-correct IGCSE 0510 English, mirroring the Malay intent:
- writing: "Write one complete sentence using the word …" / "Build a sentence that
  describes an everyday situation using …" / "Use … in a complex sentence that shows
  cause or effect."
- speaking: "Say one sentence using … — speak clearly." / "Give a spoken example about
  … from everyday life."
Malay templates left byte-identical (already verified correct).

## TDD (red → green)
+8 tests, red-proofed first:
- `src/data/__tests__/microPrompts.test.js` (+5) — English writing/speaking templates
  all-English (no Malay markers, contains the word); `getRandomPrompt(...,'en')` across
  the full index walk; `ms`/omitted == byte-identical to the Malay path; unknown-lang
  fallback. Updated the stale Issue-4b comment to the v34 bilingual reality.
- `src/lib/__tests__/interleavedQueue.test.js` (+3) — `buildCycle`/`buildSession` emit
  English micro-prompts under `lang:'en'`, Malay under default/`'ms'`.

Red proof: **5 failed | 38 passed** before implementation (`TEMPLATES_EN` undefined +
prompts still Malay); after: both files green.

## Gate
- `npm run build` — exit 0 (PWA generated)
- `npm run test:run` — **1798 passed** (was 1790; +8)
- `npm run lint` — 0 errors (3 known exhaustive-deps warnings; no new one)
- pre-commit quality gate (build → tests → lint → content) — runs on commit

UI-affecting? Pure data/logic prompt-text change inside an EXISTING rendered control
(no new screen/control/layout/flow) → e2e not required (same precedent as the
TypeMode/QuizMode F5 label fixes). Not a new mode/route/capability → README + tour not
required (Smart Session already documented).

## Decision log
- **Decision:** convert Smart Session production prompts to be language-aware so an
  English session shows English instructions. **Why:** real axis-6 parity break + axis-2
  pedagogy gap; the app's own F5 decision (English study-mode instructions in English)
  and the prompt's stated immersion rationale both demand it; purely functional/testable
  ⇒ safe-unattended. **Veto note:** a Malay-L1 learner *can* read the Malay prompt (not
  strictly "blocking"), but the inconsistency contradicts shipped behaviour and the fix
  is small, additive, and revertible — converting it is correct + low-risk.
- **HARD-invariant screen:** no paywall / individual-revision / native-app / STORE_VERSION
  / schema / free-path / `instruct.js` / feature-deletion / secrets impact. All clear.

## Deploy
Pushed to `origin/main` → Vercel prod auto-deploy (upg- + og-).

## Time check (step 1)
KL `202606210737`. `CUTOFF` is far-future (forever) → continue; no time-box hit.

## ▶ NEXT
Overnight clamp resumes — Phase 3b (T1–T3) stays resolved; NO-OP every cycle unless
another fresh evidenced axis-1/axis-2 (or, like this one, axis-6-with-pedagogy) gap
surfaces. Phases 4/5/3c remain attended-only. **Ledger now also includes** `topics.js` ✓
(clean) + `microPrompts.js` ✓ (converted). Possible future spot-check: whether any other
Malay-only learner-facing string leaks into an English session (`aiMocks.js` canned
replies, `systemPrompts.js`) — no concrete evidence of one today; don't invent it.
