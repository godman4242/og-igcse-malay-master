# Local build-loop report — 2026-06-14 14:26 UTC

**Cycle:** GOAL-driven self-source (queue empty) · **Axis 1 — correctness & content truth**
**KL clock at step 1:** 202606142226 · cutoff = far-future (forever) → continue.

## Item shipped

Content-truth fix: the `kesihatan` comprehension passage (`src/data/comprehensionPassages.js`)
mis-keyed the affix on **`memakan`** as `B) meN-...-kan` (`correctIndex: 1`) with a fabricated
*"(-kan implied transitive)"* explanation. The word is **`meN-` + `makan`** with **no suffix**
(m-initial root → me- no-change allomorph, like the app's own `memasak`), so the correct option is
`A) me-`. Because `correctIndex` is the graded key, a learner picking the right answer was marked
wrong and shown a false rule.

## Assessment (gap list against GOAL axes)

- **Axis 1 (picked):** audited `grammarEng.js` → clean (tenses/SVA/articles/confusables/find-error/
  transforms all correct); `grammar.js` → internally consistent after the recent meN-/peN-/ber-
  fixes; `comprehensionPassages.js` → **1 wrong graded key** (the memakan affix). Evidence: real
  `file:line` + a reproducible grading defect + web-verified morphology.
- Other axes: no clearly-evidenced gap above the bar surfaced this cycle that outranks an axis-1
  confident-wrong key in a graded surface.

## Decision log

- **Decision:** flip the key to the already-present correct option `A) me-` (correctIndex 0) and
  rewrite the explanation; leave word/options/passage untouched.
- **Why:** the word is fixed by the passage and `me-` is the genuinely-correct affix among the
  options → minimal, surgical correction.
- **Veto note:** considered swapping the asked word to one that truly takes meN-...-kan
  (`menghabiskan`) — rejected as a needlessly larger diff.

## Red → green evidence

- RED (fix `git stash`-ed): 2 fix-specific tests fail — `correctIndex` resolved to `B) meN-...-kan`;
  explanation contained the `(-kan` fabrication. The 3 integrity/existence tests passed (non-vacuous).
- GREEN (fix applied): all 5 tests pass.

## Gate

- `npm run build` → green (data-only; `index` chunk unchanged).
- `npm run test:run` → **1589 passed** (+5), 154 files.
- `npm run lint` → 0 errors, 3 pre-existing exhaustive-deps warnings.
- e2e skipped by design (string edit in existing data — no layout/flow change; CI runs e2e on push).

## Web verification

- memakan = prefix `me-` + makan, no `-kan` suffix:
  https://www.kompasiana.com/suprihadi48660/6330ccc34addee4d724b3e82/pemberian-imbuhan-pada-kata-makan
- Corroborated internally by `grammar.js:145` (`memasak`/`menanti` no-change rule).

## Deploy

One commit to `main` (= prod deploy). Plan: `docs/superpowers/plans/2026-06-14-comprehension-memakan-affix-fix-plan.md`.

## ▶ NEXT

Remaining axis-1 content-truth threads (none audited yet for wrong answer-bearing content):
`scenarios.js` (roleplay model answers), `exemplars.js` (band-6 writing), `listeningPassages.js`.
