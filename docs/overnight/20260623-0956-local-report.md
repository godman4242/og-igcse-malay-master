# Local build-loop cycle report — 2026-06-23 09:56 UTC (17:56 KL)

**Mode:** GOAL-driven Self-source mode (🤖 queue empty; directed Full-Page-Guide epic Phase 3c COMPLETE).
**Item shipped:** GOAL loop-safe #4 — **a doc-less source can never render the blank Layout view** (axis-1 robustness).

## Step-1 time check
No finite `CUTOFF` set (forever loop) → continue. KL `202606231756`.

## Assess → pick (3A/3B)
- 🤖 queue: **0 unchecked** items (all `[x]`). Directed epic (Full Page Guide, Phase 3c) COMPLETE — every route has a working ▶ (T25 Settings was the last).
- Ranked the open **loop-safe queue** by GOAL axis priority. The two axis-1 robustness items (#4 doc-less Layout guard, #6 Prev/jump re-entrancy) outrank #1 ASR→Worker (axis-4) and #3 AI-tier eval (tooling).
- **#4 chosen** (higher severity than #6: a stuck/blank Layout is a broken *study surface* = core read path; #6 is a guide-overlay race).
- No fresh axis-1/axis-2 **content** gap found that clears the bar this cycle (recent cycles already triaged the paper-number items → resolved or parked as needs-Kheshav).

## Anti-hallucination gate
- **Real:** confirmed in live code — `LayoutView.jsx:58` sets `model=null` when `!doc`; `:214` renders the `"Rendering pages…"` loader forever while `!model`. Three doc-less producers (`PDFReader.loadSample:365` / `runImageOcr:419` / `runAudioTranscribe:499`) each manually `setView('reflow')` — fragile (a future producer forgetting = how Bug A first happened).
- **Measurable Done:** a failing unit test that turns green + an e2e pin that a doc-less source with `layoutView=true` renders reflow (no stuck loader).
- **Verified:** no Malay/English content touched — structural only.

## What shipped
- **New pure module** `src/lib/readerView.js` — `effectiveReaderView(view, hasDoc)` → `'layout'` iff Layout selected AND a doc exists, else `'reflow'`. (Mirrors the `readerKeymap.js`/`gestureModel.js` pure-dispatcher convention.)
- **Wired into `PDFReader.jsx`:** `const viewSafe = effectiveReaderView(view, !!pdfDoc)` computed once; all 8 render reads (page branch, Reflow/Layout toolbar highlight, Sentences toolbar gate, `activeTokens` memo, tip footer) now read `viewSafe`. WRITE path (`switchView`/`setView` + `layoutView` pref) untouched. The 3 band-aids kept (belt-and-suspenders; keep `view` STATE coherent → purely additive diff).
- **Why a render-time guard, not an effect:** an effect renders one bad frame with `doc={null}` first (the stuck loader flashes). Render-time fallback = no bad frame.

## TDD red → green
- `src/lib/__tests__/readerView.test.js` (4 cases). RED proof: `Error: Cannot find module '../readerView'`. GREEN after implementing. Load-bearing case: `effectiveReaderView('layout', false) === 'reflow'`.
- e2e regression pin `tests/e2e/reading-sample.spec.js` › *"doc-less sample stays in Reflow even when the Layout pref is on (Bug A)"* — seeds `layoutView=true`, reloads (so `view` inits to `'layout'`), loads a sample, asserts reflow tokens + the `"Rendering pages…"` loader never appears. (First test exercising the doc-less + Layout-pref combo end-to-end. Passes pre- and post-fix due to the band-aids → documented as a *regression pin*; the unit test is the red→green proof of the guard.)

## Gate
- `npm run build` → exit 0. PDFReader chunk **79.12 kB** (gz 23.26) vs **79.05 kB** clean (stash-measured) = **+0.07 kB** (import only). Documented per-route exception; baseline already >77 kB from prior cycles.
- `npm run test:run` → **1931 passed** (+4), 184 files (+1), 0 fail.
- `npm run lint` → 0 errors (3 known exhaustive-deps warnings).
- **e2e (UI-affecting render path → ran locally):** `reading-sample` 3/3 (incl. new pin), `audio-transcribe` 6/6 (doc-less audio reflow path), `pdf-layout` 19/20, `pdf-replace-viewswitch` + `reader-keyboard` green.
  - ⚠️ Lone red `pdf-layout.spec.js:174` "GO WILD … swap keeps the bucket" — **fails identically on clean `main`** (verified by `git stash`): a pre-existing double-click-zoom + drag + view-swap timing flake. NOT a regression — that path always has a live `pdfDoc`, so `viewSafe===view` (my change is a provable no-op there).

## Self-review (hostile)
Malay regression — none (no content). Cross-language class leak — none (no class change). Broken study mode — no (Layout-PDF path byte-identical; doc-less paths reflow exactly as the band-aids already did). Dark/light — identical (no color token). Missed `view` read — audited, all render reads route through `viewSafe`; write path keeps `v`/`view`.

## Invariants
No `STORE_VERSION`/schema/free-path/`instruct.js` change; no feature deleted; no secrets. Pure-internal robustness fix to an existing surface → README/tour ship-contract N/A (exempt).

## ▶ NEXT
- GOAL loop-safe **#6** — extend the Next re-entrancy guard to `handlePrev`/`jumpTo` (`guideController.js`) — remaining open axis-1 robustness item.
- Also open: #5 page-tour empty-state hang audit (axis-3/1), #1 ASR → Web Worker (axis-4).
- A fresh axis-1/axis-2 **content** gap still preempts if one clears the bar.
