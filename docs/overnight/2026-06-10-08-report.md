# Overnight run report — 2026-06-10 08:xx UTC (scheduled cloud session)

Scheduled 5-hourly cloud run. All four skip guards checked and passed
(no `overnight/*` branch on the remote; the 90-minute main-activity guard was
waived for this supervised test run; kickoff queue had 3 live items; zero
unreviewed reports — `docs/overnight/` did not exist yet). Executed the
RESUME_HERE.md kickoff box in decide-and-flag mode.

## Chunks completed

### 1) aiText prefers user keys — SHIPPED
`callTextAI` order is now **user OpenRouter key → user Gemini key → server
Gemini proxy**. The Gemini rung calls `callGeminiByok` from the shipped BYOK
adapter (`src/lib/instructProviders/gemini.js`) — client-direct on the native
endpoint, never our `/api/gemini` proxy. The shipped user-OpenRouter-first
behavior and the server-fallback contract are unchanged; keyless users behave
byte-identically. An `AbortError` from the user-Gemini rung propagates instead
of retrying on the server. +5 unit tests (`aiText.test.js`, 8 total).

### 2) Quality-translate fork — DECIDED: ADDITIVE, and SHIPPED
- `src/lib/translate/providers/instructTranslate.js` (new): the numbered-list
  prompt/parse/per-word-fallback core extracted from the openrouter provider
  as `makeInstructTranslator()`; `parseNumberedList` moved here and re-exported
  from `openrouter.js` (all 12 existing provider tests pass unchanged).
- `src/lib/translate/providers/geminiByok.js` (new): quality-translate on the
  user's Gemini key (`providerId: 'gemini'`; availability = USER key only).
- `src/lib/translate.js`: quality `providerOrder` = `openrouter` (env or user
  key, unchanged) → `gemini` (user key) → free MT chain; the quality cache
  namespace (`writeNsFor`) now covers `gemini` results, preserving the
  READ-vs-WRITE namespace split (a degraded gtx gloss never poisons `'q'`).
  New export `hasQualityTranslateKey()` = user OpenRouter OR user Gemini key.
- `src/pages/PDFReader.jsx`: the "Higher quality" pill + the FullTranslation
  `hasKey` prop now gate on `hasQualityTranslateKey()` (copy updated from
  "OpenRouter key" to "AI key").
- +14 unit tests (7 new provider suite, 7 added to `translate.test.js`,
  including: gemini-only routing, openrouter→gemini→gtx degrade order, free
  path never touches gemini, `'q'`-namespace isolation + no-poison retry, and
  env-key-never-lights-the-toggle).

### 3) Multimodal Design & Research doc pair — SKIPPED (deliberately)
The chunk depends on the `project_multimodal_direction` memory, which lives in
Kheshav's local Claude memory and is **not accessible from this cloud
environment** (the repo only contains backlog pointers to it). Writing the
spec/plan pair without the source direction would invent the epic's scope and
anchor future sessions on made-up requirements. Left as the next session's
queue item (a local session with memory access should run it).

## Decision log (decision / why / how to veto)

1. **Quality-translate fork = ADDITIVE (not leave-as-is).**
   Why: learning quality ranks above convenience in the criteria stack; Gemini
   free-tier keys are the most common BYOK path (no card needed); yesterday's
   router shipped precisely to give "more ways in" — leaving quality-translate
   OpenRouter-only recreates the single-provider gap it fixed. Free path stays
   byte-identical (env path untouched; keyless users still never see the pill).
   Veto: revert the `translate.js` + `PDFReader.jsx` gate edits — the new
   provider modules are inert without them.
2. **Gemini only as the new quality provider; Ollama NOT added.**
   Why: Ollama is desktop-only, weaker at Malay (router spec R9), and each
   provider added to the translate chain grows the UI-gate/test surface; the
   factory makes adding it later a ~20-line change. Veto: say the word — it's
   one `makeInstructTranslator` call + one `providerOrder` line.
3. **Extracted `instructTranslate.js` instead of copy-pasting the openrouter
   provider.** Why: the prompt/parse/fallback logic is behavior-pinned by 12
   tests; duplicating it would fork future fixes. The openrouter module keeps
   its exact public exports. Veto: none needed — pure refactor, tests unchanged.
4. **`callTextAI` user-Gemini rung rethrows `AbortError` (only deviation from
   pure insertion).** Why: a user-cancelled call must not be retried on the
   server; non-abort failures still fall through (no dead end). Veto: drop the
   two-line rethrow.
5. **Pill copy "your own OpenRouter key" → "your own AI key".** Why: the gate
   is now either key; naming one provider would be wrong. Veto: trivial revert.
6. **Did NOT force-push main to restore one-commit-per-chunk** (see honesty
   section). Why: prod-deploy branch; rewriting published history ranks worse
   than an imperfect commit message. Veto: n/a (history is already public).
7. **Skipped chunk 3** (see above). Veto: run it from a local session.

## Gate + test output

- `npm run build` — clean; `index-*.js` 453.44 kB / 145.52 kB gz (within the
  documented ~451/145 bars); PDFReader and provider chunks within limits.
- `npm run test:run` — **772 passed / 0 failed (57 files)**; baseline was 753,
  +19 from this run.
- `npm run lint` — **0 errors**, only the 3 pre-existing exhaustive-deps
  warnings (RoleplayScorecard / Comprehension / Roleplay).
- The pre-commit hook re-ran build→test→lint on the shipping commit and passed.

### e2e — honestly: NOT run
`npx playwright install chromium` fails in this environment: the network
allowlist blocks `cdn.playwright.dev` (HTTP 403 "Host not in allowlist"), and
no cached browser exists in the container. The changed behavior is covered by
the +19 unit tests; the BYOK-quality e2e spec (`byok-quality-translate.spec.js`)
should still pass locally since the OpenRouter path is byte-identical, but a
**gemini-only-key quality-translate e2e case does not exist yet** — flagged in
the refreshed kickoff as an optional fast-follow.

## Push path — flagged anomaly (read this)

Intended: one commit per chunk. Actual: **both chunks shipped in ONE commit
(`a273b2c`)** whose message describes only chunk 1. Cause: the repo's
`.githooks/pre-commit` begins with `git add -A` (auto-stage everything) and
`post-commit` auto-pushes — my first chunk-1 commit therefore swept in the
chunk-2 working files and pushed before the split could be enforced. I reset
locally, rebuilt the split, and the second attempt was rejected non-fast-forward
because the combined commit was already on origin. Resolution: verified the
remote commit's tree is **byte-identical** to the intended final state (empty
`git diff a273b2c --cached`), then reset local main onto it rather than
force-pushing a prod branch. So: **clean push happened (via the auto-push),
content is exactly as tested, but the chunk split exists only in this report,
not in history.** Lesson for future scheduled runs: with this hook pair,
physically remove later chunks from the working tree BEFORE the first commit.

## Deploy

Vercel project `upg-igcse-malay-master` (prj_yQt1WImzPvpkOQYdj2XHqxqnIwqC):
latest production deployment `dpl_9MfbFSzX4q7e4hUiGLunDzRtUuzq` for commit
`a273b2c` is **READY** (confirmed via the Vercel MCP).

## Skipped / not done

- Chunk 3 (multimodal doc pair) — see above.
- e2e — environment-blocked, see above.
- Light/dark screenshots — require the Playwright browser; same blocker. The
  only visual change is the existing pill's gate + title text (no new UI).
