# Local build-loop cycle report — 2026-06-14 12:00 UTC (20:00 KL)

## Item shipped

**Pure-lib test coverage — `src/lib/pronunciation.js` (Speak-mode scorer) pinned.** Self-sourced
(queue empty): the pre-thought item from the `interleave.js` pin's `▶ NEXT` thread. Behaviour-
preserving — `pronunciation.js` is byte-identical; this adds a test file only.

- Plan: `docs/superpowers/plans/2026-06-14-pronunciation-test-coverage-plan.md`
- Test: `src/lib/__tests__/pronunciation.test.js` (+17)

## Why this item (decision log)

- **Decision:** Pin `pronunciation.js` over `feedback.js` / `patterns.js` (the other two named in the
  `▶ NEXT` thread).
- **Why:** `pronunciation.js`'s core (`scorePronunciation` and its tip logic) is fully deterministic
  and **pedagogy-central** (it scores the Speak study mode); `feedback.js`'s `buildSessionFeedback` is
  time-dependent (`Date.now()`/`new Date()`), harder to pin deterministically. Criteria stack: this is
  the highest-safety cycle — no app behaviour change, no STORE_VERSION/schema/free-path/frozen-API
  touch. The doc explicitly endorses behaviour-preserving test coverage as a legitimate self-sourced
  cycle ("an idle, honest cycle beats prod churn").
- **One-line veto:** if `scorePronunciation` had already been tested elsewhere, switch to `feedback`'s
  pure trio — confirmed via grep it was not.

## Red → green evidence

1. New `pronunciation.test.js` written → ran alone → **17/17 green**.
2. Red-proof (non-vacuity): mutated **four** SUT behaviours at once —
   - `close` branch `score += 0.5` → `+= 1`
   - missing-word `spk || '—'` → `spk`
   - tips `.slice(0, 3)` → `.slice(0, 4)`
   - example filter `c.ex.length > 5` → `>= 5`

   → **exactly the 4 matching tests failed** for the right reasons:
   ```
   × 1 edit on a 5-char word is "close" … → 50%
   × a missing spoken word is "wrong" and renders spoken as "—"
   × caps tips at 3 (four distinct patterns ny/ng/r/kh → only 3 returned)
   × keeps only cards whose example is longer than 5 chars
   Tests  4 failed | 13 passed (17)
   ```
   The other 13 tests (which don't touch those paths) stayed green.
3. Restored via `git checkout -- src/lib/pronunciation.js` → `git diff --stat` shows **zero diff**
   (byte-identical) → re-ran → **17/17 green**.

## Gate result (step 5)

- `npm run build` — green (SW generated, 128 precache entries; `index` chunk unchanged).
- `npm run test:run` — **1458 passed** (147 files); was 1441 → **+17**, matches the new tests.
- `npm run lint` — **0 errors**, the 3 known pre-existing exhaustive-deps warnings only
  (Comprehension.jsx, Roleplay.jsx, RoleplayScorecard.jsx); 0 new.
- e2e: not run — pure logic/test-only change, no UI render (per step 5, CI covers it on push).

## Self-review (step 6, hostile)

Malay regression / broken study mode? No — SUT byte-identical. Cross-language class leak / dark-light
theme? N/A — no JSX/CSS. Unverified content? Test fixtures use real Malay words; all expectations are
hand-computed and red-proofed; nothing ships to users. No STORE_VERSION/schema/free-path/`instruct.js`
touch.

## HARD invariants

All held: no STORE_VERSION bump, no Supabase-schema or free-path change, no feature deleted,
`instruct.js` public API untouched, no secrets, working tree limited to this one item before commit.

## Deploy

Committed to `main` (= prod deploy). Step-1 time check at cycle start: `202606142000` KL (8pm) —
before the `202606142300` (11pm) cutoff. Re-checked the clock before committing.
