# Local build-loop cycle report — 2026-06-14 12:36 UTC (732793b)

**Mode:** local build loop, single cycle (steps 1–8). **Queue:** empty → **Self-source mode**.
**Cutoff:** 23:00 KL (`202606142300`). **Time check at start:** `202606142027` (20:27 KL) — ~2.5h margin.
**Re-check before commit:** `202606142033` — within margin. Cutoff not reached.

## Item shipped (self-sourced)

**Pure-lib test coverage — `src/lib/writingFormats.js`** (the IGCSE writing format catalogue split out of
`writingGrader.js`; 3 consumers — writingGrader, Dashboard `RecentPerformance`, `MistakeJournal`; no
prior test file). Behaviour-preserving: `writingFormats.js` is **byte-identical** (tests only).

Continues the `interleave→pronunciation→feedback→patterns` pure-lib coverage thread. Picked over `json.js`
on blast radius (3 consumers vs 1) + richer surface (pure fn + derived map + data catalogue).

- Plan: `docs/superpowers/plans/2026-06-14-writing-formats-test-coverage-plan.md`
- Test: `src/lib/__tests__/writingFormats.test.js` (**+14**)

## Coverage

- **`listFormats` (5):** no-arg → all 27 (the `!lang` short-circuit); falsy lang (`undefined`/`null`/`''`)
  → all 27; `'eng'` → 13; `'malay'` → 14; unknown `'french'` → `[]`.
- **`FORMATS_BY_ID` (3):** `Object.keys().length === FORMATS.length === 27` (pins id uniqueness); known id
  maps **by reference**; absent id → `undefined`.
- **`FORMATS` data integrity (6):** 13 EN + 14 MS = 27; non-empty `id`+`label`; `lang` ∈ {eng, malay};
  `0 < minWords < maxWords` (numbers); non-empty marker/hint arrays of non-empty strings; id-prefix↔lang
  (`eng-*`⇒eng, `ms-*`⇒malay). Counts are hand-typed literals (not `FORMATS.length`-derived).

## Red → green evidence (TDD non-vacuity)

1. Wrote tests → ran new file → **14/14 green** against the real SUT.
2. Mutated SUT two ways at once: removed `listFormats`'s `!lang` guard (`f => !lang || f.lang===lang` →
   `f => f.lang===lang`) AND changed `eng-email`'s `lang` to `'english'`.
3. Re-ran → **exactly 6 failed**: the 2 falsy/no-arg `listFormats` tests (broken guard) + the
   eng-count / total-count / lang-enum / id-prefix tests (broken format). The other 8 stayed green.
4. `git checkout src/lib/writingFormats.js` → **zero diff** → re-ran → 14/14 green.

## Decision log

- **Self-source pick: `writingFormats.js`** — highest-value untested pure lib by blast radius + surface.
  *Why:* 3 consumers incl. the writing grader; bundles `listFormats` (logic) + `FORMATS_BY_ID` (derived) +
  catalogue (integrity guards). *Veto:* `json.js` (`tryParseJSON`) is queued as the next thread target.
- **Assert exact counts 13/14/27 as literals** — load-bearing; a count drift is a real catalogue change a
  reviewer should see. *Veto:* if a format is legitimately added later, the literal updates with it.
- **Self-review fix:** removed a stray dead `expect(...).toBe` line before running.

## Gate

`npm run build` ✓ (index unchanged) · `npm run test:run` ✓ **1532** tests (+14) · `npm run lint` ✓ 0
errors (3 known exhaustive-deps warnings). e2e skipped — pure data/logic, no UI surface (CI covers on push).
No STORE_VERSION bump; no schema/free-path/`instruct.js` change.

## Deploy

Commit `732793b` on `main` → auto-pushed to `origin/main` ("Everything up-to-date") → prod deploy.

## Next

Re-add a `[ ] Pure-lib test coverage` queue item for **`json.js`** (`tryParseJSON`) — subtle greedy
`{...}` extraction + array-passthrough-via-`typeof`, load-bearing for AI JSON parsing.
