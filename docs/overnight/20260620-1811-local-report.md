# Local build-loop cycle — 2026-06-20 18:11 UTC (KL 2026-06-21 02:11)

## Item
**Directed epic (Full Page Guide), plan T3 — in-box ▶ "go deeper" button.**
From any running Quick/Full tour (or a page guide), a ▶ inside the guide popover drops the learner
straight into the Full Page Guide for the route they're on. Hidden where there's no page guide (never a
dead button). Closes Phase 3a's deferred "in-popover ▶".

## Why this item (GOAL assessment)
- Queue empty (all `[x]`) → GOAL-driven Self-source mode, with the Kheshav-approved **directed axis-3 epic**
  active (Full Page Guide). No fresh axis-1 (content) or axis-2 (pedagogy) gap cleared the bar — content +
  a11y were swept clean across the last ~15 cycles. So the pick = the next unchecked plan task.
- **Evidence before TDD:** the plan lists T1 ("footer never wrap") first, but a 390×844 Playwright probe of
  the live undocked footer showed it does **not** wrap/overflow with the existing 4 controls (footer
  `scrollH==clientH==46`; nav `scrollW==226` in a 318px popover). So T1 is **not a red-provable gap**
  standalone — the wrap only appears once a 5th control crowds the footer. Built **T3** instead (a real new
  capability, fully red→green) and put its ▶ in a **header row**, not the footer → footer stays un-crowded,
  T1 deferred to the genuine crowding case (the docked 220px box = T2).

## Red → green evidence
- New unit tests RED first: `npx vitest run popoverDecorations guideController` →
  **8 failed / 35 passed** (the 8 fail for the right reason: no `canGoDeeper`/`onGoDeeper`/header-row/▶
  button yet; the 2 "does-NOT-add" absence cases trivially passed pre-impl).
- After implementing `syncGoDeeper` + controller `canGoDeeper()`/`goDeeper()` + `useGuide` ref wiring + CSS:
  **43 passed (43)** for those two files.

## Decision log
- **Header placement, not footer** (decide-and-flag). Why: worst-case footer (explore-mode "▶ Resume" +
  "12 of 22" + 5 controls ≈ 331px) overflows the 318px popover and would force invasive manipulation of
  driver.js's own Back/Next internals to collapse labels. Header sidesteps it + groups "meta" controls
  (move ⠿ + go-deeper ▶) away from tour navigation. Veto note: a YouTube-style "▶ beside Pause" footer slot
  is a later T1/T2 footer-hardening task.
- **▶ present in a page guide too** — per R2 "works from a page guide"; tapping restarts that page's guide
  (harmless, spec-compliant).
- **`useRef` for `onGoDeeper → startPage`** — avoids a useCallback dependency cycle and exhaustive-deps churn.
- **No STORE_VERSION bump** — user-initiated, no persisted state. No schema / free-path / `instruct.js` /
  Malay-content touch → nothing to web-verify.

## Gate result
- `npm run build` — OK (3.91s). Index **466 KB raw** (no eager growth — controller stays lazy). All
  per-route chunks within budget / documented exceptions.
- `npm run test:run` — **1766 passed (+10)**.
- `npm run lint` — **0 errors**, 3 known exhaustive-deps warnings (RoleplayScorecard / Comprehension /
  Roleplay) — unchanged.
- e2e (UI-affecting) — `guide-full-page` + `guide-pause-skip` + `guide-drag-dock` → **7/7 passed** against
  the production preview build (3 new in-box ▶ tests + Phase 1/2 regression intact).

## Self-review (hostile pass)
No Malay/content change; no cross-language class leak (CSS is `.guide-theme`-scoped); drag-dock + pause
unaffected (recursive selectors still find the moved grip; e2e confirms); dark+light resolve (accent tokens
in `THEME_VARS`); ▶ is keyboard-reachable with aria-label + 44×44 target; `canGoDeeper` pinned in sync with
`PAGE_GUIDES` so the ▶ never strands on a guideless route.

## Files
`src/lib/guide/popoverDecorations.js`, `src/lib/guide/guideController.js`, `src/hooks/useGuide.js`,
`src/index.css`, `tests/e2e/guide-full-page.spec.js`, `src/lib/guide/__tests__/popoverDecorations.test.js`,
`src/lib/guide/__tests__/guideController.test.js`, `README.md`, `RESUME_HERE.md`,
`docs/superpowers/plans/2026-06-21-guide-full-page-rollout-plan.md`.

## Deploy
Committed to `main` → prod deploy. (Confirm Vercel READY on upg- after push.)

## Step-1 time check
No finite CUTOFF set (forever loop). KL time at cycle start: 202606210151 — well within bounds; proceeded.

## ▶ NEXT
**T2 — minimize-to-icons when docked (R4)** is the next plan task AND the real home for T1: at the docked
220px width the footer genuinely crowds, so hide the footer control *labels* (keep icons) when
`guide-docked`, re-expanding on hover/focus + double-click (R5d). Then Phase 4 (dock-v2 along-edge) / Phase
5 (built-in samples) / Phase 3c (per-page content, one page per commit, hand-verified).
