# Local build-loop report — 2026-06-22 02:27 UTC (10:27 KL)

## Item
Directed epic axis-3 (Full Page Guide) — plan Phase 3c, **T9 increment 2: the PDF reader deep-dive GUIDE CONTENT**. The header ▶ "Tour this page" now works on `/pdf-reader` (previously Dashboard-only), walking a student through every meaningful reader control with an arrow + plain-English "what it does + example".

## Why this item (assessment)
- Queue in `RESUME_HERE.md` was all `[x]`; entered GOAL-driven Self-source mode.
- GOAL.md has an active DIRECTED epic (Full Page Guide) that overrides axis-3 gap discovery; a fresh axis-1/axis-2 gap would still win, but the converged content ledger (prior cycles) holds and nothing new cleared the bar.
- The explicit `▶ NEXT` thread from T9 increment 1 + the epic's #1 GOAL = the PDF reader deep-dive content, PDF reader FIRST (Kheshav's worked example).

## Anti-hallucination gate
- **Real:** increment 1 left the sample-load prerequisite + an explicit NEXT spec; the header ▶ did not work on `/pdf-reader` (PAGE_GUIDE_ROUTES was `['/']` only).
- **Measurable Done:** failing unit tests (the `/pdf-reader` guide + its 8 anchors) turn green; new e2e proves the ▶ launches + every loaded-state anchor exists.
- **Verified:** no new Malay authored — English UI copy; the Malay examples ("jam tangan"=watch, "penduduk"=resident) are already vetted in-app. Every control claim grounded in the code read this cycle.

## Red → green
- `src/lib/guide/__tests__/pageGuides.test.js` +3 tests. RED first: `/pdf-reader` undefined in `PAGE_GUIDES` → `TypeError: Cannot read properties of undefined (reading 'map')` (3 failed / 5 passed). GREEN after adding the guide entry + anchors (8 passed).

## Files
- `src/lib/guide/pageGuides.js` — new `/pdf-reader` entry (9 steps: intro + 8 anchored).
- `src/lib/guide/pageGuideRoutes.js` — `PAGE_GUIDE_ROUTES` += `/pdf-reader` (lights the header ▶).
- `src/pages/PDFReader.jsx` — 7 new `data-guide="pdf-…"` anchors (pdf-replace/view/mode/translate/sentences/fulltranslation/reading; pdf-sample from inc. 1).
- `src/lib/guide/__tests__/pageGuides.test.js` — +3 red-proofed unit tests.
- `tests/e2e/guide-pdf-reader.spec.js` — new (2 tests).
- `README.md` (ship contract), `docs/superpowers/plans/2026-06-21-…-plan.md` (T9 ✅), `RESUME_HERE.md` (shipped section + queue `[x]`).

## Decision log
- **Conditional-only controls (Individual/Group, Sharper read, dense-page ease) taught in an always-mounted step's body, not their own arrow.** Why: a missing anchor hangs 3 s then skips (`guideController.js:101`); folding them in keeps the walk smooth (ADD-first/calm). Veto: a dedicated arrow can be added later with zero rework.
- **Guide opens on the `pdf-sample` step** (first anchored) so a blank-page user loads the passage and the rest of the arrows resolve; an already-loaded user harmlessly skips that one step.

## Gate
- build: exit 0.
- unit: **1860 passed** (+3, was 1857).
- lint: 0 errors (3 known exhaustive-deps warnings).
- e2e (UI-affecting): `guide-pdf-reader` 2/2 + `guide-full-page` 4/4 + `reading-sample` 2/2 = 8/8.
- perf: `pageGuides` lazy chunk 5.22 kB (on-demand only); eager `index` +~0.4 kB; `PDFReader` 77.2 kB / 22.5 kB gz (within known exception).

## Invariants
No `STORE_VERSION`/schema/free-path/`instruct.js` touch; no feature deleted; no paywall; no secrets. Theme-safe (no color/layout change).

## Deploy
Committed to `main` (= prod deploy). Confirm Vercel READY on upg- after push.

## Step-1 time check
KL 202606221017 at cycle start; CUTOFF far-future (forever loop) → continue. Single cycle per the loop contract; shell handles looping.

## ▶ NEXT
Phase 3c **T10 — the Study page (`/study`) deep dive** (7 modes incl. Produce, FSRS rating buttons, deck switch). Mirror this entry's shape; add `/study` to `PAGE_GUIDE_ROUTES`; `data-guide="study-…"` anchors; `pageGuides.test.js` + a `guide-*` e2e. One page per commit; Kheshav spot-checks live.
