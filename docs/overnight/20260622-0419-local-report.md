# Local build-loop cycle report — 2026-06-22 04:19 UTC (12:19 KL)

**Item shipped:** Directed epic axis-3 (Full Page Guide), plan Phase 3c → **T17 — the Speaking picker deep-dive guide**. The header ▶ "Tour this page" now works on `/speaking`.

## Why this item
- Step-1 time-check: loop runs forever (no `CUTOFF`); proceed.
- Queue (`RESUME_HERE.md` 🤖 Autonomous build queue) was all `[x]` through **T16 (Listening)**. No fresh axis-1 (correctness/content) or axis-2 (pedagogy) gap cleared the GOAL bar this cycle — the content ledger has converged across the recent cycles, and the directed epic explicitly owns axis-3 right now.
- The pre-thought `▶ NEXT` thread from the T16 shipped section + the next page in the plan's priority order = **T17 Speaking**. Self-source mode 3A prefers a pre-thought `▶ NEXT` thread.

## Anti-hallucination gate
- **Real:** evidence = the plan's T17 line + `Speaking.jsx` controls (the picker is non-theater at landing; the prep→record→results flow mounts only after a topic opens).
- **Measurable Done:** failing unit tests (PAGE_GUIDES['/speaking'] undefined) turn green; new e2e proves the header ▶ launches and every anchor resolves.
- **Verified:** every line is English UI copy describing live controls; no new Malay authored; the one Malay example ("Keluarga Saya / My Family") is the exact first topic in `speakingTopics.js`. Exam-code attribution (Malay 0546 Paper 3 / English 0500/0510 oral) matches the page's own copy + the already-web-verified Roleplay (T12) cycle.

## Red → green evidence
- **RED:** added the T17 describe block to `pageGuides.test.js`, ran `vitest -t speaking` → 2 failed for the right reason: `Array.isArray(undefined) === false`; `steps.map` → `TypeError: Cannot read properties of undefined (reading 'map')`.
- **GREEN:** after adding the `/speaking` entry + the `PAGE_GUIDE_ROUTES` seam line + the 3 anchors → `pageGuides.test.js` 27/27 (incl. the seam-sync test).

## What shipped (surgical, additive)
- `src/lib/guide/pageGuides.js` — new `/speaking` entry: centered intro + 3 anchored steps (`speaking-lang`, `speaking-topics`, `speaking-badges`) + a centered `arrow:'none'` "Inside a topic" summary.
- `src/lib/guide/pageGuideRoutes.js` — `/speaking` added to the eager seam (lights the header ▶).
- `src/pages/Speaking.jsx` — 3 inert `data-guide` anchors (toggle row; first topic card + its badge row via an `idx === 0` guard added to the existing `.map`).
- `src/lib/guide/__tests__/pageGuides.test.js` — +2 red-proofed unit tests.
- `tests/e2e/guide-speaking.spec.js` — new (2 tests).
- `README.md` + `RESUME_HERE.md` + the rollout plan doc — ship-contract bookkeeping (same commit).

## Decision log (decide-and-flag)
1. **Guide targets the PICK screen (non-theater landing); the prep→record→results flow is taught in a centered no-arrow summary.** Why: those controls mount only after a topic opens (and theater mode hides the header), so an arrow would hit a missing node and hang-then-skip 3 s. Veto: a separate in-session deep dive can be added later with zero rework.
2. **3 anchors (lang + topics + badges), not 2.** Why: Speaking uniquely has BOTH a binary language toggle (like Roleplay/Grammar) AND a topic-card+badges picker (like Comprehension/Listening). Veto: the badges step can fold into the topics step if fewer arrows are preferred.
3. **The detailed AI grade is hedged ("if you've set up AI grading").** Why: it's gated on `aiGradeAvailable()` = `isGeminiAvailable()`; the free heuristic band is always there. No over-claim.

## Gate result
- `npm run build` → exit 0.
- `npm run test:run` → **1879 passed** (+2, was 1877).
- `npm run lint` → 0 errors (3 known exhaustive-deps warnings).
- e2e (UI-affecting): `guide-speaking` 2/2 + regressions `guide-listening` 2/2 + `guide-full-page` 4/4 = **8/8**.
- Perf: `pageGuides` lazy chunk 24.46 kB / 8.38 kB gz (▶-only, exempt); `Speaking` page chunk 25.66 kB / 6.74 kB gz (< 70 KB budget); eager `index` 479.42 kB / 153.30 kB gz (≈flat vs T16).

## Invariants screened
No `STORE_VERSION` bump · no Supabase-schema/free-path change · `instruct.js` API untouched · no feature deleted · no secrets · content verified. ✓

## Deploy
Committed to `main` (= prod deploy via the repo's auto-push). Vercel READY to be confirmed on `upg-`.

**▶ NEXT:** Phase 3c **T18 — Import (`/import`)**.
