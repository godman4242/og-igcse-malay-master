# Local build-loop cycle report — 2026-06-15 01:24 UTC

**Mode:** GOAL-driven Self-source (autonomous build queue empty → assessed app against `docs/loop/GOAL.md`).
**Time-check (step 1):** no finite `CUTOFF` (forever loop) → continue.
**Item shipped:** Axis-1 (correctness) + Axis-2 (pedagogy) — roleplay scorecard credited a key vocab/imbuhan
word matched as a **substring** of an unrelated word.

## Assessment (GOAL §3A — grounded, not from memory)

Content (both languages) is read-audited clean over the last several cycles, and the prior cycle's `▶ NEXT`
flagged code-logic as the productive vein "where the content audits can't look." Probed the same
substring-vs-whole-word bug class the produce/cloze fix found:

- Swept `new RegExp` / `.includes()` word-matching across `src/lib`, `src/components`, `src/pages`.
- `clozeListening.js:25` → position-based token spans (clean). `Import.jsx:140` → internal bookkeeping,
  not rendered as a cloze (low impact). React-purity hits (`Dashboard:188`, `Grammar:867`) → correctly
  wrapped (`useState` initializer / `shuffle` helper). Perf unchanged (2 documented chunk exceptions only).
- **The evidenced gap:** `RoleplayScorecard.jsx` key-vocab/imbuhan detection used naive substring
  `.includes()` at 4 sites + a no-boundary highlight regex.

## Gap picked (anti-hallucination gate)

- **Real:** `RoleplayScorecard.jsx:322-324/395-397/444` + reproduced against real `scenarios.js:63` data —
  `keyVocab 'menu'` matches inside "menunggu" (to wait) / "menunjukkan" (to show); `'bil'` inside "ambil";
  `'harga'` inside "berharga". Node-verified `.includes()` TRUE vs whole-word FALSE.
- **Measurable Done:** a mounted component test — 0 green "✓ used" chips for student "Saya menunggu makanan"
  + `keyVocab:['menu']` (pre-fix: 1), 1 chip for the standalone "menu" positive control; + 8 pure-helper
  unit cases. All turn green.
- **Verified:** no content change; "menu"≠"menunggu" etc. are trivially distinct Malay words. The
  `(?<![\p{L}\p{N}])…(?![\p{L}\p{N}])` boundary already ships in prod (`savedWordHighlight.js`).

## Build (TDD, surgical)

- New `src/lib/wholeWordMatch.js` (`containsWholeWord` + `wholeWordSplitRegex`) — naive impl first, red-proofed.
- New `src/lib/__tests__/wholeWordMatch.test.js` (+8) — 4 whole-word cases failed against naive impl → green
  after boundary fix.
- New `src/components/__tests__/roleplayScorecardKeywordHits.test.js` (+2) — 0-chip case failed against the
  current substring code → green after the component fix.
- `src/components/RoleplayScorecard.jsx` — 4 `.includes()` → `containsWholeWord`; highlight regex →
  `wholeWordSplitRegex`; removed unused `studentLower`/`modelLower`.

## Gate result

`npm run build` ✅ · `npm run test:run` **1652 passed** (158 files, +10 net) ✅ · `npm run lint` **0 errors**
(3 known exhaustive-deps warnings) ✅. Existing `roleplayScorecardMistakeLang.test.js` stays green. No roleplay
e2e exists → covered by the mounted component test (real React render).

## Self-review (hostile)

Malay/English parity preserved (whole-word is the app's existing convention; English `keyVocab` phrases match
as units; mistake-lang test green). No content/theme/layout/STORE_VERSION/schema/free-path change. `instruct.js`
API untouched. No feature deleted.

## Deploy

One commit to `main` (= prod deploy via the upg- + og- Vercel projects).

## Decision log

Ship one shared `wholeWordMatch.js` helper (single test anchor, stops re-inlining) over inlining or a
`findSavedWordMatches` reuse (per-word regex alloc, no split-regex). Veto: span-offset rewrite of
`highlightKeywords` rejected (bigger diff in a file with a known lint warning).
