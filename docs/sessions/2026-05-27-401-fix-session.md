# 2026-05-27 — Session: 401 bug fix + handoff cleanup

## Context

Previous session (2026-05-26) shipped Adaptive Scaffolding (Master Plan
#3). User deployed `supabase functions deploy ai-proxy` afterwards but
the working-copy diff was never committed. Subsequent smoke-test curl
on 2026-05-27 returned `UNAUTHORIZED_INVALID_JWT_FORMAT`.

User's resume note flagged this as a P1 guest-only bug at
`src/lib/ai.js:151`, with three candidate fix paths.

## Diagnosis

Three things converge:

1. `supabase/functions/ai-proxy/config.toml` has `verify_jwt = true`.
   Supabase's gateway rejects anything that isn't a real JWT *before*
   function code runs.
2. `.env.local` ships `VITE_SUPABASE_KEY=sb_publishable_*` — Supabase's
   new key format (2025 rotation). Not JWT format.
3. `src/lib/ai.js:151` set `let authToken = SUPABASE_CONFIG.key` as a
   fallback for guests. The gateway rejects this token with
   `UNAUTHORIZED_INVALID_JWT_FORMAT`.

**Not just guest-only:** `getSupabase()` is the *sync* getter on a
module-level `supabaseClient` that's only set after `initSupabase()`
resolves. AuthGuard lazy-imports + initialises supabase on mount, so
on cold load there's a window where logged-in users hit the same
fallback and 401.

**Bonus broken path:** `callEdgeFunction()` at `src/config/supabase.js:373`
hardcoded the publishable key. Dead code — nothing called it — but
would have 401'd anything that revived it.

## Fix path chosen

Option C variant — keep `verify_jwt = true` (no infra change), require
a session JWT in the client, refuse cleanly when guests try AI.

Rationale over alternatives:
- `--no-verify-jwt`: opens function to the world; only IP rate limit
  + CORS as cost-control. Worse security than current state.
- Server-side publishable-key check: doesn't work without disabling
  `verify_jwt` first (gateway rejects before function code). Equivalent
  to A with extra plumbing.
- Shared-secret header: secret has to ship in the client bundle (VITE_
  env vars inline at build time). Same security level as the
  publishable key it would replace — security theatre.

## Files changed

| File | Change |
|---|---|
| `src/lib/ai.js` | `getSupabase()` → `await initSupabase()` (kills cold-load race). Drop the publishable-key fallback. Throw `AIError('Sign in…', 'unauthenticated')` when no session token. |
| `src/lib/gemini.js` | Same race fix (was omitting Authorization on cold load — different failure mode, same root cause). |
| `src/config/supabase.js` | Delete dead `callEdgeFunction` helper (was hardcoded to publishable key). |
| `src/components/RoleplaySession.jsx` | Add `unauthenticated` branch in error banner → "Sign in to use AI roleplay" instead of generic "AI connection issue". |
| `.gitignore` | Add `supabase/.temp/` — Supabase CLI working dir, was being auto-staged by `.githooks/pre-commit`. |

## Verification

- `npm run lint` → 0 errors, 3 pre-existing warnings (none in files I touched)
- `npm run build` → `index-*.js` 405.36 KB / 130.48 KB gz (unchanged from 2026-05-26 baseline)
- No edge function redeploy needed (only client code changed; `verify_jwt = true` stays)

## Behaviour after fix

| User state | Before | After |
|---|---|---|
| Logged-in, post-AuthGuard-init | 200 (real JWT) | 200 (real JWT) |
| Logged-in, cold-load racing init | 401 (fell back to bad key) | 200 (awaits init) |
| Guest | 401 (opaque) | `AIError('unauthenticated')` — Roleplay shows "Sign in to use AI"; writing alert shows the message; CikguBot silently falls back to expert system |

## Commit hygiene incident

First commit (`241e1b6 chore(ai-proxy): commit deployed OpenRouter migration`)
accidentally bundled ALL 5 of this session's changes + 9 untracked
`supabase/.temp/` files. Root cause: `.githooks/pre-commit` runs
`git add -A`, so every commit collapses the whole dirty tree. The
post-commit hook auto-pushed before this could be caught.

Inspected `supabase/.temp/` contents — no secrets leaked (project ref
+ pooler hostname + version numbers; pooler URL has no password). No
force-push needed. Follow-up: gitignore + `git rm --cached` to untrack,
plus this session note documenting the actual contents of `241e1b6`.

## Threads B / C status

- **B (Adaptive Scaffolding QA matrix follow-ups):** blocked. The
  session note `docs/sessions/2026-05-27-adaptive-scaffolding-deploy.md`
  the previous session was supposed to create doesn't exist, so I
  don't have the user's QA observations or the console snippets for
  forcing heavy/light scaffolding. Need user paste.
- **C (v2 prompt guardrail for hallucinated spelling fixes):** depends
  on B — only worth touching if QA shows ≥20% bad-essay rate.

## Open follow-ups

- Decide whether to disable `.githooks/post-commit` auto-push for
  sessions where commit hygiene matters more than zero-keystroke push.
- Decide whether `.githooks/pre-commit`'s `git add -A` is desirable.
  It's convenient but makes "stage selectively" impossible — every
  commit drags the whole dirty tree.

## Session continuation — after the 401 fix

### Adaptive Scaffolding QA — via static code review

User opted out of the manual 12-row browser QA matrix and asked for a
code-review pass instead. Findings:

- **11 of 12 rows verified by code.** Server-side scaffold rules in
  `ai-proxy/index.ts:157-216` enforce per-level caps (≤3 fixes heavy,
  ≤2 missed-vocab on roleplay, single-sentence rewrite, etc.); client
  renderers respect `shouldHideRewriteByDefault('heavy') === true` and
  the `Stretch:` extraction in the scorecard.
- **Row 7 — cosmetic gap.** The EN roleplay "AI quota exhausted"
  fallback at `Roleplay.jsx:183` is a static "English roleplay needs
  AI — daily quota resets at midnight." with no scaffold-aware
  variation. Spec expected a "try a grammar drill" hint on heavy.
  Decision: punt — invite-only users essentially never hit the EN
  quota cap, ROI is negative.
- **Heads-up on the QA matrix wording.** §10.2 says "Adaptive: heavy"
  chip but the actual rendered text is "Adaptive: focused" per spec
  §13's anti-infantilising remap (`annotationView.js:9`). Matrix wording
  should be updated, not the code.

### Thread C — hallucinated-correction guardrail SHIPPED

Added a refusal clause to `buildWritingFeedbackV2Prompt`:

> "DO NOT INVENT CORRECTIONS. If a span's surface text is already
> orthographically and grammatically correct, set its category to null
> and groupId to null — never flag it. Never emit a fix where fix.fix
> is identical to the original surface text (e.g. \"kerana\" →
> \"kerana\" is invalid). When in doubt, leave the span un-categorised."

The original guard was that this should only ship if QA showed ≥20%
hallucination rate. User flipped to code review instead, so the gate
can't be measured — defaulted to ship-the-guardrail (cost ≈ one
sentence in prompt, risk = mild over-correction in the other
direction, payoff = student trust in the writing analyzer).

**REDEPLOY REQUIRED.** Run on the user's machine:

```bash
supabase functions deploy ai-proxy
```

The agent has no local supabase CLI access. Until redeploy, the
guardrail clause is committed in repo but the prod function still
runs the old prompt.

### Antigravity test-automation cleanup

Deleted six abandoned files plus reverted polluted package files:

- `import-map.json`, `mock-vitest.js`, `run-tests.js`,
  `scratch-loader.js` — half-baked custom test runner that
  reinvented vitest poorly (missing async describe, `vi.fn`, hooks).
  The repo already passes 214/214 via `npm run test:run`.
- `tests/example_google.test.js`, `test-results/` — `playwright init`
  boilerplate that tests google.com, not the app. `@playwright/test`
  wasn't even in `package.json`.
- `package.json` + `package-lock.json` — reverted Antigravity's
  unfinished Playwright + `chrome-devtools-mcp` additions
  (~1.1k-line lock-file delta). If Playwright is wanted later,
  install cleanly with `npm i -D @playwright/test` + a real
  `playwright.config.js`.
- `.gitignore` — added `test-results/`, `playwright-report/`,
  `playwright/.cache/` preemptively.

If auto-testing of the §10.2 QA matrix is wanted later, the rough
spec is: Playwright + `playwright.config.js` pointing at the Vite
dev server + 12 test cases that prime localStorage via
`page.evaluate()` (replacing the in-session `qa.heavy()` / `qa.light()`
console helpers built earlier in this conversation). ~1 hr of work,
deferred.
