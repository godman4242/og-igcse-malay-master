# PROBLEMS.md — IGCSE Malay Master

> Auto-generated by Clawdia on 2026-08-08 20:41 (+08:00).
> Only failures and warnings are listed — passing checks are hidden.
> Fixed problems are moved to the History section with ✅.

## Summary

| Check  | Result | Notes |
|--------|--------|-------|
| Build  | ✅ PASS (exit 0) | 2 warnings (CSS pseudo-element, chunk size) |
| Lint   | ✅ PASS (exit 0) | 0 errors, 3 warnings |
| Tests  | ❌ FAIL (exit 1) | 2378/2379 passed, 1 failure (authGuard PLAUSIBLE-2 regressed) — 2 warning categories |

---

## Active Problems

### 1. authGuard PLAUSIBLE-2 — 0-card account blob-only state restore (REGRESSED)

- **Category:** test
- **Severity:** error
- **File:** `src/components/__tests__/authGuardSignInMergeIntegration.test.js:147`
- **Detail:** FAIL: AuthGuard sign-in merge > PLAUSIBLE-2 — a 0-card account restores its blob-only state on a fresh device instead of being wiped. AssertionError: expected false to be true. The test checks that either restored OR wiped is true, but both were false — the sign-in chain did not settle correctly within the 15s timeout. Previously flaky (2 failures in 4 runs on Aug 6, passed Aug 8 18:38, now regressed in Aug 8 20:41 run). GoTrueClient multiple instances warning co-occurs (6 instances logged).
- **Suggested fix:** This is an intermittent/flaky test. The sign-in merge logic for 0-card accounts may have a race condition in the jsdom environment. Investigate the async chain in authGuardSignInMergeIntegration — the waitFor timeout at line 147 expects either examDate restoration or backend wipe within 15s. Consider: (1) increasing timeout, (2) ensuring GoTrueClient is properly singletonized in test setup to avoid the multi-instance warning that may interfere, (3) checking if a recent code change broke the blob-only state restore path.

### 2. GoTrueClient multiple instances detected in authGuard test (REGRESSED)

- **Category:** test
- **Severity:** warning
- **File:** `src/components/__tests__/authGuardSignInMergeIntegration.test.js`
- **Detail:** GoTrueClient@sb-fake-auth-token warning: Multiple GoTrueClient instances detected in the same browser context. 6 instances logged during the PLAUSIBLE-2 test. This warning was previously in history as fixed (not observed in Aug 8 18:38 run) but has regressed. It may contribute to the PLAUSIBLE-2 test failure by causing undefined behavior when instances are used concurrently under the same storage key.
- **Suggested fix:** Ensure the test setup creates only a single Supabase client instance. If the test simulates multiple devices/sessions, use separate storage keys or mock the GoTrueClient to avoid the multi-instance warning. The co-occurrence with the PLAUSIBLE-2 failure suggests this may be a root cause.

### 3. CSS pseudo-element `::highlight` not recognized by lightningcss

- **Category:** build
- **Severity:** warning
- **File:** `src/index.css` (compiled output)
- **Detail:** `'highlight' is not recognized as a valid pseudo-element. Did you mean ':highlight' (pseudo-class) or is this a typo?` — affects `::highlight(saved-words)` in compiled CSS. Build succeeds but lightningcss emits this warning during minification.
- **Suggested fix:** This is the CSS Custom Highlight API (`::highlight(name)`). lightningcss doesn't support it yet. Options: (1) suppress the warning, (2) use a different minifier, (3) accept the warning as cosmetic since the build still succeeds. The pseudo-element is valid per the CSS spec.

### 4. Large bundle chunk exceeds 500 kB after minification

- **Category:** build
- **Severity:** warning
- **File:** `dist/assets/transformers.web-CqEpkFeo.js` (847 kB)
- **Detail:** `transformers.web-CqEpkFeo.js` is 847 kB after minification, triggering the Vite/Rolldown chunk size warning. Other large chunks: `index-l_PyVqFH.js` (480 kB), `pdf-DYiRSAyA.js` (331 kB) are under the 500 kB threshold but still sizable.
- **Suggested fix:** Use dynamic `import()` to code-split `transformers.js` so it loads only when needed. Consider adjusting `build.chunkSizeWarningLimit` or configuring `manualChunks` in vite/rolldown config.

### 5. useEffect missing dependencies in RoleplayScorecard

- **Category:** lint
- **Severity:** warning
- **File:** `src/components/RoleplayScorecard.jsx:145`
- **Detail:** React Hook useEffect has missing dependencies: `'addMistake'`, `'addRoleplayHistory'`, `'lang'`, `'messages'`, `'scenario.id'`, `'scenario.title'`, `'scoreData.areasToImprove'`, `'scoreData.keyPhraseMissed'`, and `'scoreData?.overallBand'`. Either include them or remove the dependency array.
- **Suggested fix:** Add the missing dependencies to the useEffect dependency array, or wrap the effect logic in `useCallback`/`useMemo` to stabilize references. If intentional, add an `eslint-disable` comment with justification.

### 6. userInterests logical expression may cause useMemo re-render in Comprehension

- **Category:** lint
- **Severity:** warning
- **File:** `src/pages/Comprehension.jsx:47`
- **Detail:** The `'userInterests'` logical expression could make the dependencies of useMemo Hook (at line 61) change on every render. Move it inside the useMemo callback or wrap in its own `useMemo()`.
- **Suggested fix:** Move the `userInterests` initialization inside the useMemo callback, or wrap `userInterests` in its own `useMemo()` Hook to stabilize the reference.

### 7. userInterests logical expression may cause useMemo re-render in Roleplay

- **Category:** lint
- **Severity:** warning
- **File:** `src/pages/Roleplay.jsx:30`
- **Detail:** The `'userInterests'` logical expression could make the dependencies of useMemo Hook (at line 42) change on every render. Move it inside the useMemo callback or wrap in its own `useMemo()`.
- **Suggested fix:** Move the `userInterests` initialization inside the useMemo callback, or wrap `userInterests` in its own `useMemo()` Hook to stabilize the reference.

### 8. Node.js `--localstorage-file` path warning in vitest

- **Category:** test
- **Severity:** warning
- **File:** vitest config / Node.js runtime
- **Detail:** Warning: `--localstorage-file` was provided without a valid path. Appears 24+ times across worker processes during the test run. Tests pass — this is a Node.js runtime warning about an invalid flag argument.
- **Suggested fix:** Check vitest config for a `--localstorage-file` flag with an empty or invalid path. Remove or fix the path. Cosmetic — does not affect test results.

---

## History

### ✅ GuideHud test failure — renders the lazy dock zones once a drag begins
- **Date:** 2026-08-08
- **Category:** test | **Severity:** error
- **File:** `src/components/guide/__tests__/GuideHud.test.js:49`
- **Detail:** FAIL: GuideHud > renders the lazy dock zones once a drag begins. AssertionError: expected null to be truthy.
- **Fix:** Passing since Aug 8 18:38 run, still passing in Aug 8 20:41 run (GuideHud not among failures). ✅ Fixed.

### ✅ GuideHud suspended resource not wrapped in act(...)
- **Date:** 2026-08-08
- **Category:** test | **Severity:** warning
- **File:** `src/components/guide/__tests__/GuideHud.test.js`
- **Detail:** A suspended resource finished loading inside a test without act() wrapping.
- **Fix:** Not observed since Aug 8 18:38 run, including 20:41 run. ✅ Fixed.

### ✅ React act() warnings in typeModeGrading tests
- **Date:** 2026-08-08
- **Category:** test | **Severity:** warning
- **File:** `src/components/study/__tests__/typeModeGrading.test.js`
- **Detail:** An update to TypeMode inside a test was not wrapped in act(...).
- **Fix:** Not observed since Aug 8 16:35 run, including 20:41 run. ✅ Fixed.

### ✅ React act() warnings in masteredWordsModal tests
- **Date:** 2026-08-08
- **Category:** test | **Severity:** warning
- **File:** `src/components/dashboard/__tests__/masteredWordsModal.test.js`
- **Detail:** The current testing environment is not configured to support act(...).
- **Fix:** Not observed since Aug 8 16:35 run, including 20:41 run. ✅ Fixed.

### ✅ Zustand persist middleware storage unavailable in test environment
- **Date:** 2026-08-08
- **Category:** test | **Severity:** warning
- **File:** `src/store/__tests__/ (multiple test files)`
- **Detail:** Repeated stderr warnings: [zustand persist middleware] Unable to update item 'igcse-malay-store'.
- **Fix:** Not observed since Aug 8 16:35 run, including 20:41 run. ✅ Fixed.

### ✅ 'Please use the legacy build' warning in Node.js test environment
- **Date:** 2026-08-08
- **Category:** test | **Severity:** warning
- **File:** `src/pages/__tests__/importStemProvenance.test.js`
- **Detail:** Warning: Please use the legacy build in Node.js environments.
- **Fix:** Not observed since Aug 8 16:35 run, including 20:41 run. ✅ Fixed.

---

## Recommended Actions for Claude

1. **🚨 URGENT: authGuard PLAUSIBLE-2 test failure regressed** — `authGuardSignInMergeIntegration.test.js:147` fails again after passing in the 18:38 run. The test expects a 0-card account's blob-only state to be restored or wiped within 15s, but neither happened. This is a known flaky test (Aug 6: 2/4 failures, Aug 8 18:38: pass, Aug 8 20:41: fail). The GoTrueClient multi-instance warning co-occurs and may be the root cause. **Priority: investigate the Supabase client instantiation in the test setup — ensure only one GoTrueClient instance is created, or use separate storage keys for simulated devices.**

2. **GoTrueClient multi-instance warning regressed alongside PLAUSIBLE-2** — 6 GoTrueClient instances were detected during the failing test. This was marked fixed in the 18:38 run but has returned. It may cause undefined concurrent behavior under the same storage key, directly contributing to the PLAUSIBLE-2 failure. **Fix this first — it likely resolves both issues.**

3. **Lint warnings (3) are pre-existing and stable** — `RoleplayScorecard.jsx` exhaustive-deps and two `userInterests` useMemo warnings in `Comprehension.jsx` and `Roleplay.jsx`. Safe to leave as-is if intentional, but ideally wrap `userInterests` in its own `useMemo()` in both files.

4. **Build warnings (2) are cosmetic** — `::highlight(saved-words)` is valid CSS Custom Highlight API but lightningcss doesn't recognize it yet; the 847 kB transformers chunk is a known size issue from the pinned `@huggingface/transformers` v3 dependency. Neither blocks the build.

5. **`--localstorage-file` vitest warning** — 24+ occurrences, cosmetic but noisy. Check vitest config for an empty/invalid `--localstorage-file` flag path. Easy fix if the flag path is corrected or removed.
